import { describe, it } from 'vitest';

/**
 * TODO (Фаза 0a.4): покрыть тестами `StructureService`:
 *   - summary возвращает все счётчики с tenant isolation;
 *   - counters processCount/regulationCount/policyCount/metricCount;
 *   - RBAC: member → 403 на counters (γ preview доступ только owner/admin).
 */
describe.skip('StructureService', () => {
  it('TODO: покрыть summary + γ counters + RBAC', () => {
    // см. план 2026-05-21-phase-0a-data-model-and-graph-infra.md §7-§10
  });
});
