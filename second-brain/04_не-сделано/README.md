# 🚧 Не сделано — реестр открытых пробелов и отложенного

> **Назначение.** Единая точка правды о том, что **намеренно не доделано, отложено или заблокировано** —
> с причиной и ссылкой на детали. Чтобы через месяц не гадать «почему так» и чтобы ни один долг
> не потерялся между ветками и сессиями.
>
> Это **не** баг-трекер задач и **не** backlog идей (идеи → `plans/analysis/`, контракты на реализацию →
> `plans/tz/`). Сюда попадает только то, что **по ходу работы вскрылось как пробел или сознательно отложено**.

## Правила ведения (обязательно)

1. **Появился пробел / решили не делать сейчас / заблокированы → ДОБАВЬ строку** в таблицу «Открыто» ниже.
   Это делается в тот же момент, когда фиксируешь «осознанную отсрочку» (а не «потом вспомню»). Триггеры:
   - фича выкатывается частично, остаток отложен;
   - вскрыт архитектурный/иной пробел, который не закрывается в этой задаче;
   - заблокированы внешним решением (владелец / бюджет / прод-доступ / внешняя система);
   - известный минор/долг, который сознательно не чиним сейчас.
2. **Каждая строка обязана содержать:** дату, что именно не сделано, **почему**, ссылку на детали
   (файл `plans/tz/*`, `plans/analysis/*` или `path:line` в коде), статус, кто/что разблокирует.
   Если пробел тянет на реализацию — заведи `plans/tz/YYYY-MM-DD-*.md` и сошлись на него отсюда (не дублируй текст).
3. **Сделали / исправили → УБЕРИ строку** из «Открыто». Чтобы не терять след — перенеси одной строкой
   в «Закрытые (архив)» внизу с датой и коммитом. (Полную историю и так хранит git.)
4. **Не дублируй** уже описанное в `plans/tz/` или `docs/operations/prod-deploy-log.md` — здесь только
   короткая строка-указатель. Реестр — навигатор, детали живут в профильных файлах.
5. Перед началом новой задачи **загляни сюда** — возможно, пробел уже описан и пора его закрыть.

> Это правило также закреплено в корневом `CLAUDE.md` (раздел «Реестр не-сделанного») как часть
> триггеров завершения работы.

## Открыто

| Дата | Что не сделано | Почему | Детали | Статус / разблокирует |
|---|---|---|---|---|
| 2026-06-29 | **Месяц компании: TTS «Озвучить» письмо** (disabled-заглушка, как в дневном/недельном) | Письмо-проза TTS-friendly, синтез речи — отдельный объём | ТЗ [`2026-06-29-month-company-monthly-brief.md`](../../plans/tz/2026-06-29-month-company-monthly-brief.md) §«Не входит» | vNext |
| 2026-06-29 | **Месяц компании: достижения R/Y/G со статусом+вехой** (переиспользован существующий `achievements`-виджет как есть) | Действенные вехи — апгрейд виджета, отдельный объём; на MVP достаточно существующего | ТЗ [`2026-06-29-month-company-monthly-brief.md`](../../plans/tz/2026-06-29-month-company-monthly-brief.md) §«Не входит» | vNext |
| 2026-06-29 | **Месяц компании: мобильная вёрстка героя** | Канон прототипа — тёмный десктоп; мобильный герой — отдельным проходом | ТЗ [`2026-06-29-month-company-monthly-brief.md`](../../plans/tz/2026-06-29-month-company-monthly-brief.md) §«Не входит» | vNext (отдельный проход) |
| 2026-06-29 | **Навигация отчётов: отдельная страница-архив** (иерархия год→месяц + фасеты) | На раннем пилоте (~4 юзера, прод почти пуст) inline-навигатор + recent-список достаточны; полноценный архив — при накоплении истории | ТЗ [`2026-06-30-report-date-navigation-archive.md`](../../plans/tz/2026-06-30-report-date-navigation-archive.md) §«Не входит» | vNext (при накоплении истории) |
| 2026-06-29 | **Навигация отчётов: mini-calendar для недели + сравнение периодов** (сейчас клик-подпись → нативный date/month-picker; delta «период к периоду» не делали) | ASSUMPTION 1-го этапа: нативный picker; недельный hover-picker и delta-сравнение — отдельный объём | ТЗ [`2026-06-30-report-date-navigation-archive.md`](../../plans/tz/2026-06-30-report-date-navigation-archive.md) §«Не входит» | vNext |
| 2026-06-29 | **Месяц/навигация: Playwright-визуальная приёмка героя/навигатора/архива/empty** (сверка с прототипом) | В проде ещё нет месячного снапшота и накопленных периодов; верификация — build/tsc/lint/vitest зелёные. Визуальная приёмка глазами — после первого прогона крона `operations-monthly-digest` / ручного `generate` | ТЗ [`2026-06-29-month-company-monthly-brief.md`](../../plans/tz/2026-06-29-month-company-monthly-brief.md) §DoD | после первого месячного снапшота на проде (qa-tester) |
| 2026-06-29 | **Неделя компании: TTS «Озвучить» письмо** (disabled-заглушка, как в дневном) | Письмо-проза TTS-friendly, но синтез речи — отдельный объём | ТЗ [`2026-06-29-week-company-weekly-brief.md`](../../plans/tz/2026-06-29-week-company-weekly-brief.md) §«Не входит» | vNext |
| 2026-06-29 | **Неделя компании: группировка таблицы план/факт по отделам / топ-5** (сейчас плоский список + сортировка надёжность/риск) | Команда до ~10 чел — плоский список достаточен; числовой триггер — **при >12 строк в таблице** | ТЗ [`2026-06-29-week-company-weekly-brief.md`](../../plans/tz/2026-06-29-week-company-weekly-brief.md) §«Не входит» (Р6) | vNext (при >12 строк) |
| 2026-06-29 | **Неделя компании: мобильная вёрстка недельного героя** | Канон прототипа — тёмный десктоп; мобильный герой — отдельным проходом | ТЗ [`2026-06-29-week-company-weekly-brief.md`](../../plans/tz/2026-06-29-week-company-weekly-brief.md) §«Не входит» | vNext (отдельный проход) |
| 2026-06-29 | **Неделя компании: событийная цепочка тайминга на завершение синков** (сейчас фикс-час пн 06:00 локали) | Фикс-час решает на текущем масштабе — к утру пн ночные синки за пт+выходные завершены | ТЗ [`2026-06-29-week-company-weekly-brief.md`](../../plans/tz/2026-06-29-week-company-weekly-brief.md) §«Не входит» · `operations-weekly-digest.cron.ts` | vNext |
| 2026-06-29 | **Утренняя сводка задач: руководительский/командный разрез** (сводка открытых/закрытых по команде директору/руководителю) | Решение владельца В2: на MVP каждый сотрудник получает сводку про СВОИ задачи; командный разрез требует ролей+агрегации+прав — отдельная фича | ТЗ [`2026-06-29-morning-tasks-digest.md`](../../plans/tz/2026-06-29-morning-tasks-digest.md) §«Не входит» | vNext (отдельное ТЗ; с подтверждения владельца) |
| 2026-06-29 | **Утренняя сводка задач: вынос доставки в BullMQ-очередь** (сейчас синхронный цикл по сотрудникам раз в сутки) | Преждевременная оптимизация под текущий масштаб (~единицы юзеров/тенант); числовой триггер пересмотра — **>2000 активных сотрудников/тенант** (challenge-loop Q2 ТЗ) | `tracker/workers/morning-tasks-digest.cron.ts` (цикл `for org → for user`) · ТЗ [`2026-06-29-morning-tasks-digest.md`](../../plans/tz/2026-06-29-morning-tasks-digest.md) | vNext (при >2000 активных сотрудников/тенант) |
| 2026-06-29 | **Утренняя сводка задач: пер-юзерная подписка/отписка в кабинете** (UI настройки «получать утреннюю сводку») | На MVP действует существующий механизм `eventTypeDeny`/`eventTypeAllow` в user-prefs «почтальона» (кто отписался — не получит); отдельный UI — позже | ТЗ [`2026-06-29-morning-tasks-digest.md`](../../plans/tz/2026-06-29-morning-tasks-digest.md) §«Не входит» · `conversational.service.ts` user-prefs | vNext (UI настройки уведомлений) |
| 2026-06-28 | **Push: боевые креды транспортов на проде** (APNs `.p8`, FCM service-account, RuStore service-token, VAPID-пара) — без них транспорт молча no-op | Каркас отправки рабочий (token-JWT ES256 для APNs через `node:http2`; FCM v1 + OAuth2 из service-account JWT RS256; RuStore VK PNS; web-push) — но требует реальных ключей из Apple/Google/RuStore/VAPID. R34: отсутствие любого транспорта не ломает остальные | `backend/src/modules/push/services/{apns,fcm,rustore}-sender.service.ts` · env.schema `PushSchema` · `docs/operations/feature-flags.md` «🔑 НЕ флаг…» · `prod-deploy-log.md` Шаг 1 | владелец (положить креды в прод `.env`) |
| 2026-06-28 | **Удаление аккаунта: полная анонимизация PII** (сейчас `deleteAccount` = soft-delete `deletedAt` + чистка контактных точек: PushToken/ChannelBinding/ConversationMember/сессии; email/name НЕ затираются) | App Review 5.1.1(v) удовлетворён доступом к удалению + отзывом доступа; полная анонимизация PII (затереть email/name, отвязать авторство сообщений) — отдельный объём, не блокирует Ф6a | `backend/src/modules/accounts/accounts.service.ts` `deleteAccount` · `account-deletion.controller.ts` | разработчик (follow-up, при требовании 152-ФЗ/GDPR-стирания) |
| 2026-06-28 | **Push-debounce и unread-smart-badge — крутилки заведены, потребители не подключены** (`push_debounce_seconds`=30, `unread_smart_badge`=true в registry+сиде) | Ф6a — backend push-фундамент; дебаунс сигналов и бейдж — поведение клиента/доставки, реализуется в Ф6b (frontend Мобильная Кора) | `seed-admin-setting-chat.ts` · `admin-setting-schema-registry.ts` · ТЗ [`2026-06-21-unified-chat-kora-tz.md`](../../plans/tz/2026-06-21-unified-chat-kora-tz.md) Ф6 | разработчик (Ф6b — фронт/доставка) |
| 2026-06-28 | **Единый чат Ф5b — AI-крючки (вторая половина)**: «Спросить Кору/Что решили» поверх chat-v2 со ссылкой на `Message.id`; сообщение→задача (intake `chat`) и →решение (реестр решений); taskType `chat-summary` «Что пропустил» (стабильный SYSTEM + переменное в конце user) + крутилки `chat_summary_min_messages`/`chat_summary_idle_days`; `seed-llm-task-routes-chat.ts` (chat-summary→DeepSeek V4 Pro) + `chat-summary.prompt.ts` | Ф5a (chat.ingest + voice ASR + системное сообщение закрытия встречи) выкачена; остаток Ф5 — отдельный объём (retrieval/intake/prompt/seed), вынесен чтобы Ф5a доехал чистым | ТЗ [`2026-06-21-unified-chat-kora-tz.md`](../../plans/tz/2026-06-21-unified-chat-kora-tz.md) Ф5 (R19) · статус-таблица ТЗ (строка Ф5 «🟡 ЧАСТИЧНО») | разработчик (следующая сессия по unified-chat) |
| 2026-06-28 | **`/message-threads` — курсор/сортировка/фильтр в памяти** (Ф4a грузит ВСЕ разговоры члена, потом slice по составному курсору) | Корректно и просто для раннего пилота (≈4 юзера); SQL-keyset по computed-полям (unread=maxSeq−lastReadSeq, active=count) сложнее без выигрыша на текущем масштабе | `backend/src/modules/messaging/services/inbox.service.ts` `loadThreadRows`/`listThreads` · ТЗ [`2026-06-21-unified-chat-kora-tz.md`](../../plans/tz/2026-06-21-unified-chat-kora-tz.md) Ф4 | переписать на SQL-keyset при росте числа разговоров/пользователя (сотни+) — выход из пилота |
| 2026-06-28 | **Backfill `Message.contentStripped` на проде не прогнан** (новые сообщения индексируются на записи; старые — без плейнтекста до прогона) | В среде агента нет локальной БД; backfill опционален и идемпотентен | `backend/scripts/backfill-message-contentstripped.ts` (в STEPS `phase:'backfill'`) · `prod-deploy-log.md` Шаг 8 | прогнать на проде `apply-prod-deploy --mode update` (или вручную) при выкате Ф4a |
| 2026-06-27 | **Прод-приёмка миграций/HNSW/партиций слоя источника** — партиц-миграция `20260627120000` (HASH IdeaBlock/Entity по tenantId + FK-рефактор), `20260627130000`, `20260627140000`, HNSW Theme/SourceEpisode — на ПРОД не применены | ✅ **Прогнаны и приняты на чистой локальной БД 2026-06-28** (`migrate deploy` + 90 миграций + `apply-postgres-init` + 118 сидов OK). В партиц-миграции найден и **исправлен баг**: `RENAME TO *_old` не переименовывает индекс pkey → коллизия `IdeaBlock_pkey`/`Entity_pkey` при `ADD CONSTRAINT` (упало бы и на проде) — добавлен `DROP CONSTRAINT IF EXISTS *_pkey` на `*_old`. Остаётся: применить на прод + проверить swap-перенос на реальных данных | `prod-deploy-log.md` Шаг 4 · [local-dev-setup.md](../../docs/operations/local-dev-setup.md) журнал · миграция `backend/prisma/migrations/20260627120000_*/migration.sql` | needs-prod-access (на проде: если есть failed-запись в `_prisma_migrations` — `migrate resolve --rolled-back` перед deploy; затем проверить swap + retrieval) |
| 2026-06-28 | **Фаза B — весь стек Коры в Docker локально** (корневой `docker-compose.yml`: postgres+redis+migrate+backend+frontend с локальным env) | Сначала отлажен гибрид (зависимости в Docker, back/front нативно — быстрая итерация); докеризация целиком — отдельная фаза после стабилизации кабинета (как и просил владелец: «в конечном итоге через докер») | [local-dev-setup.md](../../docs/operations/local-dev-setup.md) §5 (черновой набросок: отдельный `.env.local.docker`, НЕ перезаписывать прод `/.env`; NEXT_PUBLIC_* инлайнятся на сборке) | запланировано (владелец инициирует фазу B) |
| 2026-06-27 | **Материализация Битрикс-чатов (`kind='chat'`)** — код-способность есть (модели `SourceEpisode`/ingest/контракт роутера принимают `kind='chat'`), но наполнение данными не идёт | Внешний блокер — живой поток из Битрикса (vNext-ТЗ интеграции); решение владельца Р7 (код сразу, материализация при живом потоке) | ТЗ [`2026-06-27-sloy-istochnika-i-marshrutizator-poiska-tz.md`](../../plans/tz/2026-06-27-sloy-istochnika-i-marshrutizator-poiska-tz.md) §«Вне scope» | vNext (отдельное ТЗ интеграции Битрикса; внешний поток) |
| 2026-06-27 | **Frontend-рендер `answerKind`** (список эпизодов / карта тем / итоги периода) — бэкенд-контракт `answerKind`+`episodes[]` готов и течёт в API DTO, но UI «Мастера» рендерит только текст | Решение Ф10: UI-рендер форм сверх контракта — отдельная фича (трогает живые поверхности чата); без живого UI не принять | ТЗ [`2026-06-27-sloy-istochnika-i-marshrutizator-poiska-tz.md`](../../plans/tz/2026-06-27-sloy-istochnika-i-marshrutizator-poiska-tz.md) Ф10 «Не входит» | vNext (ТЗ фронта; владелец / визуальная приёмка) |
| 2026-06-27 | **Сырой SQL by-id на партиц. таблицах без partition-pruning** — embedding-UPDATE `IdeaBlock` по `id` без `tenantId` корректен (cuid уникален), но делает 64 PK-index-lookup без прунинга партиций | Минорная оптимизация: масштаб-критичный retrieval-путь прунит корректно (несёт `tenantId`); by-id UPDATE редок | ТЗ [`2026-06-27-sloy-istochnika-i-marshrutizator-poiska-tz.md`](../../plans/tz/2026-06-27-sloy-istochnika-i-marshrutizator-poiska-tz.md) Ф1 | minor / vNext (добавить `tenantId` в WHERE by-id UPDATE при оптимизации) |
| 2026-06-27 | **vNext: полный диалоговый Мастер от probe** (многоходовый concierge с инструментами, программный старт `concierge.process` от probe) | Развилка В1: в этой итерации сделан минимальный шов (одноходовая петля clarify/confirm + детерминированный apply); полный Мастер — координационный шов / расширенная fail-surface, отложен | vNext-заглушка [`vNext-probe-master-full-dialog.md`](../../plans/tz/vNext-probe-master-full-dialog.md) · ТЗ [`2026-06-27-probe-clarify-dialog-tz.md`](../../plans/tz/2026-06-27-probe-clarify-dialog-tz.md) | vNext (по «реализуй»; с подтверждения владельца) |
| 2026-06-27 | **vNext: применение probe для доменов без apply-слоя** (цели, карточки, темпоральные, идеи, навыки, инсайты, атрибуция, регламент-владелец/шаги, consistency) | Развилка В4: онтологический долг O(домены) — каждый домен требует своего идемпотентного apply-пути; в этой итерации детерминированный apply покрывает поднабор доменов | vNext-заглушка [`vNext-probe-apply-remaining-domains.md`](../../plans/tz/vNext-probe-apply-remaining-domains.md) · ТЗ [`2026-06-27-probe-clarify-dialog-tz.md`](../../plans/tz/2026-06-27-probe-clarify-dialog-tz.md) | vNext (по «реализуй»; с подтверждения владельца) |
| 2026-06-27 | **Follow-up: дайджест probe закрывается после одного ответа** — ответ на `probe.digest` помечает всё уведомление answered → остальные probe пачки не применяются | Pre-existing ограничение; адресность ответа к нужному probe пачки — закрыта Ф5 (`resolveDigestProbeEventId`), но один ответ применяется к одному probe из пачки | ТЗ [`2026-06-27-probe-clarify-dialog-tz.md`](../../plans/tz/2026-06-27-probe-clarify-dialog-tz.md) Ф5 | low / vNext (per-item ответы дайджеста; владелец/разработчик) |
| 2026-06-27 | **Follow-up: ответ owner на эскалацию probe не авто-применяется** — escalation-уведомление owner/admin — терминальный FYI (`responseStatus=null`); owner действует в кабинете вручную | Допустимо для минимального шва: эскалация снимает probe с автоматики и передаёт человеку; авто-применение ответа эскалации — отдельный слой | ТЗ [`2026-06-27-probe-clarify-dialog-tz.md`](../../plans/tz/2026-06-27-probe-clarify-dialog-tz.md) Ф3 | vNext (владелец/разработчик) |
| 2026-06-27 | **Follow-up: curation `approve_with_edits` не материализует rename** — Ф1 прокидывает `editedName` в `curation.decide`, но curation не применяет переименование к сущности регламента | Пробел самого curation-модуля (вне scope probe): probe доставляет новое имя, применение rename — ответственность curation | ТЗ [`2026-06-27-probe-clarify-dialog-tz.md`](../../plans/tz/2026-06-27-probe-clarify-dialog-tz.md) Ф1 | vNext (правка curation-модуля; владелец/разработчик) |
| 2026-06-27 | **Follow-up: existence-confirm owner-assign в intent-пути** — `refine`→`approve_with_edits(name)`; назначение владельца через свободный текст различается только в degraded-пути (`mapExistenceConfirmAnswer`) | Edge-case: основной intent-путь обрабатывает rename, назначение владельца через свободный текст — узкий сценарий, покрыт degraded-fallback'ом | ТЗ [`2026-06-27-probe-clarify-dialog-tz.md`](../../plans/tz/2026-06-27-probe-clarify-dialog-tz.md) Ф1 | low / vNext (владелец/разработчик) |
| 2026-06-27 | **Бэкфилл `Decision.impliesAction` для существующих решений** — новое поле имеет дефолт `false`, поэтому все ранее извлечённые решения по умолчанию «не подразумевают действия» и выпадают из дашборд-метрик stalled/throughput до ре-экстракции | Корректное значение знает только LLM (`decision-extract`) — нужен ре-прогон извлечения по историческим `Decision`/их блокам; на почти-пустом проде (~единицы решений) эффект минимален, бэкфилл вне scope ТЗ Ф4 | ТЗ [`2026-06-27-task-decision-execution-unified-tz.md`](../../plans/tz/2026-06-27-task-decision-execution-unified-tz.md) Ф2/Ф4 · [[../02_architecture/data-model]] §«impliesAction» | low / vNext (LLM-ре-экстракция; разблокирует владелец) |
| 2026-06-27 | **Ф6 vNext — структурный авто-статус задачи (commit / PR `Fixes #`)** — авто-перевод задачи в done по техническим артефактам (мердж PR / коммит с `Fixes #N`), а не только по разговорному сигналу закрытия | Требует интеграции с git-хостингом (webhook'и GitHub/GitLab/Битрикс) и маппинга артефакт→`Issue` — отдельная фича вне контура «задача·решение·исполнение» Ф0–5; в ТЗ оставлена заглушкой | ТЗ [`2026-06-27-task-decision-execution-unified-tz.md`](../../plans/tz/2026-06-27-task-decision-execution-unified-tz.md) §Ф6 (заглушка) | vNext (отдельное ТЗ интеграции git-хостинга; владелец) |
| 2026-06-26 | **vNext ремонта дашбордов** — (а) day-scoped «Польза» (нужен `day` в director-enum, сейчас today→week); (б) UI outcome-capture для метрики «Доведение решений %» (сейчас окно+пустой-стейт чинятся, метрика остаётся); (в) просмотр содержания чужого чек-ина новым эндпоинтом (сейчас ссылка на `/persons/[id]/pulse`); (г) backfill `personGoalContribution` для исторических целей (крон считает вперёд) | Явный «Не входит» ТЗ: каждый пункт — отдельная фича/эндпоинт/enum-изменение, выходит за «чиним-переставляем»; владелец заморозил состав дашбордов (Р1) | ТЗ [`2026-06-25-dashboards-repair.md`](../../plans/tz/2026-06-25-dashboards-repair.md) §Scope «Не входит» | vNext (по запросу владельца — отдельные ТЗ) |
| 2026-06-26 | **R24 — комбинированная сводка «Пока спокойно» собирается только по сигналам, известным родителю** (конфликты/трения/низкая отдача в `OperationsDashboardClient`); счётчик «Хронических блокеров» не входит в сводку, т.к. `ChronicBlockersWidget` — self-fetch под-виджет (родитель не знает его число) | Архитектурное ограничение: чтобы включить блокеры в единую сводку, нужно поднять их данные в родителя (рефактор гейта `reworkEnabled` + лифт SWR) — не оправдано без визуальной приёмки на проде | `OperationsDashboardClient.tsx` (карточка «Свежие конфликты») · ТЗ [`2026-06-25-dashboards-repair.md`](../../plans/tz/2026-06-25-dashboards-repair.md) R24 | minor / vNext (лифт данных ChronicBlockers при запросе владельца) |
| 2026-06-25 | **Итеративный многошаговый поиск (route → plan → step-retrieval → sufficiency) изымается из chat-v2** — при упрощении ассистента вторая скрытая петля удаляется из горячего пути чата; метод законсервирован для будущей фичи «Большой анализ / отчёт» (тяжёлые агрегации по многим эпизодам, не влезающие в один контекст) | Решение владельца (2026-06-25): в чате петля дублирует «понимание вопроса» + флаг `aggregation` + честность синтеза; но для гигантских сводов staged-поиск нужен — не выбрасываем, консервируем | Консервация метода [`2026-06-25-iterative-rag-method-parked.md`](../../plans/analysis/2026-06-25-iterative-rag-method-parked.md) · решение [`2026-06-25-telegram-concierge-classifier-arch.md`](../../plans/analysis/2026-06-25-telegram-concierge-classifier-arch.md) §14 | **parked / vNext** (воскресить при фиче «Большой анализ» как отдельный сервис/воркер вне chat-v2) |
| 2026-06-28 | **`MeetingChatPanel` ещё на legacy `chatApi`** — чат комнаты встречи не мигрирован на Мастер scoped: нужны `speakerName` (подпись спикера), `onSeek` (прыжок к таймкоду в плеере), история сообщений поверх `MasterScopedChat` (scope:meeting) | Откол от КФ-конвергенции: панель встречи несёт спец-фичи (seek/спикеры/плеер), требует расширения `MasterScopedChat` под scope:meeting + история; вынесено в под-ТЗ, чтобы не раздувать конвергенцию | ТЗ [`2026-06-28-meeting-chat-on-master-scoped.md`](../../plans/tz/2026-06-28-meeting-chat-on-master-scoped.md) | ready-to-implement (отдельное ТЗ; разблокирует владелец / разработчик) |
| 2026-06-28 | **pin/archive/feedback в доме Мастера (`MasterChatHome`)** — паритет управления диалогами с прежним `ChatV2Client`: закрепить диалог, архивировать, 👍/👎-фидбек на ответах — в новом доме пока нет (только список+история+стрим) | Откол от КФ-конвергенции: управление диалогами требует своих эндпоинтов/полей (`ConciergeConversation` pin/archive) и UI; в первой итерации дом закрыл список+историю+действия, parity-функции вынесены | ТЗ [`2026-06-28-concierge-conversation-management-parity.md`](../../plans/tz/2026-06-28-concierge-conversation-management-parity.md) | ready-to-implement (отдельное ТЗ; разблокирует владелец / разработчик) |
| 2026-06-28 | **Визуальная приёмка chat-surface на проде — pending** — конвергенция КФ1–КФ5 закрыта в коде (typecheck/lint/build/vitest зелёные back+front), но дом Мастера `/chat`, scoped-чаты (`IssueChat`/`MobileAskClient`), двери Р1/Р3 и FAB-навигация **не приняты глазами** на korateam.ru | У агента-реализатора нет локального запуска UI → нужна приёмка через Playwright/прод-кабинет (`qa-tester`); отколотая «под глаза» фаза легко выпадает из горячего пути | ТЗ [`2026-06-25-edinyy-pomoshnik-chat-surface-convergence.md`](../../plans/tz/2026-06-25-edinyy-pomoshnik-chat-surface-convergence.md) (КФ1–КФ5, коммиты `7d111a94`..`c5b7ec10`) | **ready-to-QA** (визуальная приёмка `qa-tester`; разблокирует владелец / визуальная приёмка) |
| 2026-06-24 | **Ф10 — живой re-test на проде обоих ТЗ (граф знаний v2 + умный поиск)** — код Ф1–Ф9 (граф) и Ф4.1–4.7 (поиск) реализован, покрыт юнитами, собран (typecheck/build/lint зелёные); но 5-пунктовая прод-приёмка НЕ проведена: «суть встречи X» → связная суть+кликабельный источник; человек из встречи+чата = один профиль; блок достижим через ребро; нет `sourceMeetingId=null` у новых блоков; superseded скрыт. Плюс прогон rag-промптов на реальных LLM (кейс «Александр» без переспроса, «нет данных» → честный отказ) | Требует прод-доступа (diag.ts/qa-tester к meet.crossmark.ru/korateam.ru), который владелец подтверждает в каждой сессии явно [[feedback_prod_diagnostic_access_requires_confirmation]]; bi-temporal ON меняет выдачу — наблюдать на проде | ТЗ [`2026-06-23-knowledge-graph-ingestion-rebuild.md`](../../plans/tz/2026-06-23-knowledge-graph-ingestion-rebuild.md) Ф10 + [`2026-06-23-master-smart-search-phase4.md`](../../plans/tz/2026-06-23-master-smart-search-phase4.md) DoD | **needs-prod-access** (после `docker compose up -d --build` + `apply-prod-deploy.ts` — владелец даёт «да» на diag/qa, затем 5 пунктов + 2 кейса) |
| 2026-06-23 | **Умный подбор «кому поручить» (по зоне ответственности) не подключён к встречам и owner-вопросам** — `SkillRoutingService` (смысловой подбор по навыкам+ответственности) работает только при ручном создании задачи в UI; авто-постановка из встреч (`AssigneeResolverService`) и owner-вопросы probe (`OwnerResolver`) подбирают по имени, не по компетенции → вопросы «кто ответственный» идут к человеку | Идея владельца («за что отвечает сотрудник → использовать при назначении»); инфраструктура (ResponsibilityElement/SkillProfile/RoleProfile) готова, надо доподключить | ТЗ [`2026-06-23-skill-routing-connect-meetings-and-owner.md`](../../plans/tz/2026-06-23-skill-routing-connect-meetings-and-owner.md) | **needs-code-study-first** — задачный код пишется в параллельной сессии; перед реализацией изучить актуальный код + сверить с задачными ТЗ + переформулировать (требование владельца) |
| 2026-06-23 | **Subject Memory (самообучение на ответах probe) — нет наблюдаемости + не чистит висящие дубли** — фича выкачена и включена (флаги ON), но выученные правила негде посмотреть (нет UI/диага/read-эндпоинта; метрики per-replica → `/metrics` нули; лог-стор воркер не покрывает), а `retrieve-before-ask` подавляет только новые probe — накопленные дубли (в тесте 3× про один «Дамп») остаются висеть | Вскрыто живым тестом с владельцем: ответ на probe принят (10→9), но подтвердить вывод правила нельзя, дубли той же темы не ушли → «сделали, включили, но не видно, работает ли» | ТЗ [`2026-06-23-subject-memory-observability-and-dedup.md`](../../plans/tz/2026-06-23-subject-memory-observability-and-dedup.md) · анализ [`2026-06-23-manual-confirmations-autonomy-review-and-probe.md`](../../plans/analysis/2026-06-23-manual-confirmations-autonomy-review-and-probe.md) §6.2 | ready-to-implement (наблюдаемость Ф1 — приоритет, без неё тест обучения вслепую) |
| 2026-06-22 | **Регламенты/инструкции/политики/процессы не привязаны к клону должности** — карточки `Regulation`/`Instruction`/`Policy`/`Process` (`scope='role:<id>'`, `embedding`, `severity` у `Policy`) живут отдельной подсистемой; сборка клона `buildForRole` и ответ `callCloneRespond` их не читают → клон-советчик отвечает только из неявного опыта носителей, записанные правила игнорирует; проактивная подсказка/проверяльщик пока невозможны | Дыра вскрыта при разборе с владельцем; решение — «Способ C» (указатель правил в снапшоте клона + retrieval по запросу + приоритет `blocking`-политик над опытом); проверяльщик — отдельный этап поверх, с предусловием «починить доставку» (тихий пропуск Telegram + quiet_hours) | ТЗ [`2026-06-22-clone-regulation-grounding-method-c.md`](../../plans/tz/2026-06-22-clone-regulation-grounding-method-c.md) · аналитика-задел [`2026-06-22-regulation-checker-proactive-and-postfact.md`](../../plans/analysis/2026-06-22-regulation-checker-proactive-and-postfact.md) | ✅ **РЕАЛИЗОВАНО** (Способ C, Фазы 1–3; ветка `feature/2026-06-22-clone-regulation-grounding-method-c`, 3 коммита `feat(clones)`; `RoleRegulationRetrievalService` + блоки `<applicable_regulations>`/`<regulations_index>` + миграция аддитивная + 4 крутилки). **Ждёт прод-выката** (`docker compose up -d --build` — миграция авто + сид агрегатором; см. `prod-deploy-log.md` 2026-06-23). После выката — снять. Проверяльщик — vNext по «начни реализацию» владельца |
| 2026-06-23 | **`scope` карточек знаний — строка, не FK на `Role`** — `Regulation`/`Instruction`/`Policy`/`Process`.`scope` хранится строкой `role:<id>`/`org`/`department:<id>` и матчится точным сравнением строки (helpers `parseRoleScope`/`buildRoleScopeFilter` в `role-scope.util.ts`); полноценного внешнего ключа на `Role`/`Department` нет | Способ C (Р2): точный строковый фильтр закрывает 100% задачи retrieval; миграция модели `scope`→FK рискованнее и не нужна для ценности сейчас | ТЗ [`2026-06-22-clone-regulation-grounding-method-c.md`](../../plans/tz/2026-06-22-clone-regulation-grounding-method-c.md) §Нецели + Р2 | vNext (нормализация `scope`→FK при запросе владельца) |
| 2026-06-22 | **Задачная подсистема — 15 находок** (вкл. F1 404 вместо «создать+спросить»; F3 quality-gate теряет задачу встречи; F5 задачи без срока невидимы сводке; F6 просрочка не доходит до исполнителя; F7 нет вечерней сверки «не сделал»; F8 отдел/роль не отличается; F10 две несвязанные системы Issue/Task; F11 ответ probe не исполняется; F12 решение↔задача разомкнуто; F13 дашборд «почему/затык»; F14 кроны по UTC). F2/F9 уже выкачены | Готовая «система обучения» `SubjectMemory` к назначению не подведена; probe не имеет `task.*`-reason; резолвер не отличает отдел; напоминания завязаны на `dueDate` и Telegram | Анализ [`2026-06-22-tasks-lifecycle-deep-audit.md`](../../plans/analysis/2026-06-22-tasks-lifecycle-deep-audit.md) + единое ТЗ [`2026-06-22-tasks-subsystem-unified-fix.md`](../../plans/tz/2026-06-22-tasks-subsystem-unified-fix.md) | ✅ **РЕАЛИЗОВАНО** (блоки A→C→B→D, 16 фаз; ветка `feature/2026-06-22-qa-and-tasks-subsystem`, 25 коммитов; миграций/ENV нет). **Ждёт прод-выката** (`docker compose up -d --build` + сиды агрегатором; см. `prod-deploy-log.md`). После выката — снять. Хвосты vNext — строки ниже |
| 2026-06-22 | **#13 неизвлечение задач из встречи** — на ряде встреч action items не извлекаются | Это **диагностика данных**, не код-фикс (вне scope qa-feedback-batch): нужен разбор конкретных встреч (transcript/segment/extractor), а не правка кода | QA-пачка [`2026-06-22-qa-feedback-batch.md`](../../plans/tz/2026-06-22-qa-feedback-batch.md) §Scope (вынесено) | needs-diagnostic (разработчик/qa — разобрать данные конкретных встреч) |
| 2026-06-22 | **3 вынесенные фичи задачной/встреч-области — ТЗ есть, не реализованы:** единые клиенты, единый календарь, real-time чат встречи «на руках» | Каждая фича — самостоятельный объём, выносилась из текущих пачек, чтобы ТЗ не разрастались | ТЗ [`2026-06-22-unified-clients.md`](../../plans/tz/2026-06-22-unified-clients.md) · [`2026-06-22-unified-calendar.md`](../../plans/tz/2026-06-22-unified-calendar.md) · [`2026-06-22-meeting-realtime-chat-hand.md`](../../plans/tz/2026-06-22-meeting-realtime-chat-hand.md) | ready-to-implement (по «начни реализацию», с подтверждения владельца) |
| 2026-06-22 | **B2 — полная унификация `Task`/`Issue` отложена** — переписочные задачи (`Task`) видны в триаже `/intake` через read-union in-memory; `Task` остаётся пред-слоем (без дубль-записи, без единого хранилища) | Read-union «не терять» дешевле и безопаснее дубль-записи; единое хранилище `Task`↔`Issue` — отдельный объём (миграция + переезд данных + промоут) | ТЗ [`2026-06-22-tasks-subsystem-unified-fix.md`](../../plans/tz/2026-06-22-tasks-subsystem-unified-fix.md) Блок B2 | vNext (владелец/разработчик) |
| 2026-06-22 | **B1 — выделенная колонка `IntakeIssue.lowQuality` не вводилась** — гейт качества задачи встречи помечает низкое качество через `suggestedPriority='low'`, отдельного булева поля нет | Чтобы не вводить миграцию ради одного флага: `suggestedPriority='low'` несёт ту же семантику; выделенное поле + индекс/фильтр — отдельный слой | ТЗ [`2026-06-22-tasks-subsystem-unified-fix.md`](../../plans/tz/2026-06-22-tasks-subsystem-unified-fix.md) Блок B1 | vNext (миграция `IntakeIssue.lowQuality` при запросе) |
| 2026-06-22 | **D4 — час кронов как AdminSetting-крутилка не введён** — юзер-facing суточные кроны (issue-overdue/feed-digest/chatbox) переведены на `timeZone='Europe/Moscow'`; полный перевод всех кронов на ежечасный-тик + крутилка-часа отложен | Фикс «по UTC» закрыт фиксированной МСК-таймзоной (достаточно для текущего сценария); параметризуемый час требует переписи кронов на hourly-tick + per-tenant час — отдельный объём | ТЗ [`2026-06-22-tasks-subsystem-unified-fix.md`](../../plans/tz/2026-06-22-tasks-subsystem-unified-fix.md) Блок D4 | vNext (hourly-tick + крутилка-часа при запросе) |
| 2026-06-23 | **Дроп legacy-модели `Task` + переписка 15+ `prisma.task.*` читателей** — unified-task-extraction Ф4 оставил `Task` как явный пред-слой «AI-кандидат» (промоут Task→Issue однонаправленный, провенанс `TaskSource{issueId}`); единое хранилище без `Task` отложено | Р-1: дроп = миграция + переезд данных + переписка всех `prisma.task.*` читателей (`me/tasks`, триаж, дашборды) — самостоятельный объём, рискованный для прода | ТЗ [`2026-06-23-unified-task-extraction.md`](../../plans/tz/2026-06-23-unified-task-extraction.md) §12 vNext (а) | vNext (отдельное ТЗ; владелец/разработчик) |
| 2026-06-23 | **Полное слияние `meeting-extract-actions` в спайн-специалист `3-15-tasks`** — встречи по-прежнему извлекают задачи отдельным путём (`meeting-extract-actions`, резолв по identity участников), спайн покрывает только НЕ-meeting-каналы (`action_item`) | Б-1: встречи имеют ограниченный набор участников → identity-резолв точнее tenant-wide fuzzy; слияние требует переноса participant-резолва на спайн без регресса — отдельный объём | ТЗ [`2026-06-23-unified-task-extraction.md`](../../plans/tz/2026-06-23-unified-task-extraction.md) §12 vNext (б) + §6 граничный контракт | vNext (владелец/разработчик) |
| 2026-06-23 | **Кадэнс sweep-крона анализа чата фиксирован 10 мин (`sweepIntervalMin` не введён)** — `ChatboxAnalyzeCron.sweep` зашит на `EVERY_10_MINUTES`; параметризуемый интервал-крутилка не добавлен | В кодовой базе нет механизма динамического крона; defined-but-unused крутилка = анти-паттерн Ship-On (склад выключенного). Фиксированные 10 мин покрывают требование «10–15 мин» | ТЗ [`2026-06-23-unified-task-extraction.md`](../../plans/tz/2026-06-23-unified-task-extraction.md) §12 отклонения + ТЗ [`2026-06-23-chatbox-analysis-no-stuck-sessions.md`](../../plans/tz/2026-06-23-chatbox-analysis-no-stuck-sessions.md) | vNext (нужен механизм динамического крона; владелец/разработчик) |
| 2026-06-23 | **2 LOW-остатка дедупа задач** — (а) ключ advisory-lock и нормализация заголовка не схлопывают `ё/е` одинаково (рассинхрон нормализации lock-key); (б) flip вердикта дедупа на ретрае (LLM вернул другой ответ) может создать и link, и IntakeIssue | LOW: оба узких (редкие заголовки с ё; недетерминизм LLM на грани серой зоны); закрытие требует единой нормализации ключа + строгой идемпотентности вердикта — отдельный точечный проход | ТЗ [`2026-06-23-unified-task-extraction.md`](../../plans/tz/2026-06-23-unified-task-extraction.md) §9 риски / §12 vNext (г) | low / next-wave (владелец/разработчик) |
| 2026-06-23 | **DROP deprecated-колонок `ChatboxCustomer.linkedPersonId` / `ChatboxChannelClient.linkedPersonId`** — после перевода клиентов на `Customer` колонки больше не пишутся, но физически остаются | Б4: миграция-схемы прогоняется ДО backfill (`migrate deploy` в `--with-schema`), а backfill читает `linkedPersonId` — дроп в том же релизе сломал бы перенос. Drop безопасен только после подтверждения backfill на проде | ТЗ [`2026-06-23-chatbox-customer-vs-manager-split.md`](../../plans/tz/2026-06-23-chatbox-customer-vs-manager-split.md) Б4 / Scope «Не входит» | vNext (отдельная миграция после прод-выката + прогона backfill; владелец/разработчик) |
| 2026-06-24 | **Пофименная атрибуция сообщений группового чата в knowledge-граф/задачи** — `ingestSession` приписывает всю сессию одному customer + одному responsible; для групп реплики разных участников атрибутируются одному лицу (идентификация/UI/контакты групп — уже сделаны) | Решение владельца «фазами» (В2): основная ценность доставлена, граф-атрибуция по отправителю — отдельный объём, переписывает критичный ingest | vNext-заглушка [`2026-06-24-chatbox-group-graph-attribution.md`](../../plans/tz/2026-06-24-chatbox-group-graph-attribution.md) | vNext (по «реализуй»; предусловие — выкат групп на прод) |
| 2026-06-23 | **Унификация Bitrix-CRM на `Customer`** — `BitrixContact/Company/Deal/Lead` остаются сырыми зеркалами, не привязаны к `Customer`/`Entity{customer}` | Вынесено из scope; модель `Customer` заложена с `source`/`externalCrmId` под переиспользование, но переключение Bitrix — отдельный объём | ТЗ [`2026-06-23-chatbox-customer-vs-manager-split.md`](../../plans/tz/2026-06-23-chatbox-customer-vs-manager-split.md) Scope «Не входит» | vNext (отдельное ТЗ Bitrix→Customer; владелец/разработчик) |
| 2026-06-23 | **Карточка клиента `/customers/:id` + write API (`Customer` create/update/delete)** — раздел «Клиенты» read-only (list+get), детальной страницы и редактирования нет (как у Vendors на старте) | Достаточно для запрошенного «отображения клиентов»; карточка/жизненный цикл — отдельный слой по аналогии с будущим Vendor-management | ТЗ [`2026-06-23-chatbox-customer-vs-manager-split.md`](../../plans/tz/2026-06-23-chatbox-customer-vs-manager-split.md) Б8 / Фаза 5Б | vNext (по запросу владельца) |
| 2026-06-23 | **Двунаправленный дедуп «одного артефакта», когда задача из чата опережает Issue со встречи** — спайн покрывает порядок «встреча первой» (Issue существует → чат линкуется); обратный порядок (чат создал Issue, потом пришла встреча с тем же поручением) дедупится по общему пути, но строгий «один артефакт при любом порядке» не гарантирован | Текущий сценарий покрыт (meeting-first ordering); полная симметрия требует доп. сверки при материализации задачи встречи против уже созданных спайн-Issue — отдельный объём | ТЗ [`2026-06-23-unified-task-extraction.md`](../../plans/tz/2026-06-23-unified-task-extraction.md) §12 vNext (д) | vNext (владелец/разработчик) |
| 2026-06-22 | **Пред-существующий долг: 4 падающих фронт-теста `tracker/Board.spec.tsx`/`OrgBoard.spec.tsx`** — фикстуры без `sourceBlockIds` (`IssueCard.tsx:186`) | Не наши изменения — красные уже на `origin/dev` до этой работы; чинить = трогать чужой код вне scope этих двух ТЗ | `frontend/src/ui/tracker/{Board,OrgBoard}.spec.tsx`; `IssueCard.tsx:186` | needs-fix (владелец чужой зоны / отдельная задача по фикстурам) |
| 2026-06-22 | **`learned-memory-edit-distance-signal`** — выученная память уточнений учится только на correct-исходе; сигнал «правка/edit-distance» (человек поправил предложенную формулировку → корректирует/опускает правило) не используется как обучающий сигнал | vNext в исходном ТЗ: Слой 3 в v1 копит confirm/refute от ответов на probe; разбор дельты правки требует отдельного захвата исходного-vs-исправленного текста | ТЗ [`2026-06-21-learned-clarifications-memory.md`](../../plans/tz/2026-06-21-learned-clarifications-memory.md) §vNext | vNext (владелец/разработчик) |
| 2026-06-22 | **`learned-memory-into-answer-prompts`** — выученные правила (термин/дизамбигуация/предпочтение) подмешиваются только в `probe-formulate`; в chat-v2 / ответы ассистента они НЕ подмешиваются (ответ компании не использует выученную дизамбигуацию терминов) | vNext в исходном ТЗ: v1 ограничен retrieve-before-ask для probe; интеграция в answer-промпты — отдельный слой (retrieval + место в SYSTEM/USER, cache-friendly) | ТЗ [`2026-06-21-learned-clarifications-memory.md`](../../plans/tz/2026-06-21-learned-clarifications-memory.md) §vNext | vNext (владелец/разработчик) |
| 2026-06-22 | **`learned-memory-admin-ui`** — нет UI для просмотра/правки выученных правил (`SubjectMemory`): статусы shadow/canary/active, правка `ruleText`, ручной rollback/disable доступны только через generic admin/БД | vNext в исходном ТЗ: v1 — авто-активация за judge-ансамблем + авто-rollback canary (no-human-in-loop); выделенная админ-поверхность — отдельный слой | ТЗ [`2026-06-21-learned-clarifications-memory.md`](../../plans/tz/2026-06-21-learned-clarifications-memory.md) §vNext | vNext (владелец) |
| 2026-06-22 | **`department-profile-autobuild`** — авто-профиль собирается для компании (Слой 1) и роли (`RoleProfile.summaryCache`), но НЕ для отдела: маршрутизация задач по скиллам использует role-prior, отдельного авто-summary отдела нет | vNext в исходных ТЗ: Слой 1 покрыл уровень компании, Слой 2 — role-prior; отдел как промежуточный уровень — отдельная сборка (по образцу `CompanySummaryCompilerCron`) | ТЗ [`2026-06-21-company-profile-autobuild-and-prompt-context.md`](../../plans/tz/2026-06-21-company-profile-autobuild-and-prompt-context.md) §vNext + [`2026-06-21-skill-based-task-routing.md`](../../plans/tz/2026-06-21-skill-based-task-routing.md) §vNext | vNext (владелец/разработчик) |
| 2026-06-22 | **`company-summary-provenance-ui`** — авто-summary «Чем занимается компания» подмешивается в чат/concierge, но в ответе нет «откуда это» (источники топ-IdeaBlock, на которых собран summary, не кликабельны/не показаны) | vNext в исходном ТЗ: v1 строит summary из топ canonical-IdeaBlock, но провенанс-вывод в UI чата — отдельный слой (по образцу ProvenanceDrawer) | ТЗ [`2026-06-21-company-profile-autobuild-and-prompt-context.md`](../../plans/tz/2026-06-21-company-profile-autobuild-and-prompt-context.md) §vNext | vNext (владелец/разработчик) |
| 2026-06-22 | **`task-skill-tags`** — у задач нет явных тегов навыка: маршрутизация по скиллам выводит совпадение из текста задачи (semantic) + skill-профили людей, но ручных skill-тегов на `Issue` (для точного hard-gate/фильтра) нет | vNext в исходном ТЗ: Слой 2 в v1 опирается на семантику + departmentId hard-gate; явные skill-теги задачи — отдельная сущность/поле + UI | ТЗ [`2026-06-21-skill-based-task-routing.md`](../../plans/tz/2026-06-21-skill-based-task-routing.md) §vNext | vNext (владелец/разработчик) |
| 2026-06-22 | **`clone-signaltype-methodology-step`** — РЕАЛИЗОВАНО (`ff1d09db..890fd7c5`, ветка→dev): `methodology_step` добавлен в набор-потребитель клона через единую константу `SKILL_SUBJECT_SIGNAL_TYPES` (4 рубежа + 3 находки сверх ТЗ: probe×2, practice-skill-extractor, +clone-build-harness) + backfill-скрипт + 11 юнит-тестов. **Остаётся открытым:** (а) прод-выкат+backfill+форс `SkillTraitVerifyCron.tick`+прод-acceptance (за владельцем); (б) vNext: `process_step`/`expertise`/`competence` в наборе + правка прозаического claim «только reasoning» в `skill-trait-detect.prompt.ts`/`process-marker-detect`/`value-motivation-detect` (prompt-cache, при след. ревизии промптов) | Вскрыто боевым прод-тестом 2026-06-22; код-фикс закрыл рассинхрон набора (литерал в ≥8 местах → одна константа); прод-данные «ооо ромашка» трогаются только с подтверждения владельца | ТЗ [`2026-06-22-clone-signaltype-methodology-step.md`](../../plans/tz/2026-06-22-clone-signaltype-methodology-step.md) · анализ [`2026-06-22-clone-module-prod-test/RESULTS.md`](../../plans/analysis/2026-06-22-clone-module-prod-test/RESULTS.md) · prod-deploy-log Шаг 8 | needs-deploy (backfill + verify-cron + acceptance на Org `cmpuz4gbs000201mvfbf3k2zk`); vNext за владельцем |
| 2026-06-20 | **Умные таблицы мертвы на проде + авто-наполнение 4/10 + промпты 12b не выкачены** — (1) деталка `/tables/[id]` крашится React #185, фикс `26219233` НЕ выкачен (прод на старом билде, заодно нет R1–R4); (2) в `nav-config.ts` нет пункта меню «Таблицы» (осиротевший импорт `Table2`); (3) из 10 шаблонов авто-наполняются 4, и даже у них все прод-строки ручные (`entityId:null`, regulations=0 строк); (4) промпты 12b лучше живых (A/B 7/8 vs 5/8) но не в коде; (5) латентность агента 41–47с | Живой прод-аудит (Playwright + API + A/B `deepseek-v4-pro`); агенты работают и качество хорошее, но операционно недоступны и наполовину без источника данных | анализ [`2026-06-20-smart-tables-live-audit/`](../../plans/analysis/2026-06-20-smart-tables-live-audit/README.md) (приоритеты P0–P2 + дизайн моста граф→6 «мягких» с гейтом уверенности) | P0 редеплой + вернуть пункт меню (без решений владельца); P1 выкатить 12b + порезать латентность; P2 мост авто-наполнения — нужно отдельное ТЗ |
| 2026-06-20 | **Модуль регламентов: фича реализована, ждёт прод-прогона backfill + проверки на «Ооо луа»** — гейт чья-норма/существенность, fail-open арбитр, дедуп инструкций, ProcessTemplate каноничен, крон-консолидатор (всё в коде, ветка `feature/regulations-process-fix`); накопленные 121 дубль схлопнёт только прогон `backfill-regulation-consolidate.ts` + тик крона на проде | Консолидатор и backfill идемпотентны, но прод-данные «Ооо луа» (`cmpndk2tw000101mwmixvacuj`) трогаются только с подтверждения владельца; смоук «число Process сократилось» возможен лишь на проде | ТЗ `plans/tz/2026-06-20-regulations-process-module-fix.md` + `docs/operations/prod-deploy-log.md` (Шаг 8) | needs-deploy (прогон backfill + наблюдение метрик not_our_org/not_keepable/dedupe_fallback_new) |
| 2026-06-20 | **Провенанс «кнопка к первоисточнику» — ВТОРАЯ ВОЛНА отложена из v1**: (а) deep-link к конкретному сообщению чата (`ChatboxMessage.externalId`/`messageId` не прокинут в `IdeaBlockEvidence`, таймкоды чата синтетические `startSec:i`); (б) якорь к странице документа (парсер уплощает в один blob, нет page/bbox в духе RAGFlow + превью страницы в S3); (в) сохранение аудио голосовых `free_note` в S3 (сейчас аудио ВЫБРАСЫВАЕТСЯ после ASR — оригинал невосстановим) | Решение владельца Р-1: v1 покрывает встречи-карточки + AI-чат + probe (данные богаче, последняя миля `?t=` почти готова); чат/документ/голос требуют новых полей в `IdeaBlockEvidence` + page-aware парсинг + аудио-хранилище — отдельная волна, иначе релиз растягивается | анализ [`2026-06-20-provenance-source-traceability/`](../../plans/analysis/2026-06-20-provenance-source-traceability/) (99-synthesis §В-1/В-4) + будущее ТЗ [`2026-06-20-provenance-source-traceability-tz.md`](../../plans/tz/2026-06-20-provenance-source-traceability-tz.md) раздел «Не входит в v1» | vNext после v1 (vNext-ТЗ `provenance-source-traceability-wave2`) |
| 2026-06-20 | **Провенанс v1 + умный probe — РЕАЛИЗОВАНЫ и в dev (9 коммитов `fab510be`..`841ba213`), остаются хвосты**: (а) `chat-v2.loadContextBlocks` НЕ переведён на `ProvenanceService` — оставлен как эталон последней мили (acceptance «≥2 из 3 дублей» выполнен на tables+regulations); (б) `provenancePreview` в `domain/decision`/`tracker/issue` = `null` (backend list-DTO решений/задач не отдаёт `previewQuote`/`previewSourceRef`); (в) эмиттер `specialist-3-4` (card) не получил чистый `objectName` (не в scope ТЗ); (г) живой LLM-бенч `probe-module-bench.ts` и Playwright/qa провенанса не прогнаны (песочница без сети — api.deepseek.com/proxy.agent-lia.ru → 000) | Хвосты вне acceptance ТЗ; промпты выкатываются ship-and-observe ([[../05_история/2026-06-20-provenance-and-probe-smart-questions]]) | **ВСЁ доделываемое собрано в ТЗ** [`2026-06-20-provenance-probe-followups`](../../plans/tz/2026-06-20-provenance-probe-followups.md) (+orchestrator-prompt): Блок A (A1 preview в списках · A2 chat-v2→сервис · A3 card objectName · A4 email-ingest) + Блок B (B1 chatbox deep-link · B2 fast-task block-link · B3 аудио S3 · B4 документ-якорь · B5 phone_call), REALITY-CHECK по якорям + решения владельца Р-1…Р-4. Исходники: ТЗ [`provenance`](../../plans/tz/2026-06-20-provenance-source-traceability-tz.md) + [`probe-smart-questions`](../../plans/tz/2026-06-20-probe-smart-questions-module.md) | готово к реализации другим агентом (Блок A — без решений владельца; Блок B — после Р-1/Р-3) |
| 2026-06-20 | **Вскрытые ingest-пробелы (не в scope v1, мешают deep-link к не-встреча источникам)**: `phone_call` — мёртвый enum без адаптера (источник не наполняется); email-ingest баг класса «не тот ключ» (тело под `payload.text`, а `SegmentBuilder` ждёт `fullText` → письмо `JSON.stringify`-ится одним зашумлённым сегментом, цитата ненадёжна); chatbox — данные для deep-link к сообщению есть в БД, но `externalId` не прокинут в turn/evidence | Вскрыто при research провенанса; не в scope v1 (встречи), но нужны для второй волны и для качества графа из чата/почты | анализ [`2026-06-20-provenance-source-traceability/10-internal-baseline.md`](../../plans/analysis/2026-06-20-provenance-source-traceability/10-internal-baseline.md) §2 (verified: `segment-builder.service.ts:189` vs `email-fetch.service.ts:130`; `chatbox-ingest.service.ts:290`) | vNext (часть второй волны провенанса) |
| 2026-06-20 | **Перенос ~240 редких ENV-крутилок + 60-90 хардкод-констант в AdminSetting (Шаги 9–10) отложен**; реализация ТЗ (гейт Шаги 2–4 + операционный перенос 5–8) ещё не начата (написано Шаг 1 — правило) | Решение владельца: «глубина = гейт + операционные крутилки сейчас, остаток по востребованию»; гейт всё равно держит инвариант от роста свалки | ТЗ [`2026-06-20-config-knobs-to-admin-settings.md`](../../plans/tz/2026-06-20-config-knobs-to-admin-settings.md) (Шаги 9–10 = по востребованию); анализ [`2026-06-20-config-env-vs-admin-settings-audit.md`](../../plans/analysis/2026-06-20-config-env-vs-admin-settings-audit.md) | Шаги 2–8 — по «начни реализацию»; 9–10 — по востребованию |
| 2026-06-21 | **UI-поле для крутилок `daySignals.*`** (выделенная admin-страница операций/чек-инов) — `daySignals.enabled`/`detectThreshold`/`processLocalHour` зарегистрированы в схеме и сидятся, но отдельного поля в админке нет | Вне scope ТЗ Ф8: фича работает на code-fallback (Ship-On), крутилки пока редактируются через generic admin-settings API; выделенный UI — отдельный слой | ТЗ [`2026-06-21-universal-daily-checkin-fixator-tz.md`](../../plans/tz/2026-06-21-universal-daily-checkin-fixator-tz.md) Ф8 | vNext (владелец при запросе) |
| 2026-06-20 | **Трекер: карточка+датированный прогресс+паритет — ТЗ написано, реализация не начата; часть отложена в vNext** — ТЗ на 3 волны (лицо карточки · сущность «обновление прогресса» с авто-черновиком из графа · кастом-поля/автоматизации/повторение/worklog). Из scope отложено: прогресс в Telegram-помощнике, критический путь/авто-сдвиг сроков на Ганте, worklog→деньги/payroll | Развилки согласованы владельцем 2026-06-20 (Р1 компактная карточка+опция, Р2 отдельная сущность+авто-черновик, Р3 порядок волн); реализацию ждёт явного «начни реализацию» (правило [[feedback_tz_means_tz_not_code]]) | ТЗ [`2026-06-20-tracker-card-redesign-and-progress.md`](../../plans/tz/2026-06-20-tracker-card-redesign-and-progress.md) + анализ [`2026-06-20-tracker-card-anatomy-and-board-ux.md`](../../plans/analysis/2026-06-20-tracker-card-anatomy-and-board-ux.md) | готово к реализации (по «погнали Волну 1»); vNext-хвосты — отдельными ТЗ |
| 2026-06-18 | **Богатый «Календарь» в рабочем столе задач не реализован** (встречи + дедлайны + спринты + подсказки Коры); вкладка не рендерится | Завязан на подсистему встреч + LLM-подсказки — отдельная фича, иначе ТЗ разрастается и застревает | парное ТЗ [`2026-06-18-tasks-workspace-calendar.md`](../../plans/tz/2026-06-18-tasks-workspace-calendar.md) (draft) | отдельным ТЗ (с подтверждения владельца) |
| 2026-06-18 | **Легаси `/tasks` («задачи из встреч», `tasksApi`) — orphaned**: меню «Задачи» ведёт на `/projects` (рабочий стол), на `/tasks` ничего не ссылается | Роль поглощена рабочим столом задач (ТЗ tasks-unified-workspace); чистка вне scope | `frontend/app/(authenticated)/tasks/TasksClient.tsx`; ТЗ [`2026-06-18-tasks-unified-workspace-phase1.md`](../../plans/tz/2026-06-18-tasks-unified-workspace-phase1.md) | **внесено в ТЗ** [`tracker-card-redesign-and-progress`](../../plans/tz/2026-06-20-tracker-card-redesign-and-progress.md) Волна 0 (Ф0.2 — редирект `/tasks`→`/projects`) |
| 2026-06-18 | **Дублирование DnD-каркаса `OrgBoard` ↔ `Board`** (~80 строк sensors/DndContext/обёртка карточки) принято сознательно | Разная семантика: `Board` = реальные статусы проекта, `OrgBoard` = 5 категорий + transition-to-category; преждевременная абстракция рискованнее дубля | `frontend/src/ui/tracker/{Board,OrgBoard}.tsx` | рефактор-ТЗ позже (low-prio) |
| 2026-06-18 | **Визуальная qa-приёмка хлебных крошек (D Ф5) и «Ленты Коры» в дашбордах (C Ф5)** — глазами / Playwright не проверено | Код-приёмка зелёная (typecheck/build/тесты), но визуальная приёмка — ручной шаг, не выполнялась в батч-сессии | ТЗ [`2026-06-17-cabinet-breadcrumbs-and-mobile-back.md`](../../plans/tz/2026-06-17-cabinet-breadcrumbs-and-mobile-back.md), [`2026-06-17-cora-feed-into-dashboards.md`](../../plans/tz/2026-06-17-cora-feed-into-dashboards.md) | needs-qa (владелец / qa-tester) |
| 2026-06-18 | **`/issues/[id]` хлебная крошка без `parentHref` на доску проекта** — сегмент `issues` помечен non-navigable | Домен Issue не несёт slug проекта; доп. fetch ради крошки — дорого, осознанно отложено | ТЗ [`cabinet-breadcrumbs-and-mobile-back`](../../plans/tz/2026-06-17-cabinet-breadcrumbs-and-mobile-back.md) Ф4 | **внесено в ТЗ** [`tracker-card-redesign-and-progress`](../../plans/tz/2026-06-20-tracker-card-redesign-and-progress.md) Волна 0 (Ф0.1 — `projectSlug` в Issue DTO + `parentHref` на доску) |
| 2026-06-18 | **Повод probe `attribution.unresolved_at_ingest` (Ф6) может шуметь** — эмитится на каждой новой `customer`/`vendor`-сущности без привязки | By design — deferrable-окно + семантический дедуп + recheck должны гасить; наблюдать на проде | ТЗ [`probe-system-phase2`](../../plans/tz/2026-06-17-probe-system-phase2.md) Ф6 | наблюдать на проде (при шуме — порог значимости / глубже в digest) |
| 2026-06-20 | **🔴 Реестр решений молча пуст у активного тенанта (Decision=0 при decision-сигналах)** — Б3 из knowledge-core-MASTER реализовали лишь как guard (Б48 check-then-create), но он НЕ удержал: на проде 2026-06-20 у Романа 4× `P2002 sourceIdeaBlockId` УЖЕ после Б48 → решения теряются. Причина рецидива — TOCTOU поверх check-then-create (concurrency=4). Класс шире Б3: 2-й сырой писатель `specialists-combined:269` + латентный cross-tenant в `graph.service:697`. Idea/Insight/Goal/Task не затронуты | Подтверждено большим тестом агентов (diag прод + harness). Прошлый фикс — «лучше проверять», гонку так не лечат; нужен create+catch(P2002)→merge | прод-подтверждённый «добив» Б3: ТЗ [`2026-06-20-decision-materialization-idempotency-fix.md`](../../plans/tz/2026-06-20-decision-materialization-idempotency-fix.md) (ready-to-implement, Ф1–Ф6) + аналитика [`…big-test-RESULTS.md`](../../plans/analysis/2026-06-20-agents-big-test-RESULTS.md) №2b/2c | **РЕАЛИЗОВАНО в dev** (Ф1–Ф6): три писателя Decision идемпотентны — specialist-3-3 create+catch(P2002)→merge (`6e344b59`), specialists-combined P2002=дедуп + graph.service.upsertDecision P2002-safe+cross-tenant→ConflictException (`3fcaa2df`), бэкофилл `backfill-decisions-from-signals.ts` (`5657f5b2`). **@unique оставлен** (точка сериализации трёх писателей; миграции нет). Остаётся только real-DB e2e гонки (отдельная строка ниже, CI-гейт) + прод-прогон бэкофилла. Закрыть после прод-выката |
| 2026-06-20 | **Назначение задачи на другого человека не уведомляет исполнителя — нигде** (ни помощник, ни UI трекера); помощник вообще не умеет ставить задачу на другого (`me/tasks` жёстко self), а текст подтверждения сыплет англ. поля `title/description/dueDate` | Вскрыто из вопроса Насти; событие `issue.assignee_changed` есть, но его слушает только граф знаний, пуша исполнителю нет; примитив доставки `sendNotification` готов | ТЗ [`2026-06-20-assistant-assign-task-to-others-and-notify.md`](../../plans/tz/2026-06-20-assistant-assign-task-to-others-and-notify.md) (Ф1–Ф5) + анализ [`…confirm-text.md`](../../plans/analysis/2026-06-20-assistant-task-assignment-and-notify-and-confirm-text.md) | ✅ **РЕАЛИЗОВАНО на dev** (Фазы 1–5, коммиты `642d52cf`/`a1d616b4`/`939d7165`/`240e388c`: эндпоинт `/me/tasks/assign` + резолвер имени + инструмент `assign_task` + уведомление `issue.assigned` Telegram/MAX + человекочитаемый confirm). **Ждёт прод-выката** (миграций нет, `docker compose up -d --build`). Реестр был устаревшим. **2026-06-22 дополнено Слоем 2 (skill-based-task-routing):** появилась ПОДСКАЗКА «кому назначить» по скиллам — `POST /me/tasks/suggest-assignee` + concierge-tool `suggest_assignee` (`SkillRoutingService`, НИКОГДА не присваивает сама, Р1); присвоение остаётся существующим путём `addAssignee` (`viaRouting:true`) + это же уведомление исполнителю |
| 2026-06-16 | **59 подтверждённых багов логики/контракта в модуле усвоения knowledge-core** — состязательный аудит (12 под-областей, 178 агентов, 2 прохода): 4 high (P2002-`catch` абортит транзакцию слияния — Б1; soft-deleted рёбра графа отдаются в RAG/5 ридеров — Б2; коллизия `@unique sourceIdeaBlockId` тихо теряет богатое решение Specialist 3.3 — Б3; RawEvent `ingested` до `enqueueBlockDistill` без reconcile → потеря дистилляции — Б4), 36 med, 19 low + 8 пробелов покрытия (G1 ⚠️ обход платного `feature.graph`). Фиксы НЕ реализованы | Read-only аудит по `docs/methodology/code-audit`; сведён в единый зонтик с переписью 31 промпта (🟡) и task-dedup ТЗ — чтобы исполнять одной сессией по волнам | **ТЗ-зонтик** [`2026-06-16-knowledge-core-MASTER.md`](../../plans/tz/2026-06-16-knowledge-core-MASTER.md): §3 контракт 59 багов + 23 опровергнуто, §4 классы, §5 связки «промпт↔код», §6 волны 0–6 | передаётся на реализацию по волнам (§6); G1–G8 — следующий раунд аудита |
| 2026-06-16 | **21 баг в логике кода модуля M5 «Клоны»** — состязательный аудит вскрыл: застой pending-черт + голодание verify (Б1/Б2), потерю роле-версионных полей при rebuild клона роли → клон без носителя + runaway-пересборка (Б12/Б16), гонку двух active клонов роли (Б13), supersede до успешного build → `role_persona_unavailable` (Б17), дрейф `traitCount` + молчаливый no-op слияния концептов (Б6/Б8), confidence принципов не из distinct-dates (Б9), CDM-бюджет считает недоставленные probe (Б21) и др. (10 high / 6 med / 5 low) | Read-only аудит, фиксы НЕ реализованы — передаётся другой сети на реализацию; + ревизия 12 промптов M5 (тоже не выкачена) | ТЗ [`2026-06-16-clone-agents-prompt-revision.md`](../../plans/tz/2026-06-16-clone-agents-prompt-revision.md) §8 (21 баг+фиксы, 6 опровергнуто) · Г1–Г4 §3.2 · промпты Прил. A–D · **Раздел 7** (модель носителей «один человек = один клон должности» — переопределяет фиксы Б12/Б13/Б16/Б17, + под-фича «совет бывших», + миграция freeze) | передано на реализацию (другая сеть); порядок — §8.0, кластер версионирования — по Разделу 7 |
| 2026-06-16 | **Дубль-ветка извлечения сущностей: `specialists-combined` (1 вызов→8 типов) СПИТ, поблочные специалисты 3-1/3-3/3-5/3-6/3-9 — живой прод-путь** — обе делают одно и то же. Combined выиграл эксперимент Б+ (18:13, ×3.7 дешевле), но его cron-producer удалён 2026-06-10 вместе с v2-стеком (`KNOWLEDGE_CORE_V2_AGENTS_ENABLED` прод не включал); enqueue остался ручной/тестовый. Нужно решить судьбу: включить combined вместо поблочных (дешевле) ИЛИ выпилить combined как мёртвый задел | Вскрыто при апгрейде промптов (пачки 2–3). Промпты обоих путей переписаны по методологии, но архитектурный выбор «какой путь живёт» — решение владельца, вне scope промпт-ТЗ | [specialists-combined.worker.ts:11-14](../../backend/src/modules/knowledge-core/workers/specialists-combined.worker.ts) · ТЗ [`2026-06-16-knowledge-core-specialist-extractors-prompts.md`](../../plans/tz/2026-06-16-knowledge-core-specialist-extractors-prompts.md) REALITY-CHECK | needs-owner-go (включить combined / выпилить; промпты обоих путей готовы) |
| 2026-06-16 | **`idea-cluster-merge`: вердикт `standalone` не имеет отдельной ветки в кроне — эффективно = `new_cluster`** — `idea-clusterer.cron.ts` обрабатывает `add_to_existing`, остальное (включая `standalone`) ведёт к созданию нового кластера; «оставить идею одиночной, дождётся похожих» downstream не обеспечивает | Вскрыто при апгрейде промпта пачки 7 (cluster-rollup). Промпт смягчён (не обещает долгоживущего одиночного состояния); сама ветка — отдельный downstream-тикет, вне scope промпт-ТЗ | [idea-clusterer.cron.ts:229,267](../../backend/src/modules/knowledge-core/workers/idea-clusterer.cron.ts) · ТЗ [`2026-06-16-knowledge-core-cluster-rollup-prompts.md`](../../plans/tz/2026-06-16-knowledge-core-cluster-rollup-prompts.md) REALITY-CHECK | low / next-wave (`idea-cluster-standalone-branch`) |
| 2026-06-17 | **Document Conversion Service (DCS) — НЕ построен, repo-артефакты удалены** — планировался Python-sidecar (Docling/RapidOCR, REST `POST /convert`) для универсального ingest документов; дошёл только до Ф0 research+smoke, в прод-стек не вводился. Smoke-харнес `infra/document-conversion/` удалён, арх-док убран из `02_architecture/` | Сервис не строился (в коде/ENV ноль ссылок); ingest документов закрыт текущим парсером. Возрождать — отдельное решение владельца | research `plans/analysis/2026-05-31-document-conversion-stack.md`, ТЗ `plans/archive/2026-05-31-document-ingest-universal.md` | отложено (vNext / решение владельца) |
| 2026-06-17 | **Дизайн-доки: риск рассинхрона `design-system.md` ↔ корневые `DESIGN.md`/`PRODUCT.md`** — нарратив слит в `02_architecture/design-system.md` (канон), но `DESIGN.md` (Stitch) и `PRODUCT.md` оставлены в корне как машинно-читаемый контекст скилла `impeccable` (он их регенерит) → при правках дизайна нужно синхронить вручную; кросслинки добавлены | Удалять корневые нельзя — сломает `impeccable` (tool-managed, как `.claude/CLAUDE.md`); решение владельца — вариант A | `DESIGN.md`, `PRODUCT.md`, `second-brain/02_architecture/design-system.md`; `.claude/skills/impeccable/scripts/load-context.mjs` | принято (sync вручную; авто-генерация из design-system → DESIGN.md — возможный vNext) |
| 2026-06-17 | **dead-code: ~1100 knip-кандидатов (exports/types) НЕ вычищены** — удалены только 228 с 0 ссылок (count≤1); остальные используются внутри файла/через ре-экспорт/динамически и требуют поштучной проверки | Консервативный режим (решение владельца): без поштучной проверки массовое удаление рискует задеть DI/Zod-DTO/реэкспорт. Также НЕ трогали dep-false-positives (multer/swagger-ui-express/react-force-graph-2d) и спорный `media-icons` (Vidstack peer) | `bunx knip` в backend/ и frontend/; рефлексия 2026-06-17 | отложено (поштучный разбор по доменам) |
| 2026-06-17 | **plans/ гигиена — ЧАСТИЧНО, остаток** — перенесено 39 code-verified реализованных ТЗ/логов + live-SoT ссылки 202→71 (коммит `e8475ecb`); осталось: (а) в `plans/tz/` возможны ещё shipped-ТЗ без точного code-verify (эвристика ненадёжна — не двигал без сверки); (б) битые относительные ссылки во ФРОЗЕН-доках (`plans/archive`, `05_история` append-only) НЕ чинились | Двигать ТЗ без code-verify рискованно (можно заархивировать открытое); фрозен-доки — снимок времени, низкая ценность правок | коммит `e8475ecb`; `/tmp/linkcheck2.mjs` | отложено (точечная сверка остатка ТЗ; фрозен-ссылки — не чинить) |
| 2026-06-17 | **user-guide 01/06/08 — косметический re-stamp** не сделан (аудит дал «verify on next refresh», без конкретного факт-противоречия) | Конкретного расхождения нет, только «дата старше навигации». Ре-индекс `index.md` (0 несвязанных) и конкретные «update»-доки (runbooks systemd→docker, economics→super_admin, dashboards-статусы, openapi-scope, livekit-баннер) — СДЕЛАНЫ | коммиты `ff9a2d81`, `c25a2228` | отложено (косметика при следующем refresh user-guide) |
| 2026-06-17 | **Bitrix CRM заметки/таймлайн** (`BitrixCrmNote`) — посуточный CRM-дайджест (контакты/компании/сделки/лиды) сделан (Ф4b), а синк заметок/активностей (`crm.timeline.comment`/`crm.activity`) НЕ наполняет `BitrixCrmNote` → комментарии и активности по карточкам в дайджест/граф не попадают | `crm.timeline.*` — per-entity (запрос на каждую сущность), тяжело; вынесено отдельно, чтобы не раздувать дельта-синк. Зеркало `BitrixCrmNote` уже есть (Ф3.5) | ТЗ [`2026-06-17-bitrix24-source-sync.md`](../../plans/archive/2026-06-17-bitrix24-source-sync.md) §«Осталось» | отложено (per-entity синк timeline — отдельный проход) |
| 2026-06-16 | **Role-scope staleness-watcher не покрыт триггерным кроном** — role-snapshot клона должности пересобирается только недельным `ExecutablePersonaBuildCron`, между прогонами нет 48ч max-age триггера (как у person-scope через `ExecutablePersonaTriggerWatcherCron`) | `triggerRebuild` принимает только `profileId` (person-scoped); добавление role-пути в watcher — отдельная задача, в scope Раздела 7 не входила | `backend/src/modules/knowledge-core/workers/executable-persona-trigger-watcher.cron.ts` (читает только `profileId`/`scope:'person'`); ТЗ [`2026-06-16-clone-agents-prompt-revision.md`](../../plans/tz/2026-06-16-clone-agents-prompt-revision.md) Б14 | next-wave (`role-scope-staleness-watcher`) |
| 2026-06-16 | **Фронтенд «спросить версию» / «совет бывших» рендерят ответ inline, а не через полноценный ChatV2-conversation-поток** — frozen-версии клона роли НЕ создают conversation (нет сохранённой нити per-version) | v1-ограничение Раздела 7 Фазы D: ответ показывается одноразово; полноценный per-version чат с историей — отдельный слой | ТЗ [`2026-06-16-clone-agents-prompt-revision.md`](../../plans/tz/2026-06-16-clone-agents-prompt-revision.md) §7.5; frontend история роли + экран «Совет бывших» | vNext (per-version conversation) |
| 2026-06-15 | **«Моя карта должности» на /me (и /roles/[id]) пуста** — `GET /me/profile` не отдаёт `summaryCache`, фронт `RoleProfileBlock` всегда показывает заглушку «Карта формируется…»; маппер `summaryCache → blocks` для `/me` НЕ существует (есть в `role-profiles`-сервисе, надо переиспользовать). **Контракт-баг + нужно продуктовое решение владельца какой вариант карты показывать** (что показывать, как считать `pending`, нужен ли источник-клик) | Вскрыто при Ф2 QA-багфиксов; включение карты роли на /me — продуктовая поверхность (CLAUDE.md правило про продуктовое). Ф2 не расширяла scope | ТЗ [`2026-06-15-me-role-map-card-contract.md`](../../plans/tz/2026-06-15-me-role-map-card-contract.md) | ✅ реализовано 2026-06-15, коммит `8f2a6f73` (владелец выбрал Вариант 1 — полная карта роли через Role Map; починен и /roles/[id] overview) |
| 2026-06-15 | **Селектор клона (кабинет): единая СОХРАНЁННАЯ нить помощник+клон** — в v1 склейка клиентская, клон-сообщения живут в своей clone-ленте (`/chat` их не перезагружает) → рассинхрон ленты | Осознанное v1-ограничение (desktop-селектор реализован, коммит `4e9f1fec`); «единая сохранённая нить» — отдельный слой | ТЗ [`2026-06-15-cabinet-assistant-clone-selector.md`](../../plans/archive/2026-06-15-cabinet-assistant-clone-selector.md) §9 «Риски» | vNext (владелец) |
| 2026-06-15 | **Селектор клона в мобильном кабинете (`OrgChatPanel`)** — desktop-селектор «помощник / клон» готов, мобильный композер не интегрирован | Mobile-композер сильно отличается; интеграция дорогая, вынесена отдельно, чтобы не блокировать desktop | ТЗ [`2026-06-15-cabinet-assistant-clone-selector.md`](../../plans/archive/2026-06-15-cabinet-assistant-clone-selector.md) §9 «Риски» | vNext (владелец/разработчик) |
| 2026-06-15 | **`ask_chat_v2` passthrough — смешанный turn «узнать→сделать» не продолжает цикл** — терминальным считается только когда ЕДИНСТВЕННЫЙ вызванный инструмент = `ask_chat_v2`; если помощник в одном turn зовёт и `ask_chat_v2`, и инструмент-действие, продолжение цикла ограничено | Ограничение принято осознанно при дедупе помощника (`ask_chat_v2` терминальный, понимание/синтез по одному разу), коммит `f49e7212` | ТЗ [`2026-06-14-assistant-router-dedup-and-prompt.md`](../../plans/archive/2026-06-14-assistant-router-dedup-and-prompt.md) | ограничение принято (vNext при необходимости) |
| 2026-06-15 | **Модуль «Операционный директор»: orphan-слой — бэкенд считает, UI не показывает.** (1) Радар клиентов под риском (`customer-risk`), контролёр доведения решений (`decisions/throughput`+`stalled`), знание-под-риском (`knowledge-at-risk`) — эндпоинты живут, фронт `operations-dashboard.api.ts` их не вызывает; (2) 6 из 7 pulse-паттернов (ROI встреч, bus-factor, узкие места, необратимые решения, скорость ответа, повторяющиеся темы) считаются, на главной используется только goalVector→CompassWidget; (3) **2 мёртвых сигнала**: `PromiseNetworkAnalyzerCron`→`PromiseNetworkSnapshot` (никто не читает) и `TeamHealthAnalyzerCron`→`Department.healthSummaryJson` (ежедневный LLM-вызов вхолостую); (4) доставка дневного дайджеста OFF по умолчанию (`operations.daily_digest.deliver_to_telegram=false`) — нарушает Ship-On; (5) заглушки `team-health.decisions`/`team-detail.goals`; (6) нет анализа: совещательная нагрузка, cycle time решений, реальная ёмкость из сигналов | Системный корень: редизайн «rhythms» переписал главную с вкладок на «Сегодня», осиротив готовые виджеты. Это аналитический разбор (вскрыт пробел подключения, не отсрочка реализации) | анализ [`plans/analysis/2026-06-15-coo-operational-director-module-agents-and-dashboards.md`](../../plans/analysis/2026-06-15-coo-operational-director-module-agents-and-dashboards.md) §5b–5e | research-complete (ТЗ на «подключить orphan'ы к UI» не начато; приоритет 1 — customer-risk «деньги») |
| 2026-06-15 | **UI-селектор «общий помощник / клон» у окна ввода (кабинет) + маршрутизация выбранного клона на `ClonesService.askPerson`; Telegram — только общий помощник (клоны кабинет-онли)** — решено убрать клон-как-инструмент помощника, клон выбирается человеком (как выбор модели) | Решение владельца 2026-06-15; фронт + маршрутизация каналов — отдельный слой, в ТЗ промпта chat-v2 не входит | ТЗ [`2026-06-15-cabinet-assistant-clone-selector.md`](../../plans/archive/2026-06-15-cabinet-assistant-clone-selector.md) | ✅ реализовано 2026-06-15, коммит `4e9f1fec` (desktop-селектор через `clonesApi.askRole`; mobile + единая сохранённая нить — отдельные vNext-строки выше) |
| 2026-06-10 | **Агент-компилятор орг-документа (A7): полная версионная обвязка + sync `steps[]` процесса** — компилятор собирает `contentMd` и инкрементит `Regulation/Policy.version`, но `changeReason` НЕ пишется в отдельную версию (модели `RegulationVersion` нет в схеме — анализ описывал её как будущую инфру через `CardVersion`); для `process` собранное описание пишется в `Process.description`, а `steps[]` из вывода компилятора НЕ синхронизируются с таблицей `ProcessStep` (шаги по-прежнему пишутся single-step upsert'ом из `processStepHint`) | Полная версионная обвязка (`CardVersion`/`changeReason` history + diff-просмотр) и steps-merge — отдельный слой; на A7 минимум: собрать `contentMd` + инкремент `version`, не ломая dedupe-путь | `backend/src/modules/knowledge-core/services/specialist-3-1-regulations.service.ts` (`tryCompileContent`, ветки existing-update) + анализ [`plans/analysis/2026-06-09-prompt-rewrites/22-org-entities-COMPARE-and-compiler.md`](../../plans/analysis/2026-06-09-prompt-rewrites/22-org-entities-COMPARE-and-compiler.md) §3 | next-wave (версии/steps-sync) |
| 2026-06-14 | **Instruction — упрощённый `upsertInstruction` (нет версий/триажа/дедупа)** — инструкции пишутся single-upsert по `tenantId_name`: НЕ пишется `CardVersion` (поэтому `getHistory` для instruction возвращает `{items:[]}`, `regulations.service.ts:396`), нет KNN-дедуп-арбитра и curation-triage (в отличие от regulation/process), нет `instructionCount` в `structure.service` (есть в новом `/regulations/summary` C4, но не в structure-сводке). Хаб «Оцифровано» (C-ТЗ master-fixes) показывает инструкции и их источники-цитаты (C3), но история версий/триаж для них пусты | Осознанно упрощено при вводе Instruction; полная обвязка (CardVersion history + dedup + triage + steps) — отдельный слой, как у A7-компилятора (строка выше) | `specialist-3-1-regulations.service.ts:1103-1177` (`upsertInstruction`); `regulations.service.ts:388-418` (`getHistory`, instruction→пусто) | next-wave (`instruction-versioning-and-dedup`) |
| 2026-06-14 | **Три расходящихся источника мобильной навигации (A11.6 master-fixes, low)** — `PRIMARY_NAV_ITEMS` (`primary-nav.ts`), `MOBILE_EXEC_TABS`, `MOBILE_MANAGER_TABS` (`nav-config.ts`) не выведены друг из друга и расходятся; оба файла заявляют себя «единым источником» | Связная консолидация (свести MOBILE_*_TABS к роль-фильтру поверх `resolveDesktopNav` ИЛИ PRIMARY_NAV_ITEMS к производному от nav-config) — рефактор nav с риском задеть мобильный UX; не крамить в конец большого свипа. **Риск расхождения уже закрыт гардом** (A11.2: `nav-subset.spec` теперь проверяет `MOBILE_EXEC_TABS`/`MOBILE_MANAGER_TABS ⊆ desktop`) | `frontend/src/ui/components/app-shell/primary-nav.ts` vs `nav-config.ts:474-490` | low / next-wave (`nav-config-consolidation`) |
| 2026-06-10 | **LiveKit на korateam.ru не доставляет вебхуки на backend (`room_started`/`room_finished`) → каждая новая встреча виснет в `scheduled`** (инфра, ВНЕ кода). Прод: встреча `01KTR54GYJ47MMD76HADMVYAQH` зависла, технический след пуст (401 нет — вебхуки не доходят физически). Код-страховка (Ф5 устойчивый `finish` из `scheduled` + Ф6 idle-cron reconcile брошенных `scheduled`) реализована (ветка `feature/meeting-cabinet-fixes-2026-06-10`), но саму доставку она НЕ чинит — лишь не даёт встрече зависнуть навсегда | Переезд `meet.crossmark.ru → korateam.ru` (09.06): `webhook.urls`/reverse-proxy не настроены на эндпоинт `/webhooks/livekit` (ВНЕ `/api/v1`) | ТЗ [`2026-06-10-meeting-stuck-and-team-roster-fixes.md`](../../plans/tz/2026-06-10-meeting-stuck-and-team-roster-fixes.md) §4 | ✅ **РЕШЕНО владельцем 2026-06-11** — инфра LiveKit (`webhook.urls` + nginx на `/webhooks/`) настроена, вебхуки доходят. Код-страховка Ф5/Ф6 остаётся как защита от зависания. Из активного списка снято |
| 2026-06-10 | **Раздел «Команда»: дубли сотрудников по email + статус «не приглашён» у всех — код сделан, ждёт прод-выката backfill+индекса** — один человек двумя строками; активные участники помечены «не приглашён». Ф1–Ф4 реализованы (ветка `feature/meeting-cabinet-fixes-2026-06-10`: дедуп ростера по email + статус по membership, дедуп при создании, partial unique), но существующие дубли в проде схлопнет только прогон backfill Ф3 + установка self-skip-индекса | ростер объединял только по `userId` (`orgs.service.ts:294`), не по email; `@@unique([tenantId,email,deletedAt])` бесполезен (NULL≠NULL в PG, `schema.prisma:4834`); статус из `invitations[0]` игнорировал `Membership` (`orgs.service.ts:285`) | ТЗ [`2026-06-10-meeting-stuck-and-team-roster-fixes.md`](../../plans/tz/2026-06-10-meeting-stuck-and-team-roster-fixes.md) (Ф1–Ф4) + `docs/operations/prod-deploy-log.md` Шаг 5/8 | needs-deploy (прогон `backfill-merge-duplicate-persons.ts --apply` + `apply-postgres-init` на проде) |
| 2026-06-10 | **Telegram-вход: согласованный интент-классификатор ОБХОДИТСЯ «ботом для задач»** — любой голос/текст боту → IntakeIssue (задача); классификатор `dialog-classify` (план/отчёт/вопрос/заметка, большой промпт, согласован владельцем ТЗ 2026-05-29 rev3) НЕ получает управление, т.к. task-handler (ТЗ 2026-05-23) перехватывает раньше. В промпте классификатора к тому же НЕТ категории «задача» | Две фичи не состыкованы: `taskHandler.tryHandle` (всегда `true` на непустой ввод) вызывается до `classifyIntent` — `telegram-bot.adapter.ts:526-534` (текст) / `:377-385` (голос) | анализ [`plans/analysis/2026-06-10-telegram-intent-classifier-bypass.md`](../../plans/analysis/2026-06-10-telegram-intent-classifier-bypass.md) → ТЗ [`2026-06-10-cabinet-fixes-master.md`](../../plans/tz/2026-06-10-cabinet-fixes-master.md) (§2) + читалка «мои задачи» (§2 Ф5) | **реализовано** (ветка `feature/meeting-cabinet-fixes-2026-06-10`, коммит `57793d3c`; §2 Ф1–Ф5: classify.prompt +task/show_tasks, classifyIntent ДО task-handler, tryHandleStructural, handleShowTasks), ждёт прод-выката (`docker compose up -d --build backend`; миграций/ENV нет) |
| 2026-06-10 | **Чек-ины (утренний план / вечерний отчёт) НЕ попадают в граф знаний** — `DailyCheckIn` (plans/dones/blockers/инсайты) хранится изолированно; событие `checkin.created` слушает только sentiment-анализатор, моста в knowledge-core нет → AI-чат не отвечает «что делал сотрудник X на прошлой неделе», ролевые клоны не учатся на ежедневной работе (известный пробел «β-8») | Нет `IngestService.ingest`/`RawEvent` для чек-инов (verified: `router.service.ts:365` — лишь комментарий «COO/DailyCheckIn в β-8»; подписка только `checkin-sentiment-analyzer.worker.ts:42`) | ТЗ [`2026-06-10-daily-checkin-to-graph-bridge.md`](../../plans/tz/2026-06-10-daily-checkin-to-graph-bridge.md) (решение A — мост по образцу chatbox-ingest) | **реализовано** (ветка `feature/meeting-cabinet-fixes-2026-06-10`, коммит `de288e8a`; Ф1–Ф4: enum+миграция, CheckinIngestService, CheckinGraphIngestListener, kill-switch+метрика), ждёт прод-выката (`docker compose up -d --build` → migrate deploy авто) → после выката перенести в «Закрытые» |
| 2026-06-10 | **Re-ingest чек-ина при replace того же дня НЕ доезжает в граф (v1-ограничение моста checkin→graph)** — если сотрудник переотправил план/отчёт того же дня (`upsertFromParser` обновляет ту же `DailyCheckIn`), в граф попадает ПЕРВАЯ завершённая версия | Осознанный выбор: `occurredAt` стабилизирован по `dateLocal`, `sourceExternalId=checkInId` неизменен → `IngestService.ingest` отдаёт существующий RawEvent и payload НЕ обновляет (эмпирически проверено). Стабильный idempotencyKey нужен для инварианта «ровно один RawEvent на чек-ин» (R3); `completedAt` мутирует на каждом upsert и дал бы дубль. «Первый завершённый чек-ин дня = канон» | `backend/src/modules/operations/services/checkin-ingest.service.ts` (`checkinOccurredAt`); ТЗ [`2026-06-10-daily-checkin-to-graph-bridge.md`](../../plans/tz/2026-06-10-daily-checkin-to-graph-bridge.md) §«Не входит» | vNext-ТЗ `checkin-graph-reingest-on-replace` (supersede старого RawEvent/блоков при wasReplace) — при запросе владельца |
| 2026-06-10 | **Чат «Помощник компании»: ответ ~25–60с синхронно, без стрима/стадий → «зависает»; синтез ~28.6с впритык к таймауту 30с** (риск потери ответа) | `POST /chat-v2/messages` синхронный, единственный фидбек «Кора печатает…», нет стрима/прогресса (прод-трасса) | ТЗ [`2026-06-11-cabinet-leftovers-ui-probe-chat.md`](../../plans/tz/2026-06-11-cabinet-leftovers-ui-probe-chat.md) (Часть 3); verified 2026-06-11: `chat-v2.controller.ts:79` всё ещё синхронный | ready-to-implement |
| 2026-06-10 | **Уведомления/probe** — изначально 4 бага: адресация «не тому» + технические тексты (типы/cuid/уровни/логины) + заголовки-коды + ответ как JSON. **+ 5-й (пункт E): фронт показывает сырой `payload.message` блоком «Контекст» для `probe.question`** (дублирует человеческий вопрос + протекает латиницей-классами/cuid от всех эмиттеров) | эмиттеры кладут техповод в `payload.message`; `NotificationsClient.tsx:339-344` рендерит его дословно для probe.question | §3 Ф1–Ф3: ТЗ [`2026-06-11-cabinet-leftovers-ui-probe-chat.md`](../../plans/tz/2026-06-11-cabinet-leftovers-ui-probe-chat.md) (Часть 2); пункт E: отдельное ТЗ [`2026-06-11-probe-question-context-leak-fix.md`](../../plans/tz/2026-06-11-probe-question-context-leak-fix.md) | **§3 Ф1–Ф3 реализованы** (origin `meeting-cabinet-fixes-2026-06-10`: `a088e25d`/`dbde0d57`/`6655f769`), **ждут прод-выката** (живой симптом 2026-06-11); **пункт E — РЕАЛИЗОВАН** (поглощён provenance Ф0.2, 2026-06-20, `fab510be`: probe.question payload несёт blockId+quote, `NotificationsClient` рендерит блок «По поводу») |
| 2026-06-10 | **UI технические протечки — 186 находок (6 классов)**: error.message/HTTP, англ-enum, cuid, raw-JSON, заглушки. Ф0 (класс ошибок core + курация + спринты) **закоммичена** (`25981b53`/`5679b5d0`) + **Ф1** humanizeApiError на 114 файлах (`5b0b27c3`) | мульти-агентный аудит; чинить классами | аудит + ТЗ [`2026-06-11-cabinet-leftovers-ui-probe-chat.md`](../../plans/tz/2026-06-11-cabinet-leftovers-ui-probe-chat.md) (Часть 1) | partial (Ф0+Ф1 done; **Ф2–Ф6 open** — verified 2026-06-11) |
| 2026-06-10 | **«Кому видно» — vNext-остаток** (видео-часть РЕАЛИЗОВАНА, см. «Закрытые»). ОТКРЫТО: (а) **«факты наследуют доступ источника»** — chat-v2/search/клоны пока НЕ фильтруют блоки графа по видимости встречи-источника (нужен отдельный ТЗ; владелец «надо подумать») — Д7; (б) **«Кому видно» для документов** — сейчас «Всей компании», сужение не делали — Д6. knowledge-access авто-раскладку НЕ трогаем (требование владельца) | Владелец сузил scope до видео-встреч и просил граф знаний не ломать; документы и facts-inheritance отложены в vNext | видео-ТЗ [`plans/tz/2026-06-10-meeting-visibility-who-can-see.md`](../../plans/tz/2026-06-10-meeting-visibility-who-can-see.md) §Scope «Не входит»; анализ [`source-access-parity`](../../plans/analysis/2026-06-10-source-access-parity-with-knowledge-groups.md) | факты: needs-owner-go (отдельный ТЗ `facts-inherit-source-visibility`, Д7); документы: needs-owner-go (Д6, vNext) |
| 2026-06-10 | **ChatBox: анализ переписки у «Ооо луа» (svmazur@mail.ru, tenant `cmpndk2tw…`) не запускался ни разу** — синк забрал ~215 чатов, но `analysisEnabled=false` (дефолт OFF) → цепочка знаний не отработала, переписка в граф/память не попадает. Прод-факт (diag logs: 0 `ChatboxAnalyze` у этого тенанта; анализ шёл только у dev-орга Tozix) | Гейт `analysisEnabled` молча дефолтится в OFF (против Ship-On); + матчинг сотрудников только по email, нет видимости результата и наблюдаемости | анализ [`plans/analysis/2026-06-10-chatbox-svmazur-analysis.md`](../../plans/analysis/2026-06-10-chatbox-svmazur-analysis.md) → ТЗ master (§5) | **Ф1 сделано** (`d0e0167b` — дефолт `analysisEnabled=true` + патч для подключённых), ждёт прод-выката; Ф2–Ф5 (матчинг участников/виджет/метрики) — open |
| 2026-06-10 | **retest3 #26 — пословные/сегментные тайминги Vox для живых дорожек** (транскрипт «дорожками подряд», поведенческие метрики абсурдны). Сделан гард вырожденной диаризации (UI «недоступно») + #74 диагностика, но САМ фикс таймингов не написан | Требует **ШАГ-0 smoke** на реальной прод-модели Vox (`backend/scripts/smoke-vox-diarization.ts`) — имя submit-флага неизвестно; ТЗ: «БЕЗ ШАГ-0 фикс #26 не писать». Нужен прод-доступ + реальная встреча | [`plans/analysis/2026-06-10-diarization-timing-rootcause.md`](../../plans/analysis/2026-06-10-diarization-timing-rootcause.md); retest3 Ф3 | needs-owner-go («можно в прод» + smoke) |
| 2026-06-10 | **retest3 #20/#24 — Sidebar UX**: сворачивание второстепенных групп меню для новичка (Р10) + единый nav-конфиг desktop/mobile (Р11, mobile bottom-nav ⊄ desktop: `/feed` нет в desktop) | Связная перестройка `Sidebar.tsx` (1106 строк) с role-gating — рискованно крамить в конец большого свипа; вынесено на отдельное ревью. Решения Р10/Р11 приняты | [`plans/tz/2026-06-10-onboarding-nav-and-prod-retest-followup.md`](../../plans/tz/2026-06-10-onboarding-nav-and-prod-retest-followup.md) Ф1–Ф2 | ready-to-implement |
| 2026-06-10 | **retest3 #17 — прод-ретест принудительной смены временного пароля** (код реализован, пропущенных путей картография не нашла) | Требует прод-прогона (инвайт→временный пароль→редирект→403→смена); нельзя проверить без прод-доступа | [`plans/tz/2026-06-10-onboarding-nav-and-prod-retest-followup.md`](../../plans/tz/2026-06-10-onboarding-nav-and-prod-retest-followup.md) Ф3 | needs-owner-go («можно в прод») |
| 2026-06-09 | **Bitrix24: синк данных в knowledge-core** — установка + жизненный цикл токена готовы, но CRM-данные (контакты/сделки/компании/пользователи) в граф знаний ещё НЕ тянутся | Осознанный scope-cut: «сейчас именно установка». Фундамент (`callMethod`+refresh) готов | [`plans/analysis/2026-06-09-bitrix24-next-steps.md`](../../plans/analysis/2026-06-09-bitrix24-next-steps.md) (Этапы 1–2) | needs-owner-go (приоритет сущностей + глубина истории) |
| 2026-06-09 | **Bitrix24: миграция не применена локально + не зарегистрировано приложение в Маркете** | `migrate deploy` на dev заблокировал safety-классификатор (счёл корневой `.env` продом); client_id/secret появятся после регистрации тиражного app | миграция `20260609112355_bitrix_integration`; партнёрский кабинет Bitrix24 | владелец (регистрация app + ENV) |
| 2026-06-09 | **Bitrix24: box/self-hosted + placement-виджеты** не сделаны | Вне scope установки; box использует свой oauth-сервер, виджеты требуют app с UI | [`plans/analysis/2026-06-09-bitrix24-next-steps.md`](../../plans/analysis/2026-06-09-bitrix24-next-steps.md) (Этапы 3, 5) | low-prio / при запросе |
| 2026-06-09 | **Регрессия `/persons`: пустые имена** — list-эндпоинт отдаёт `name`/`email`/`userId` (person-card), а фронт `ListPersonsResultApi.items` типизирован как `PersonEntityApi` (`canonicalName`/`aliases`/`mentionsCount`) → TS молчит, рантайм рендерит `undefined`. chatbox-пикер обошёл локально (читает `name`), но **страница `/persons` и её карточки сломаны** | Дрейф контракта (вероятно из мерджа `dev`); нужен правильный list-item тип + переработка `PersonsListClient` (убрать entity-поля). Вне scope chatbox-сессии, риск на core-странице | `frontend/src/api/persons.api.ts` (`ListPersonsResultApi.items`), `frontend/app/(authenticated)/persons/PersonsListClient.tsx` | needs-owner-go |
| 2026-06-09 | **ChatBox: профиль/страница клиента** (`ChatboxCustomer`) — в сообщении клиент только имя+бейдж, кликнуть некуда; ссылка на профиль есть только у менеджера (→ `/persons/[id]`) | Нет UI-роута карточки клиента в Коре | `frontend/.../chats/[id]/ChatDetailClient.tsx` | vNext (владелец) |
| 2026-06-08 | **value-recap: рендер слайдов-картинок (format=slides)** — `format=pptx` уже отдаёт реальный бинарный .pptx (`buildValueRecapPptx` через `pptxgenjs`, подтверждено аудитом 2026-06-15); остаётся только `format=slides` как структурный payload (заголовок + буллеты + json), без рендера в картинки | Настоящий PPTX закрыт; рендер слайдов-в-картинки — отдельная инфра, не блокирует (фронт печатает браузером + есть PPTX) | `backend/src/modules/operations/utils/value-recap-pptx.ts` (готов) + `value-recap-export.ts` (slides); аудит [`plans/analysis/2026-06-15-coo-operational-director-module-agents-and-dashboards.md`](../../plans/analysis/2026-06-15-coo-operational-director-module-agents-and-dashboards.md) §5a | low-prio (PPTX-часть закрыта; slides-картинки — при запросе владельца) |
| 2026-06-04 | Write-back одобренной курации в канонические таблицы: одобрение куратором *предложения рядового сотрудника* (`decide(approve*)`) не переносит `proposedPayload` в `regulations`/`decisions`/`process`/`policy` | Затрагивает **ядро `CurationService.decide()`** (высокий blast-radius на всю курацию); не входило в acceptance E1. Доминирующий путь (owner/admin «Исправить» → применяется сразу) работает | [`plans/tz/2026-06-04-curation-canonical-writeback.md`](../../plans/tz/2026-06-04-curation-canonical-writeback.md) | needs-owner-go (владелец: «погнали») |
| 2026-06-04 | Карточки знаний как цитаты в корпоративном чате (chat-v2) + метка доверия у цитат | Меняет стоимость/латентность и продукт ответа чата — продуктово-стоимостная развилка | [`plans/tz/2026-06-03-chat-v2-card-citations.md`](../../plans/tz/2026-06-03-chat-v2-card-citations.md) | needs-owner-go (владелец) |
| 2026-06-04 | `dispute` («Это неверно») создаёт сигнал-флаг (decided light item), а не pending-задачу куратору на разбор | Соответствует образцу `entities/:id/mark-wrong` и дословному acceptance E1; авто-pending-разбор не входил в scope | `backend/src/modules/{decisions,regulations}/services/*.service.ts` (`dispute`) | возможная доработка (low-prio) |
| 2026-06-05 | **Прод-baseline миграций ещё не прогнан на проде** (переход с db push на `migrate deploy`): baseline теперь АВТОМАТИЧЕСКИЙ (`ensureBaseline()` в schema-фазе), но первый авто-прогон на боевой БД ещё не выполнялся. Также НЕ проверен локальный `migrate dev` (shadow DB + pgvector) на живой dev-БД | У агента нет прод-доступа; локальная dev-БД пустая/не поднята. Код+доки готовы; на проде — просто `docker compose up -d` (baseline сам отработает) | [`plans/tz/2026-06-05-prisma-migrations-switch.md`](../../plans/tz/2026-06-05-prisma-migrations-switch.md) (Ф6/Ф7) + `docs/operations/prod-deploy-log.md` § «Переход на миграции» | владелец (обычный выкат) |
| 2026-06-05 | Папка skill `prisma-db-push-rules` не переименована (содержимое уже про миграции) | Переименование ломает ссылки/`name:`; не блокирует | `.claude/skills/prisma-db-push-rules/SKILL.md` | low-prio (отдельной задачей) |
| 2026-06-05 | **ChatBox: создание чата с нуля из Коры** (`POST /chats` ChatBox) — только отправка в существующий чат | Вынесено в vNext; MVP — read-mostly зеркало + ответ в существующий тред | [`plans/tz/2026-06-05-chatbox-integration.md`](../../plans/tz/2026-06-05-chatbox-integration.md) §«Не входит» | vNext (владелец) |
| 2026-06-05 | **ChatBox: скачивание медиа в S3** (image/audio/video/file) — храним только `*Url` из ChatBox | Объём/стоимость хранилища; для MVP ссылок достаточно | [`plans/tz/2026-06-05-chatbox-integration.md`](../../plans/tz/2026-06-05-chatbox-integration.md) §«Не входит» | vNext (владелец) |
| 2026-06-05 | **ChatBox: отправка нетекстовых сообщений** (только `TEXT`) | Ограничение апстрима — ChatBox `SendMessageDto` сейчас поддерживает только TEXT | [`plans/tz/2026-06-05-chatbox-integration.md`](../../plans/tz/2026-06-05-chatbox-integration.md) §«Не входит» | апстрим ChatBox |
| 2026-06-05 | **ChatBox: двусторонний PATCH правок клиентов/кастомеров** обратно в ChatBox | Кора read-only по этим сущностям; запись обратно — отдельный контракт | [`plans/tz/2026-06-05-chatbox-integration.md`](../../plans/tz/2026-06-05-chatbox-integration.md) §«Не входит» | vNext (владелец) |
| 2026-06-05 | **ChatBox: связка `ChatboxCustomer` с `Person`/`Entity` графа** (в MVP связаны только менеджеры↔Person) | Объединение клиента с графом знаний — отдельная фича | [`plans/tz/2026-06-05-chatbox-integration.md`](../../plans/tz/2026-06-05-chatbox-integration.md) §«Не входит» | vNext (владелец) |
| 2026-06-05 | **ChatBox: аналитические дашборды по чатам** (SLA, метрики ответов) | Продуктовая надстройка над зеркалом; не входит в MVP | [`plans/tz/2026-06-05-chatbox-integration.md`](../../plans/tz/2026-06-05-chatbox-integration.md) §«Не входит» | vNext (владелец) |
| 2026-06-05 | **ChatBox: открытая (последняя) сессия активного многодневного чата анализируется только после её закрытия** (пауза > порога или `CHAT_CLOSED`) — контент очень долгих `ACTIVE`-чатов не попадает в граф до закрытия сессии | By-design: в граф мостим только закрытые сессии (`endedAt != null`), открытая ещё копит сообщения | [`plans/tz/2026-06-05-chatbox-integration.md`](../../plans/tz/2026-06-05-chatbox-integration.md) | by-design (владелец, если потребуется) |
| 2026-06-05 | Встреча без записи (хост не нажал «Запись») завершается `completed` без транскрипта/отчёта и **никак не сообщает** пользователю «запись не велась — анализа не будет». Вскрыто при разборе живой встречи `01KTBK4…` на проде | Авто-запись только при `recordByDefault` ([livekit-events.handler.ts:187](../../backend/src/modules/webhooks/livekit-events.handler.ts#L187)); вне scope ТЗ schema-drift. Бьёт по доверию: «провёл встречу — результата нет» | вне рамок [`plans/tz/2026-06-05-dataclass-audit-schema-drift-fix.md`](../../plans/tz/2026-06-05-dataclass-audit-schema-drift-fix.md) §8; нужен отдельный ТЗ | needs-owner-go (владелец) |
| 2026-06-06 | Прод-выдача бонусного доступа tenant «ооо ромашка» (`cmpuz4gbs000201mvfbf3k2zk`) | Требует явного подтверждения прод-доступа владельцем в сессии (UI и эндпоинт активации готовы; не хватает только нажатия в проде) | [`plans/tz/2026-06-05-bonus-access-and-paywall-sync.md`](../../plans/tz/2026-06-05-bonus-access-and-paywall-sync.md) Ф0 | владелец |
| 2026-06-06 | Ребренд служебной панели «Z-Admin» → «Кора-Админ» (~50 видимых строк + логотип `AdminShell` + хлебные крошки) | Вне Фазы 2 ТЗ-2 + продуктовое название требует решения (Кора-Админ vs «Админка Коры»); глобальная замена `Z-Admin`→… задела бы ~130 файлов комментариев — оставлено за рамками | [`plans/tz/2026-06-05-frontend-detail-pages-and-ui-honesty.md`](../../plans/tz/2026-06-05-frontend-detail-pages-and-ui-honesty.md) Итог | владелец / след. фронт-проход |
| 2026-06-06 | **Нет разграничения доступа к знаниям внутри компании**: граф знаний (IdeaBlock) в AI-чате/поиске/контексте клона фильтруется только по `tenantId+status` → менеджер низшего звена видит «информацию совета директоров». `dataClass` гейтит только LLM-routing/egress, не доступ пользователя. Контекст ответа клона не фильтруется по правам спрашивающего. Провенанс-атрибуция к человеку неполна (только commitment + 6 reasoning-типов, ~37 типов фактов без автора) | Намеренный дефолт «shared knowledge» ([rbac-access-control.md:126](../01_projects/rbac-access-control.md)); модель согласована владельцем (группы: отделы+Руководство/Совет/личный сейф, вертикаль главнее, горизонталь мягкая, метки из источника/должности, провенанс на все типы+источники). **КОД РЕАЛИЗОВАН Ф1–Ф8** (коммиты `e253dd19`..`a5803ec4`, ~9000 строк; модели `KnowledgeGroup`/`KnowledgeGroupMember`/`IdeaBlockAccess`/матрица, миграция `20260606114416_knowledge_access_groups`, `canAccessKnowledgeGroup`, фильтр графа во ВСЕХ каналах chat-v2/search/клоны, провенанс на все типы, флаг `KNOWLEDGE_ACCESS_ENFORCEMENT` off→shadow→enforce). **Verified 2026-06-11 — реестр был устаревшим** | анализ [`plans/analysis/2026-06-06-knowledge-access-levels-and-provenance.md`](../../plans/analysis/2026-06-06-knowledge-access-levels-and-provenance.md) → ТЗ [`plans/tz/2026-06-06-knowledge-access-groups-and-provenance.md`](../../plans/tz/2026-06-06-knowledge-access-groups-and-provenance.md) | **needs-owner-DEPLOY** (не реализация): задать матрицу доступа + flip off→shadow→enforce + backfill блоков |
| 2026-06-06 | **Групповые чаты сотрудников как источник знаний** ещё не подключены (chatbox = клиент↔менеджер, не группа сотрудников) — «ежедневная переписка = как встреча» пока без коннектора | Источника employee-group-chat в коде нет; модель аудитории доступа проектируется generic (multi-participant → как встреча), но реальный ingest = отдельная фича | [`plans/tz/2026-06-06-knowledge-access-groups-and-provenance.md`](../../plans/tz/2026-06-06-knowledge-access-groups-and-provenance.md) §«Не входит» | vNext (владелец) |
| 2026-06-06 | **Финал плана теста агентов/второго мозга/клонов отложен** — оформлен только ПРЕДВАРИТЕЛЬНЫЙ (карта найденного на текущем коде, решения владельца: гибрид · снизить пороги + объём · расширить diag) | Скоро меняется код по **клонам** и по **разграничению доступа к знаниям** — план надо пересобрать на новом коде (иначе устареют цепочки, Ф4/Ф5 и спецификация diag-снимка §4.3) | [`plans/analysis/2026-06-06-agents-brain-clones-test-plan.md`](../../plans/analysis/2026-06-06-agents-brain-clones-test-plan.md) | владелец (после выката изменений клонов + доступа к знаниям) |
| 2026-06-06 | **DB-инвариант на дубль участника** (partial-unique `(meetingId,userId)`/`(meetingId,personId)` + backfill схлопывания существующих дублей) — Фаза 4 ТЗ participant-count-dedup | App-дедуп (Ф1 join) + фильтр счётчика (Ф3 present-only) уже закрыли видимый баг; миграция+backfill рискованны на фоне незавершённого прод-baseline миграций. Существующие дубли в БД останутся (счётчик их не считает) до backfill'а | [`plans/tz/2026-06-06-participant-count-dedup.md`](../../plans/tz/2026-06-06-participant-count-dedup.md) Ф4 | владелец (после прогона baseline на проде) |
| 2026-06-06 | **Корень ASR word-timestamps не подтверждён на проде** — `vox` (`v3_e2e_rnnt`) отдаёт текст, но 0 пословных таймингов → поведение/длительность нулевые. Парсер расширен на `segments[].words`, но реальная форма ответа неизвестна | Нет сырого прод-ответа Vox; submit-параметр word-ts угадывать нельзя (риск 400, как `language`). Лог `vox.no_words` (PII-safe форма) пишется и сработал на встречах 06-07; пора прочитать форму ответа и решить | **ТЗ готово** [`plans/tz/2026-06-07-asr-word-timestamps-duration-behavior.md`](../../plans/tz/2026-06-07-asr-word-timestamps-duration-behavior.md) (Ф1: снять `firstSegmentKeys`/`rawKeys` с прода → парсер/submit-флаг/модель) | needs-impl (владелец «начни»; submit-флаг — решение после Ф1) |
| 2026-06-06 | **Слияние «Входящие» + «Подтверждения» в один раздел** не сделано (две отдельные точки входа в навигации) | Отложено в vNext; не входило в acceptance A3 финального ТЗ | [`plans/tz/2026-06-06-FINAL-session-tracker-and-meetings.md`](../../plans/tz/2026-06-06-FINAL-session-tracker-and-meetings.md) (A3 §«Что НЕ входит») | needs-owner-go (владелец) |
| 2026-06-06 | **Общая оптимизация поллинга кабинета** (feed / proactive / clone-access / effective-access) — A7 консолидировал только бейджи навигации | Остальные SWR-поллеры кабинета не сведены к единому интервалу/dedup; нужен отдельный perf-ТЗ | A7 в [`plans/tz/2026-06-06-FINAL-session-tracker-and-meetings.md`](../../plans/tz/2026-06-06-FINAL-session-tracker-and-meetings.md); `frontend/src/lib/badge-polling.ts` | low-prio (отдельный perf-ТЗ) |
| 2026-06-06 | **`IntakeAutoTriageWorker take:40`** — потенциальный недобор при > 40 необработанных входящих за один проход | Вне scope A1 (пикер проекта); отдельная доработка пагинации/курсора воркера | `backend` `IntakeAutoTriageWorker` (`take: 40`) | low-prio (отдельная доработка) |
| 2026-06-06 | **Per-guest отзываемые / ротация токенов приглашений** — `POST /meetings/:id/invitees` (B5) переиспользует существующий `inviteToken` без отзыва/ротации | Вынесено в vNext; для MVP достаточно одноразового токена входа по ссылке | [`plans/tz/2026-06-06-FINAL-session-tracker-and-meetings.md`](../../plans/tz/2026-06-06-FINAL-session-tracker-and-meetings.md) (B5) | vNext (владелец) |
| 2026-06-06 | **Семантика summary в «Архив спринтов»** — тайлы «Завершены/Отменены» читают backend-поля `SprintArchiveSummaryApi.confirmed/rejected` (гипотезная семантика), а не `completed/cancelled` по статусу цикла | Релейбл UI сделан (A2), но бэк-расчёт summary остался на гипотезной семантике; нужна бэк-доработка | `frontend/app/(authenticated)/sprints/archive/SprintArchiveClient.tsx`; бэк-расчёт `SprintArchiveSummaryApi` | low-prio (бэк-доработка, вне A2) |
| 2026-06-06 | **Дублирование `pluralRu`** — 3 копии (`src/domain/contribution.ts`, `src/domain/weekly-per-person.ts`, локальная в `SprintArchiveClient.tsx`) | Стоит вынести в общий util; не блокирует, ручной дубль | `frontend/src/domain/{contribution,weekly-per-person}.ts` + `…/sprints/archive/SprintArchiveClient.tsx` | low-prio (вынести в util) |
| 2026-06-06 | **`ControlsBar.tsx` — разделители `bg-slate-600`** (жёсткий slate, нарушает правило парных цветовых токенов) | Оставлено вне scope B2/B3; заменить на токен `bg-border` | `frontend` `ControlsBar.tsx` ([[feedback_paired_color_tokens]]) | low-prio (заменить на `bg-border`) |
| 2026-06-06 | ТЗ-2: **прод-верификация ≤2-мин паузы пайплайна** не прогнана (composite-reconcile-крон реализован, default ON) + ack-first за флагом OFF не замерен | Нужен прод-доступ/живая встреча; код готов, флаги выставлены безопасно | [`plans/tz/2026-06-06-recording-pipeline-reliability-reconcile.md`](../../plans/tz/2026-06-06-recording-pipeline-reliability-reconcile.md) Ф5 + `docs/operations/prod-deploy-log.md` §«Стабильность прода» Шаг 12 | владелец (после выката) |
| 2026-06-06 | ТЗ-3: **forced `tool_choice` + `strict:true` для deepseek НЕ активированы** (флаг `LLM_DEEPSEEK_FORCE_TOOL_CHOICE_ENABLED` default OFF; strict не добавлен) | Нужна прод-проба agent-lia (принимает ли прокси forced function / strict без 400); внешне-наблюдаемое поведение LLM-API. Корень потери связей уже закрыт Ф1+Ф2 (entity-graph устойчивость + router validate→secondary, guard сам откатит на 'auto') | [`plans/tz/2026-06-06-graph-arbiter-json-resilience.md`](../../plans/tz/2026-06-06-graph-arbiter-json-resilience.md) Ф3/Ф4 | владелец (прод-проба → включить флаг/добавить strict) |
| 2026-06-06 | ТЗ-4 Ф4: **capable-модель `deepseek-v4-pro` на task-экстракции НЕ включена** (patch-скрипт готов, не в авто-выкате) | Бюджетное решение — pro дороже flash; route admin-editable, владелец флипает и мерит. Измеритель — ТЗ-6 baseline | `backend/scripts/patch-task-extractor-route-pro.ts` + `docs/operations/prod-deploy-log.md` §«Опциональные ручные операции» | владелец (cost-decision) |
| 2026-06-06 | ТЗ-6: **baseline качества извлечения НЕ снят** (golden-харнесс реализован, runner требует живой backend+LLM) | Прогон `agent-quality-harness.ts` на стенде (LLM-вызовы стоят денег, prod-guard) — приёмка ТЗ-4 (ДО/ПОСЛЕ ASR-ноты+дедупа) | `backend/scripts/agent-quality-harness.ts` (ТЗ-6) | владелец/разработчик (прогон на стенде) |
| 2026-06-07 | **NUL-байт в `makeExternalId`** (`h.update('\0')` вместо разделителя) — `meeting-extract-actions.service.ts:386`, pre-existing; влияет на sha1-разделитель `externalId` IntakeIssue | Вскрыт при ТЗ-4 Ф3 (rg видит файл бинарным). Ретроактивная правка сменит хэши существующих IntakeIssue (риск дублей) → осознанное решение, не косметика | `backend/src/modules/tracker/services/meeting-extract-actions.service.ts:386` | владелец (решение по миграции externalId) |
| 2026-06-06 | 🟡 **Боковое превью встречи в журнале — англицизмы** «Summary/Action items/host» (полная `/result` уже по-русски) + тип встречи «Team sync» (англ.); мелочи `/intake` «2 меток»/«email»/«AI» | Модуль превью (`MeetingDetailPane`) не переведён на русский structured-report; точечные строки | [`plans/analysis/2026-06-06-retest-RESULTS-technical.md`](../../plans/analysis/2026-06-06-retest-RESULTS-technical.md) §6 | low-prio (фронт-проход) |
| 2026-06-08 | **ТЗ D Vox word-timings — исход б/в не финализирован** (парсер расширен на `extendedResult` — исход «а», `accdfe7b`; но содержимое extendedResult неизвестно: если тайминги там — уже берутся; если пуст — нужен submit-флаг или смена модели) | Диагностика `vox.no_words` теперь логирует `extendedResultKeys`/`taskParamsKeys` (PII-safe) — следующая реальная COMPLETED-встреча без таймингов ПОСЛЕ выката покажет исход; имя submit-параметра придёт из `taskParamsKeys` (не угадывать — language→400) | [`plans/tz/2026-06-07-asr-word-timestamps-duration-behavior.md`](../../plans/tz/2026-06-07-asr-word-timestamps-duration-behavior.md) | владелец/разработчик (прочитать `extendedResultKeys` после выката) |
| 2026-06-08 | **Ф2 — частично:** C4 широкий few-shot (сделан только process-template; ~15 промптов без few-shot не трогали); C3 булев гейт для regulation/insight (decision/idea сделаны; у этих нет детерминированного fallback → recall-риск); C2 meetingDateIso для пер-блочных специалистов (meeting-report-fast сделан) | No-golden: ширпотреб few-shot/гейтов без замера рискует тихой деградацией recall; решено наблюдать прод по `core_extraction_entity_total{type}` после выката сделанного, доработку — по факту | [`plans/tz/2026-06-08-agent-chain-remaining-no-golden.md`](../../plans/tz/2026-06-08-agent-chain-remaining-no-golden.md) п.1 | low-prio (по факту прод-наблюдения) |
| 2026-06-08 | **Ф6 Решение Б (shared-prefix транскрипта) НЕ реализовано** (Часть 3 smoke cache-hit-ratio WARN СДЕЛАНА `f28200cb`) | Решение Б = router-wide реструктуризация (роль SYSTEM→хвост USER + общий префикс транскрипта), нужен прод-замер поведения прокси по `cache_control` + риск регрессии индивидуальной настройки per-agent SYSTEM. Smoke-метрика `z_llm_cache_hit_ratio` теперь измеряет эффект | отдельное ТЗ `cache-prefix-everywhere` ([`02_architecture/llm-cache-status.md`](../02_architecture/llm-cache-status.md)) | владелец (по smoke-метрике решить, нужен ли Б) |
| 2026-06-08 | **ТЗ B Фаза 4 — остался ops-хвост:** ollama-401 (валидный `OLLAMA_API_KEY`/URL ЛИБО деактивация ollama-провайдера в админке) + флип `LLM_DEEPSEEK_FORCE_TOOL_CHOICE_ENABLED` ON с замером доли fallback арбитра графа | Код-часть закрыта (`c457403d`: Express5 named-wildcard + JSON-резилиенс speaker/intake). Остаток — прод-конфиг (внешнее) + замер внешне-наблюдаемого поведения LLM-API | [`plans/tz/2026-06-07-provider-smoke-test-and-alerting-fix.md`](../../plans/tz/2026-06-07-provider-smoke-test-and-alerting-fix.md) Фаза 4 | владелец (прод-конфиг) + замер |
| 2026-06-08 | **Новые флаги по умолчанию OFF ждут флипа+наблюдения:** `meetings.taskDedupeEnabled` (семантический дедуп задач, `a933138e`), `goals.goalTaskLinkEnabled` (привязка Goal↔Task, `e174baaa`) | Data-affecting (дедуп удаляет fast-черновики; линкер пишет `Issue.goalId`) + новые LLM-арбитры без golden → безопасный дефолт OFF, владелец включает в админке и смотрит витрину/метрики (`z_task_dedupe_total`, `z_goal_task_link_total`) | [`plans/tz/2026-06-08-agent-chain-remaining-no-golden.md`](../../plans/tz/2026-06-08-agent-chain-remaining-no-golden.md) пп.3,4 | владелец (флип ON + прод-наблюдение) |
| 2026-06-09 | **ТЗ-4 Волна 3 — распознавание сканов (OCR) + конвертация через docling НЕ реализованы** (Волны 1–2 сделаны: форматы xlsx/pptx/html/rtf/odt/csv, мультифайл, дедуп, привязка, импорт ZIP/Notion/Confluence, AI-подсказка) | OCR (`tesseract.js`) + `docling`-сервис — отдельная инфраструктура: docling требует `DOCLING_SERVICE_URL` от владельца (внешний HTTP-микросервис в `infra/*`), а у сканов-регламентов открыт вопрос свежести/владельца документа (кто отвечает за актуальность распознанного) | ТЗ [`plans/archive/2026-06-08-manual-document-upload-and-import-tz.md`](../../plans/archive/2026-06-08-manual-document-upload-and-import-tz.md) (Волна 3) | needs-owner (`DOCLING_SERVICE_URL` + регламент свежести) |
| 2026-06-09 | **DocumentApi-поля `uploaderName`/`attachedRoleName`/`sizeBytes`/`parsedAt` не отдаются backend'ом** — фронт ожидает их, но `toDocumentDto` их не кладёт → в UI рендерятся «—» | Вскрыто при S3.1 фронт-волне; backend `toDocumentDto` не расширили этими полями (контракт фронта забегает вперёд бэка). Косметика отображения, не блокирует загрузку/привязку/граф | `backend/src/modules/documents/documents.service.ts` (`toDocumentDto`); `frontend/src/api/documents.api.ts` | low-prio (расширить `toDocumentDto`) |
| 2026-06-09 | **officeparser `postinstall` блокируется Bun** в dev-окружении — нужно проверить, что нативная установка проходит на прод-сборке Docker | officeparser v7 имеет postinstall-скрипт; Bun в этом окружении его не выполнил. На прод-сборке (Dockerfile, node-окружение) поведение может отличаться — проверить, что парсер pptx/rtf/odt реально работает после `docker compose up -d --build` | `backend/package.json` (officeparser); ТЗ [`plans/archive/2026-06-08-manual-document-upload-and-import-tz.md`](../../plans/archive/2026-06-08-manual-document-upload-and-import-tz.md) | проверить на прод-сборке (smoke: загрузить .pptx) |
| 2026-06-09 | **ТЗ-5 vNext:** загрузки >5 ГБ (нет multipart-upload), транскод не-нативного **видео** в mp4 (сейчас нормализуется аудио-дорожка ffmpeg), Vox `role_labeling`/prefill подсказок (нет авто-предзаполнения имён говорящих), split одной диаризационной реплики на двух говорящих | MVP ручной загрузки встречи: presignPut одним PUT (≤ лимита), извлекается аудио для Vox, разметка говорящих чисто ручная (merge есть, split нет). Остальное — отдельные доработки | ТЗ [`plans/archive/2026-06-08-meeting-upload-diarized-speaker-mapping.md`](../../plans/archive/2026-06-08-meeting-upload-diarized-speaker-mapping.md) §«Не входит» | vNext (владелец) |
| 2026-06-09 | **`ParticipantPicker` user-vs-person edge при подписи говорящего** — при подтверждении (`POST /speakers/confirm`) сотрудник резолвится через `userId→personId` fallback; не проверено на проде, что связка не теряется для участника без `Person` | Вскрыто в ТЗ-5 Ф4: говорящий-сотрудник создаёт/привязывает `Person` (external company/jobTitle) и `Participant`; путь user→person работает в коде, но боевой случай (приглашённый user без Person в этой Org) не пройден на проде | `backend/src/modules/meeting-uploads/meeting-uploads-speakers.service.ts`; ТЗ [`plans/archive/2026-06-08-meeting-upload-diarized-speaker-mapping.md`](../../plans/archive/2026-06-08-meeting-upload-diarized-speaker-mapping.md) Ф4 | проверить на проде после выката |
| 2026-06-09 | **value-recap-export не имеет отдельного флага** — экспорт слайдов/печать на `/dashboard/value-recap` (S2.6) read-only поверх уже гейтнутого `operations.value_recap.enabled` | Осознанное решение оркестратора: экспорт ничего не пишет и не отправляет (read-only поверх существующей витрины), отдельный флаг плодил бы лишний тумблер. Сам генератор слайдов (PPTX) — отдельная строка выше (2026-06-08) | `backend/src/modules/operations/utils/value-recap-export.ts`; ТЗ [`plans/tz/2026-06-08-dashboards-info-rework.md`](../../plans/tz/2026-06-08-dashboards-info-rework.md) S2.6 | by-design (read-only) |
| 2026-06-09 | **QA-обход новых экранов не проведён** (`/dashboard` 7-величин, `/dashboard/portfolio`, `/dashboard/value-recap`, `/me` 9-виджетов, `/meetings/upload`, `/meetings/[id]/speakers`) | Ветка ещё не выкачена на прод — на `meet.crossmark.ru` сейчас СТАРЫЙ код; визуальная приёмка глазами имеет смысл только после прод-выката (qa-tester ходит в прод) | ветка `feature/2026-06-08-daily-value-dashboards-uploads` | владелец (после прод-выката → qa-обход) |
| 2026-06-08 | **ТЗ-3 светлая тема дашбордов** — modern-язык сделан в тёмной теме (стекло/градиент/объём), светлую владелец задизайнит отдельно | Светлую тему владелец взял на себя (Ф проектирования вне scope ТЗ-3); modern-токены `tokens.css` готовы под обе темы, но светлые значения градиентов/стекла не финализированы | ТЗ [`plans/tz/2026-06-08-dashboards-redesign-modern-visual-language.md`](../../plans/tz/2026-06-08-dashboards-redesign-modern-visual-language.md) | владелец (дизайн светлой темы) |
| 2026-06-08 | **Осознанно OFF (решения владельца) при Ship-On-выкате дефолтов:** `llm.budget.enforce_enabled` (реализована только жёсткая блокировка, без hard-лимитов эффект ноль, с лимитами — режет весь ИИ компании) и `goals.pulse.deliver_to_telegram` (внешняя рассылка людям — риск спама) | ТЗ enable-shipped включил 4 готовых флага дефолтом, эти два оставлены OFF: budget enforce необратимо тратит/блокирует деньги (нужен параметр-лимит владельца + выбор поведения А/Б/В), telegram-пульс — внешне-наблюдаемое действие (нужно явное «да»). Knowledge-access enforce — отдельная строка выше (засеяна пустой матрицей) | [`plans/tz/2026-06-08-enable-shipped-features-by-default.md`](../../plans/tz/2026-06-08-enable-shipped-features-by-default.md) §3 + [`docs/operations/feature-flags.md`](../../docs/operations/feature-flags.md) | владелец (задать лимит / дать «да» на ТГ) |
| 2026-06-10 | **Индекс `@@index([tenantId, createdAt])` на `BlockerSynthesis`/`EntityLink`** для `weeklyInflow` (Ф1 доводки редизайна) | Отложен: добавлять только если `EXPLAIN` покажет seq-scan на крупном tenant (ТЗ §Б, прецедент `buildSparkline` — сначала reuse `findMany`+in-memory bucket, индекс — преждевременен). Сейчас ряд считается тем же паттерном, что уже работает в проде | [`plans/tz/2026-06-09-dashboards-redesign-completion-full-dataviz.md`](../../plans/tz/2026-06-09-dashboards-redesign-completion-full-dataviz.md) (Ф1 §«Что НЕ входит» + Риски) | владелец / перф-замер (после прод-выката → `EXPLAIN`) |
| 2026-06-09 | **Служба поддержки Ф5 (авто-отправка клиенту) + Ф6 (тон-адаптер) не реализованы** (Ф1–Ф4 готовы: деск + закрытый контур R-INV-1 + клон-черновик + петля обучения + ночной куратор). Ф5 — клон отвечает клиенту сам за гейтом calibrated-уверенности+groundedness (флаг `SUPPORT_CLONE_AUTOSEND_ENABLED`). Ф6 — DPO/LoRA тон-адаптер на парах `editType='tone'` | Ф5: нужен отдельный owner-go + решение про раскрытие AI клиенту (Р-5, юр-риск EU AI Act / Air Canada только у авто-отправки) + калибровка на исходах; в Ф1–Ф4 человек шлёт ВСЕГДА. Ф6: отдельное ТЗ, ждёт накопления чистых классифицированных `tone`-пар; обучающий фреймворк не в прод-пути Z | [`plans/archive/2026-06-09-support-desk-clone-and-closed-contour-tz.md`](../../plans/archive/2026-06-09-support-desk-clone-and-closed-contour-tz.md) (Ф5/Ф6) + [[../01_projects/support-desk]] | владелец (Ф5 — owner-go + раскрытие AI; Ф6 — отдельное ТЗ после датасета) |
| 2026-06-11 | **intake-project-picker — код НЕ начат (0 из 3)**: коммит `98f1f10e` положил только markdown ТЗ. Нет: модалки создания проекта во «Входящих» (Ф1), серверного автогена `slug/identifier` (Ф2 — сейчас обязательны в DTO), калибровки автотриажа (Ф3 — description в контекст + fuzzy-match по имени + метрика `ai_intake_project_resolved_total`) | ТЗ опубликован, реализация не запускалась; блокеров нет (фронт-модалка + бэк-хелпер, решения в ТЗ) | ТЗ [`2026-06-10-intake-project-picker-and-triage-calibration.md`](../../plans/tz/2026-06-10-intake-project-picker-and-triage-calibration.md); аудит [`2026-06-11-tz-implementation-audit-last-2-days.md`](../../plans/analysis/2026-06-11-tz-implementation-audit-last-2-days.md) | ready-to-implement (можно сейчас) |
| 2026-06-11 | **Промпты-MASTER A4: few-shot для insight-экстрактора НЕ добавлен** (A1.2 гейт `isOrgNorm` + версии/steps-sync + 11 few-shot по орг-агентам сделаны) | Широкий few-shot без замера рискует тихой деградацией recall на insight'ах; решено ждать явного «да» владельца на расширение примеров именно по инсайтам | ТЗ [`2026-06-11-prompt-polish-fewshot-org-and-reports.md`](../../plans/tz/2026-06-11-prompt-polish-fewshot-org-and-reports.md) (A4) | needs-owner-go (владелец «да») |
| 2026-06-11 | **ASR word-level (пословные) тайминги — Tier1 + backfill старых встреч** (сегментные тайминги Vox теперь персистятся+мержатся — см. «Закрытые»). Остаётся: запросить у `agent-lia` word-level вывод Vox (имя submit-флага неизвестно) + backfill уже обработанных встреч на посегментный мердж | Word-level у `v3_e2e_rnnt` нет by design; нужен запрос к провайдеру + прод-проба флага (угадывать нельзя, как `language`→400). Backfill старых встреч — отдельный прогон (сегменты тогда не сохранялись) | ТЗ [`2026-06-11-asr-segment-timings-persist-and-merge.md`](../../plans/tz/2026-06-11-asr-segment-timings-persist-and-merge.md) §«Не входит» | needs-owner-go (запрос к agent-lia) + backfill |
| 2026-06-11 | **Кабинет D5: слияние «Входящие» + «Подтверждения» в один раздел** — отложено владельцем (две точки входа в навигации) | Владелец отложил объединение; не входило в acceptance Волн 1–2 | ТЗ [`2026-06-11-cabinet-inbox-nav-ui-honesty.md`](../../plans/tz/2026-06-11-cabinet-inbox-nav-ui-honesty.md) (D5) | needs-owner-go (владелец отложил) |
| 2026-06-11 | **Кабинет D8: уведомление «встреча без записи — анализа не будет»** — встреча, где хост не включил запись, завершается без транскрипта/отчёта и не сообщает пользователю | Нет канала `notifications` под этот случай — нужно отдельное ТЗ (куда и как доставлять «запись не велась»). Бьёт по доверию | ТЗ [`2026-06-11-cabinet-inbox-nav-ui-honesty.md`](../../plans/tz/2026-06-11-cabinet-inbox-nav-ui-honesty.md) (D8); дубль-контекст — строка 2026-06-05 выше | отдельное ТЗ (канал уведомления) |
| 2026-06-11 | **Кабинет: консолидация `pluralRu`** (несколько копий хелпера склонения) — вынести в общий util | Опц.: ручной дубль, не блокирует; пересекается со строкой 2026-06-06 про `pluralRu` | ТЗ [`2026-06-11-cabinet-inbox-nav-ui-honesty.md`](../../plans/tz/2026-06-11-cabinet-inbox-nav-ui-honesty.md) | low-prio (вынести в util) |
| 2026-06-11 | **Документы: колонка `parsedAt` в DTO/UI** — фронт ждёт `parsedAt` (когда документ распознан), backend `toDocumentDto` его не кладёт → «—» | Косметика отображения; пересекается с DocumentApi-дрейфом (строка 2026-06-09) — закрывается тем же расширением `toDocumentDto` | `backend/.../documents.service.ts` (`toDocumentDto`); ТЗ [`2026-06-11-cabinet-inbox-nav-ui-honesty.md`](../../plans/tz/2026-06-11-cabinet-inbox-nav-ui-honesty.md) | low-prio (расширить `toDocumentDto`) |
| 2026-06-11 | **ChatBox Ф0 — прод-патч включения анализа** (`patch-enable-chatbox-analysis.ts --apply`) для уже подключённых интеграций, в т.ч. «Ооо луа» (~215 чатов, анализ не запускался) | Дефолт `analysisEnabled=true` уже выкатан кодом; существующие подключённые интеграции включит только прогон патча (в STEPS `phase:'patch'`, `--mode update`) | ТЗ [`2026-06-11-chatbox-memory-finishing-and-tasks-from-chat.md`](../../plans/tz/2026-06-11-chatbox-memory-finishing-and-tasks-from-chat.md) (Ф0); `docs/operations/prod-deploy-log.md` §«Опциональные ручные операции» | needs-deploy (прогон патча на проде) |
| 2026-06-11 | **ChatBox sendMessage (A3/Ф4): боевая форма ответа POST не зафиксирована** — чистый парсер `extractChatboxOutboundId` готов (best-effort), но точную форму JSON-ответа ChatBox на отправку даёт только 1 реальная отправка | В dev WARN `sendMessage` не воспроизводится; нужна живая интеграция (отправить ответ менеджера из Коры в реальный чат), чтобы зафиксировать ключ id в ответе | парсер в `chatbox-chats.service`; ТЗ [`...chatbox-memory-finishing...`](../../plans/tz/2026-06-11-chatbox-memory-finishing-and-tasks-from-chat.md) (Ф4) | needs-owner-go (1 живая отправка владельцем) |
| 2026-06-11 | **Свайп внутри мобильного экрана (Mobile Ф8/B7) НЕ реализован** — горизонтальные жесты между подэкранами | Опц., низкий приоритет: навигация работает через bottom-tabs `MobileShell`; нужна доп. зависимость `embla-carousel-react` | ТЗ [`2026-06-11-mobile-cora-exec-manager.md`](../../plans/tz/2026-06-11-mobile-cora-exec-manager.md) §Ф8 | low-prio (владелец/разработчик) |
| 2026-06-11 | **pluralRu консолидация (Кабинет Ф10/C2) — отложена** | owner Р7: фоновая уборка (несколько копий хелпера склонения), нулевая видимая ценность, риск регрессии в `contribution.ts` | ТЗ [`2026-06-11-cabinet-inbox-nav-ui-honesty.md`](../../plans/tz/2026-06-11-cabinet-inbox-nav-ui-honesty.md) §Ф10 | владелец (owner-deprioritized) |
| 2026-06-11 | **Промпты A4 — insight few-shot (gate `isInsight`) НЕ добавлен** | ТОЛЬКО по явному «да» владельца: широкий few-shot без замера рискует тихой деградацией recall на инсайтах | ТЗ [`2026-06-11-remaining-handoff-finishable-now.md`](../../plans/tz/2026-06-11-remaining-handoff-finishable-now.md) §D | needs-owner-go (владелец «да») |
| 2026-06-11 | **VAPID-ключи в проде НЕ заданы** — без них браузерный web-push (вкл. новый `ExecMorningPushCron`) graceful no-op | Первый крон, реально вызывающий `enqueuePushSend`, появился в этом пакете; без `VAPID_*`/`NEXT_PUBLIC_VAPID_PUBLIC_KEY` отправка молча не идёт (ошибки нет) | `docs/operations/prod-deploy-log.md` Шаг 1 + `docs/operations/feature-flags.md` §VAPID | владелец (`npx web-push generate-vapid-keys` → прод-`.env`) |
| 2026-06-11 | **Уведомление «встреча без записи — анализа не будет» (C1/Ф9) — реализация ждёт владельца** | ТЗ написано, но не выбран канал доставки «запись не велась» (нужно решение владельца куда и как) | ТЗ [`2026-06-11-meeting-no-recording-notification.md`](../../plans/tz/2026-06-11-meeting-no-recording-notification.md) | needs-owner-go (канал доставки) |
| 2026-06-11 | **Слияние «Входящие» + «Подтверждения» (D5/C3) — ждёт продуктового решения** | mini-design написан, но объединение двух точек входа меняет навигацию — нужно решение владельца | design-doc [`2026-06-11-inbox-actions-merge-mini-design.md`](../../plans/analysis/2026-06-11-inbox-actions-merge-mini-design.md) | needs-owner-go (продуктовое решение) |
| 2026-06-11 | **Мобильная Кора Ф8 (свайп) — единственный остаток** (Ф0–Ф7 сделаны: каркас + первый экран + exec/manager-разделы + голос в чек-ине + «Спросить»/«Память» + утренний web-push) | Свайп опц./низкий приоритет (см. строку B7 выше); Push (Ф7) подключён, но требует **VAPID-ключи в проде** (отдельная строка выше) | ТЗ [`2026-06-11-mobile-cora-exec-manager.md`](../../plans/tz/2026-06-11-mobile-cora-exec-manager.md); VAPID — prod-deploy-log Шаг 1 | low-prio (Ф8 свайп) + needs-owner (VAPID) |
| 2026-06-11 | **Probe Фаза 2 (LLM-judge ценности вопроса + семантический дедуп `pgvector` + полный graph-answer-search) и Фаза 3 (re-ask петля + память предпочтений тона)** — Фаза 1 (формулировка + дайджест + recheck + adaptive fatigue + видимое следствие) реализована | Нужны калибровочные данные `probe_outcome_total` из Ф5 (engagement/исходы) перед обучением judge'а ценности; порядок A→B→C доказан анализом §10 Р3 | ТЗ [`2026-06-11-probe-system-upgrade-phase1.md`](../../plans/tz/2026-06-11-probe-system-upgrade-phase1.md) + анализ [`2026-06-11-proactive-clarifying-questions-probe-research.md`](../../plans/analysis/2026-06-11-proactive-clarifying-questions-probe-research.md) | needs-owner-go (владелец после накопления метрики) |
| 2026-06-11 | **Отчёт→граф: регенерация отчёта НЕ обновляет граф** — повторная генерация отчёта той же встречи (новый `reportFastGeneratedAt`, тот же `endedAt`) даёт тот же `idempotencyKey` → обновлённые факты в граф не попадают; в графе остаются факты ПЕРВОЙ версии отчёта | Осознанное v1-ограничение: `occurredAt=meeting.endedAt` стабилен (НЕ `reportFastGeneratedAt`), чтобы регенерация не плодила дубль-RawEvent. «Отчёт завершённой встречи стабилен» — для MVP приемлемо. Re-ingest при регенерации (supersede старого RawEvent/блоков) — vNext, не меняя `occurredAt` | `backend/src/modules/ingest/adapters/report.adapter.ts` (`occurredAt`) + суб-ТЗ [`2026-06-11-report-to-graph-phase2.md`](../../plans/tz/2026-06-11-report-to-graph-phase2.md) §4.2 | vNext-ТЗ `report-graph-reingest-on-regen` — при необходимости (владелец) |
| 2026-06-12 | **Инструмент `create_task` у помощника в каналах НЕ включён** — «поставь задачу Пете» из Telegram пока идёт прежней веткой task-intent, не через мозг помощника | Требует политики `intake_issue`/`write` в RBAC для канального service-режима — решение владельца (кому из канала можно создавать задачи) | ТЗ-заглушка [`2026-06-12-assistant-create-task-tool.md`](../../plans/tz/2026-06-12-assistant-create-task-tool.md) | blocked-on-owner (policy intake_issue/write) |
| 2026-06-12 | **Probe Ф2.4 — авто-черновики ответов на probe** (Кора сама предлагает вариант ответа, человек только подтверждает/правит) | Зависит от шага «улучшаем промпты» (строка ниже) — без дошлифованных промптов авто-черновик плодит шум | ТЗ-заглушка [`2026-06-12-probe-auto-drafts-phase24.md`](../../plans/tz/2026-06-12-probe-auto-drafts-phase24.md) | blocked-on-owner (после шага «улучшаем промпты») |
| 2026-06-12 | **M-4: service-JWT канального ToolRouter обходит revoke сессий** — self-signed session JWT (60с, без jti) для loopback-вызовов из Telegram/MAX не проверяется по реестру живых сессий → разлогин пользователя не отрезает канал | Риск принят осознанно: токен живёт 60 секунд и минтится только при валидном `ChannelBinding`; настоящий offboarding обязан снимать `Membership` (тогда минт невозможен) | `backend/src/modules/concierge/services/tool-router.service.ts` (`mintServiceSessionToken`) | риск принят; ревизит при ужесточении offboarding |
| 2026-06-12 | **L-5: intake из email без rate-limit per `sourceEmail`** — злоумышленник может заспамить авто-приём задач письмами с одного адреса | Страховки уже есть: порог авто-приёма 0.75 + injection-guard; отдельный лимитер не делали в W4 | `backend/src/modules/tracker/workers/intake-auto-triage.worker.ts`; ТЗ [`2026-06-11-autonomy-remove-manual-confirmations.md`](../../plans/archive/2026-06-11-autonomy-remove-manual-confirmations.md) (W4) | low-prio (добавить лимитер при первом инциденте) |
| 2026-06-12 | **Стрим ответа помощника в Telegram** — ответ приходит одним сообщением после полной генерации (без постепенного «печатает…»-стрима) | Telegram Bot API не даёт SSE; editMessageText-эмуляция стрима — отдельная механика с rate-limit рисками | ТЗ [`2026-06-11-assistant-channels-telegram-max.md`](../../plans/archive/2026-06-11-assistant-channels-telegram-max.md) §«Не входит» | vNext (владелец) |
| 2026-06-12 | **Руководительские инструменты помощника сверх текущего реестра** (новые manager-tools в канальном whitelist) | v1 ограничен существующим реестром ToolRouter (SELF + manager по RBAC); расширение — по запросу владельца | ТЗ [`2026-06-11-assistant-channels-telegram-max.md`](../../plans/archive/2026-06-11-assistant-channels-telegram-max.md) §«Не входит» | vNext (владелец) |
| 2026-06-12 | **Чек-ин через помощника** — утренний план/вечерний отчёт по-прежнему идут прежней веткой `daily_checkin_self` (классификатор), не через мозг помощника | Осознанно: чек-ин-ветка стабильна и не зависит от `ASSISTANT_CHANNEL_ROUTING_ENABLED`; перенос в помощника — отдельная фаза | ТЗ [`2026-06-11-assistant-channels-telegram-max.md`](../../plans/archive/2026-06-11-assistant-channels-telegram-max.md) §«Не входит» | vNext (владелец) |
| 2026-06-12 | **Шаг «улучшаем промпты»** — промпты арбитра конфликтов / судьи confirm / probe-специалистов (3.1/3.4/3.9) / `probe-formulate` / триажа intake выкатаны первой редакцией, дошлифовка не делалась | Правки промптов — выкатываем и смотрим прод ([[../../docs/methodology/prompts/README|методология]] + feedback no-golden); дошлифовка пошагово вместе с владельцем по прод-метрикам (`z_conflict_arbiter_total`, `z_assistant_turn_total`, `probe_outcome_total`) | ТЗ [`2026-06-11-autonomy-remove-manual-confirmations.md`](../../plans/archive/2026-06-11-autonomy-remove-manual-confirmations.md) (Ф3.2/Ф4.3) + [`2026-06-12-probe-auto-drafts-phase24.md`](../../plans/tz/2026-06-12-probe-auto-drafts-phase24.md) | владелец (совместная дошлифовка по прод-наблюдению) |
| 2026-06-12 | **Клон: коммуникативный стиль / OCEAN-conditioning НЕ строится** — клон получил слой МЕТОДА (принципы/ценности/процедуры/маркеры, ТЗ clone-persona-method-layer реализовано целиком), но слой ФОРМЫ (styleProfile, тон) не делался | vNext по анти-scope основного ТЗ: вторично для роли («советник от должности», не имитация манеры речи), риск стилевого дипфейка; помечено «Ask first» — владелец решит отдельно | заглушка [`2026-06-12-clone-style-and-traits.md`](../../plans/tz/2026-06-12-clone-style-and-traits.md) | владелец (явное «да» на стилевой слой) |
| 2026-06-12 | **Клон: обучающий мост фидбека→DPO/LoRA + заполнение `SkillUsage.outcome`/`editDistance` НЕ сделаны** — петля evaluator'а PracticeSkill работает только на adversarial-компоненте (outcome/editDistance никто не пишет — известный разрыв); фидбек на ответы клона не собирается | vNext по анти-scope основного ТЗ: сначала нужен датасет (фидбек + `CloneQueryLog`-трафик после выката); офлайн-дообучение — вне прод-пути Z, отдельная инфраструктура | заглушка [`2026-06-12-clone-learning-dpo-loop.md`](../../plans/tz/2026-06-12-clone-learning-dpo-loop.md) | владелец (после накопления фидбек-датасета) |
| 2026-06-12 | **Клон: snapshot-таймлайн persona + UI редактирования принципов носителем НЕ сделаны** — история версий persona копится, но витрины «как менялся метод роли» нет; носитель не видит свои `RolePrinciple` и не может оспорить формулировку (контроль только post-hoc у manager/admin) | vNext по анти-scope основного ТЗ: read-only витрина не блокирует слой метода; права носителя на правку — продуктово-этическая развилка (само-курация ломает revealed-preference) — решение владельца | заглушка [`2026-06-12-clone-snapshot-timeline.md`](../../plans/tz/2026-06-12-clone-snapshot-timeline.md) | владелец (права носителя + ≥3-4 недели истории снапшотов) |
| 2026-06-13 | **Редизайн кабинета: decision auto-implement только детерминированный** — авто-перевод решения в «внедрено» работает по задачам/outcomes; mention-based вариант (LLM по транскриптам — «решение упомянуто как сделанное») НЕ реализован | Детерминированный путь надёжен и без LLM-стоимости; mention-based — отдельный LLM-агент, выкатывать после прод-наблюдения детерминированного (no-golden, выкатываем→смотрим прод) | ТЗ [`2026-06-13-cabinet-redesign-rhythms-and-decision-queue.md`](../../plans/archive/2026-06-13-cabinet-redesign-rhythms-and-decision-queue.md) (Ф8.1) | vNext (владелец / после прод-наблюдения) |
| 2026-06-13 | **Редизайн кабинета: визуальная доводка светлой темы на проде** — светлая тема в коде есть (тема-зависимые поверхности `tokens.css`/`modern.css`/`tokens.ts`, переключатель), но оттенки графиков (recharts) на белом фоне не калиброваны на боевых данных | Ветка ещё не выкачена; визуальная приёмка глазами имеет смысл только после прод-выката (qa-tester ходит в прод). Корень контраста закрыт (был хардкод тёмного в `modern/tokens.ts`), остаётся доводка оттенков дата-виз | ТЗ [`2026-06-13-cabinet-redesign-rhythms-and-decision-queue.md`](../../plans/archive/2026-06-13-cabinet-redesign-rhythms-and-decision-queue.md) (Ф10/Р6) | владелец / qa-tester (после прод-выката) |
| 2026-06-13 | **Редизайн кабинета: виджет «Пульс людей» на /structure + maturity-деталь + связка должность /me↔/structure (Б-2) НЕ сделаны** | Вынесено за scope Ф0–Ф10 (профильная перестройка раздела «Структура»); отдельный проход | ТЗ [`2026-06-13-cabinet-redesign-rhythms-and-decision-queue.md`](../../plans/archive/2026-06-13-cabinet-redesign-rhythms-and-decision-queue.md); аудит [`2026-06-12-product-audit-cabinet-ui-dashboards.md`](../../plans/analysis/2026-06-12-product-audit-cabinet-ui-dashboards.md) (Б-2) | ready-to-implement (отдельный проход) |
| 2026-06-13 | **Редизайн кабинета: open_question-детектор не учитывает праздники** — счётчик `counters.open_question` (дни без ответа) считает только рабочие дни, праздничные не вычитаются → значение — нижняя оценка | Календарь праздников в коде нет; рабочие дни — достаточная аппроксимация для MVP, «нижняя оценка» безопаснее завышения | ТЗ [`2026-06-13-cabinet-redesign-rhythms-and-decision-queue.md`](../../plans/archive/2026-06-13-cabinet-redesign-rhythms-and-decision-queue.md) (Ф8.6) | low-prio (календарь праздников — при запросе владельца) |
| 2026-06-17 | **knowledge-core MASTER (волны 0–6): код в ветке `feature/knowledge-core-master`, ждёт прод-выката.** 30 переписанных промптов knowledge-core (code-промпты с code-fallback) + task-dedup (новые `task-dedup-arbiter`/`task-closure-verify`) + миграции/HNSW Goal/backfill эмбеддингов целей. Промпты едут с деплоем кода; сиды маршрутов и backfill — в `apply-prod-deploy.ts` STEPS | Реализация завершена и зелёная (typecheck/build/eslint/тесты), но ещё не выкачена; промпты выкатываем→смотрим прод (no-golden) | ТЗ [`2026-06-16-knowledge-core-MASTER.md`](../../plans/tz/2026-06-16-knowledge-core-MASTER.md) + [`2026-06-16-task-dedup-and-tracker-reconcile.md`](../../plans/tz/2026-06-16-task-dedup-and-tracker-reconcile.md); `docs/operations/prod-deploy-log.md` | needs-deploy (выкат + сиды task-dedup-маршрутов + `apply-postgres-init` Goal HNSW + backfill `backfill-goal-embeddings.ts`) |
| 2026-06-17 | **knowledge-core очереди — G5: DLQ + per-queue override `CORE_DEFAULT_JOB_OPTIONS` отложено** — единый дефолт job-опций (`attempts:5`, exp 5s, `removeOnComplete{age:24h,count:20000}`, `removeOnFail:false`) на все ~25 очередей `core.*`; нет dead-letter-очереди с алертом, нет дифференцированных attempts/backoff/circuit-breaker по типу нагрузки | Осознанная отсрочка (enhancement надёжности, не баг): острое класса уже снято в волнах аудита — Б32 (умножение ретраев meeting-report-fast) и Б33 (count 1000→20000, дедуп по jobId держится 24ч); потери failed-job нет (`removeOnFail:false`) | ТЗ-заглушка [`2026-06-17-knowledge-core-queue-dlq-per-queue-config.md`](../../plans/tz/2026-06-17-knowledge-core-queue-dlq-per-queue-config.md) | vNext (по явному «начни реализацию» владельца) |
| 2026-06-20 | **Real-DB e2e тест гонки писателей (combat-harness, 2 параллельных dispatch одного блока → 1 сущность, `db_conflict≥1`) не прогнан** — логика гонки/идемпотентности всех писателей сущностей (3-3/3-5/3-6/3-14, `specialists-combined`, `graph.service`) покрыта детерминированными unit-гардами, но сквозной прогон на ЖИВОЙ БД отсутствует | dev-postgres = pgvector/pg17 БЕЗ Apache AGE; полный конвейер требует AGE + LLM-доступ | ТЗ [`knowledge-core-silent-loss-reliability`](../../plans/tz/2026-06-20-knowledge-core-silent-loss-reliability.md) Ф6 / [`decision-materialization-idempotency-fix`](../../plans/tz/2026-06-20-decision-materialization-idempotency-fix.md) Ф5; [`docs/testing/test-inventory.md`](../../docs/testing/test-inventory.md) | CI-гейт (когда поднят полный стек) |
| 2026-06-20 | **Регуляции: `nextCardVersion` (`specialist-3-1-regulations.service.ts:1874`) — отдельное чтение версии (TOCTOU под гонкой)** — снимок версии может теряться при параллельном апдейте | Домен регуляционного ТЗ (T4), не в scope класса «тихая потеря» silent-loss | ТЗ [`2026-06-20-regulations-process-module-fix.md`](../../plans/tz/2026-06-20-regulations-process-module-fix.md) | передано в регуляционное ТЗ (T4) |

## Закрытые (архив)

| Дата закрытия | Что было | Коммит |
|---|---|---|
| 2026-06-29 | **«Месяц»-герой как третий ритм брифинга** (контент месячного executive-разбора) | **РЕАЛИЗОВАНО** ТЗ [`2026-06-29-month-company-monthly-brief.md`](../../plans/tz/2026-06-29-month-company-monthly-brief.md) Ф1–Ф6: модель `MonthlyOperationsDigest` + `MonthlyDigestService` (свод 4 недель) + `OperationsMonthlyDigestCron` + `MonthCompanyHero` на `/month` (вердикт/тренд по неделям/письмо/компас+темп/таблица/решить-фокус) + доставка. Ветка `feature/month-company-and-report-navigation`, коммиты `9c36a0b6`..`f13f57a7`. Рефлексия [[../05_история/2026-06-29-month-company-and-navigation]] |
| 2026-06-28 | **Схлопывание 3 движков чата (chat-v2 / concierge / legacy) на один Мастер** — фронт-консолидация поверхностей чата | **РЕАЛИЗОВАНО** (ТЗ `2026-06-25-edinyy-pomoshnik-chat-surface-convergence.md`, КФ1–КФ5): десктоп `/chat` = `MasterChatHome` (concierge, список диалогов+история+стрим+действия+`asOf`); переиспользуемый `MasterScopedChat`, `IssueChat`+`MobileAskClient` → тонкие обёртки; concierge body += `scope/scopeRefId/asOf`, list += `titlePreview`, SSE += `citations`/`confirm_required`; двери Р1 (nav «Спросить» лидерам)/Р3 («Память»→Мастер), FAB → навигация в /chat; Cmd+K «?» → concierge; удалены 9 сирот (`ChatV2Client`/`OrgChatPanel`/`AssistantClient`/`ConciergeChat`/`ConciergeClonesTab`/`ConciergeSlot`/`chat-v2/ChatPanel`+specs). Коммиты `7d111a94`..`c5b7ec10`, typecheck/lint/build/vitest зелёные back+front. **Хвосты в «Открыто»:** `MeetingChatPanel`-миграция, pin/archive/feedback дома, визуальная приёмка прода. Рефлексия [[../05_история/2026-06-28-chat-surface-convergence]] |
| 2026-06-26 | **Полный дроп legacy-модели `Task` — унификация задач на `Issue`** (зонтик: двойной импорт meeting_legacy↔meeting + дроп Task + двунаправл. дедуп) — задачи жили в двух параллельных моделях БД (`Task` + `Issue`), миграция плодила третью копию `meeting_legacy` в обход дедуп-гейта | **РЕАЛИЗОВАНО** (ТЗ `2026-06-25-drop-legacy-task-model-unify-on-issue.md`, Ф0–Ф10): `Task`/`TaskStatus`/FK/`TaskSource.taskId` снесены (миграция `20260625000000`), миграция данных с дедуп-гейтом против spine-Issue + починкой маппинга + collapse дублей (raw SQL, guard, в `runSchemaPhase` pre-migrate ДО дропа), все писатели (writeTasks/cross-source/task-evidence)/читатели(аналитика/shares/provenance/public-api/chatbox-метрика) на Issue, модуль `tasks/` удалён, фронт-вкладка встречи на tracker (`?linkedMeetingId`), 5 флагов-распорок убраны, GIN-индекс `Issue.linkedMeetingIds`. Ветка `feature/drop-legacy-task-unify-issue`, коммиты `e7c8519b`..`ec41a025`. typecheck/build/lint back+front зелёные, 6468 тестов. **Ждёт прод-выката** (`apply-prod-deploy.ts --with-schema` сам переносит данные → дроп; см. `prod-deploy-log.md`). Рефлексия [[../05_история/2026-06-26-drop-legacy-task-unify-on-issue]] |
| 2026-06-25 | **Онбординг: demo-org 403 + 4 вторичных бага** — опросник Блока A писал в read-only демо-оргу (`getMe` demo-приоритет) → 403/мёртвые кнопки/зацикливание/не заходило в кабинет (2 реальных юзера застряли, вкл. `callintellect24`); вторичные: WizardShell вечный Skeleton, гонка редиректа step-6, потеря/затирание ответов мультивыбора при «Назад», тихий пропуск Document в `completeWelcome` | Основной `40ce79bd` (`getMe firstOwnedMembership ?? demoMembership` + фронт-тосты; **выкачен, онбординг пройден end-to-end на проде**); вторичные `6d2877b5` (WizardShell без латч-флагов · `refresh()`→user+confirm+`replace` · `GET /orgs/:id/welcome`+префилл 4/5/6 · 400 на демо-орге + warn). Письмо/сброс пароля — домен уже korateam.ru, присланное письмо старое (04.06). Рефлексии [[../05_история/2026-06-25-onboarding-demo-org-403-fix]] · [[../05_история/2026-06-25-onboarding-secondary-fixes-and-email-check]] |
| 2026-06-23 | **Задачи фрагментированы / не на спайне знаний** — извлечение задач из текстовых каналов (chatbox/telegram/tracker/email) шло кустарными путями (свои substring-резолверы исполнителя, дублированный дедуп, обход графа), не через общий спайн `RawEvent → IdeaBlock → специалист` | **РЕАЛИЗОВАНО** (unified-task-extraction, strangler-fig, 4 фазы): специалист `3-15-tasks` (`action_item` → LLM `task-extract` → IntakeIssue), единый `AssigneeResolverService`, единый дедуп с LINK-семантикой + advisory-lock race-guard, Task = пред-слой с промоутом Task→Issue. Анализ-задел `33f93401`; реализация `72249ebe` (Ф1) / `ee6ef1fa` (Ф2) / `658b3194` (Ф3) / `3dbd2627` (Ф4) + `e17a3cf6` (chatbox-no-stuck) / `764bd39c` (chain-test). Ветка `feature/unified-task-extraction`. **Ждёт прод-выката** (2 миграции авто + сиды агрегатором; см. `prod-deploy-log.md`). vNext-хвосты (дроп Task, слияние meeting-extract, sweepIntervalMin, 2 LOW дедупа, двунаправл. дедуп) — отдельными строками в «Открыто» |
| 2026-06-22 | **Остаток ТЗ `meeting-to-tracker-and-models-unified-fix` (сверх блоков A–D параллельной сессии):** корень авто-триажа «фильтр 4» (задачи встречи висли в `/intake`, не доходили до трекера) + системные баги конвейера F1–F8 (идемпотентность combined-пути решений; proxy-400 «json»; урезанный combined-путь идей/решений; vox `no_words`/`empty`; неотранскрибированная completed-встреча; `Goal:0`/no_owner; ложная диагностика AGE при провале LLM; quality-score не считался) + извлекающие модели на Pro (E) + diag-наблюдаемость (G) + решения встречи в UI (D5) | A2 `df071d7a` (триаж meeting→Issue ВСЕГДА, replay 0→7 PASS); F1+F3 `c155ce45` (combined `decision.upsert` + полнота triage/embedding/weight/supporters); F6 `8c388e7b` (goal no_owner-фолбэк + пороги AdminSetting); F5 `d606fbc5` (cron `track-egress-watchdog`); F2 `924a848d` (proxy-400 json). 14 коммитов, миграций/ENV нет, kill-switch ON. **Покрытие, не отдельные строки «Открыто»:** «фильтр 4»/F3/F5/F6/неотранскрибир. жили внутри зонтика строки `tasks-subsystem-unified-fix` (строка 34, ждёт прод-выката); F1/combined — внутри строки «Реестр решений молча пуст» (Decision=0, ждёт прод-выката); proxy-400 отдельной строки не имел. Все ждут общего прод-выката (`apply-prod-deploy.ts` seed-base+patch + `docker compose up -d --build`), см. `prod-deploy-log.md` |
| 2026-06-22 | **Помощник и chat-v2 представлялись «Корой», а не сотрудником КОНКРЕТНОЙ компании; не было переменной «описание компании» в промпте** (профиль компании в промпт) | **Слой 1 программы «Память субъекта»** (`c5fd6d49`/`7cde8e6e`/`4896a7ed`/`00798a4e`, ТЗ `plans/tz/2026-06-21-company-profile-autobuild-and-prompt-context.md`): `CompanyProfile.summaryJson`/`summaryPinned` (миграция `20260621170737`), `CompanySummaryCompilerCron` (топ canonical-IdeaBlock → LLM `company-summary-compile` → `applyAutoSummary`, защита pinned/fresh/cold-start), хвост «## О компании» в SYSTEM chat-v2+concierge (cache-friendly), UI `/company` + Switch «Закрепить», kill-switch `companyProfile.autoSummaryEnabled`. Ждёт прод-выката |
| 2026-06-21 | **Фиксатор (DailyCheckIn) не универсален** — детектор «план/отчёт» был заперт в Telegram-личке; переписки (Bitrix/chatbox/email/free_note) и реплики на встречах в чек-ин не превращались → сотрудник на дашборде «не сдал» | **Универсальный фиксатор реализован** (Ф1–Ф8, ветка `feature/universal-daily-checkin-fixator`, 8 коммитов `20ebbc77`..`75b569ce`): enum `DailyCheckInSource` +5 значений + `sourceContributions` (миграция `20260621104106`), `DaySignalExtractorService`/`DaySignalDetectorService`/`DaySignalAggregatorService`, taskType `day-signal-detect`, `DaySignalAggregatorCron` (дневной свип) + `MeetingCheckinListener` (`@OnEvent('meeting.ai_ready')`), `upsertFromDaySignal` (merge+`sourceRank`), крутилки `daySignals.*` (kill-switch ON). Ждёт прод-выката. vNext (выделенное UI-поле крутилок) — отдельной строкой в «Открыто» |
| 2026-06-17 | **Bitrix CRM посуточный дайджест (Ф4b)** — дайджест изменений CRM за день не строился | Дельта-синк `crm.{contact,company,deal,lead}.list` по `>=DATE_MODIFY` (+ колонка `modifiedAt`, курсор `lastCrmSyncAt`, `syncLeads`) + `BitrixIngestService.ingestCrmDigests` (посуточный детерминированный `fullText` → `RawEvent('crm-digest-<день>')`, без отдельного LLM-вызова) + триггер в `BitrixAnalyzeCron`. Окно `CRM_BACKFILL_DAYS=7` (решение владельца). Миграция `20260617030000_bitrix_crm_delta`. Заметки/таймлайн — отдельной строкой выше. |
| 2026-06-14 | **Петля `sourceBlockIds` + `DecisionTaskLink` из next-step НЕ замкнута** — задача из next-step встречи не несла блоки-источники и не линковалась в `DecisionTaskLink('derived')` | A10 master-fixes — новая колонка `IntakeIssue.sourceBlockIds` (миграция `20260614110154_add_intake_source_block_ids`) + резолв провенанса по canonical-блокам встречи (`BlockFetchService`, action-сигналы) в `createFromMeetingNextStep`/`meeting-extract-actions`; промоут intake→Issue прокидывает `sourceBlockIds` и создаёт `DecisionTaskLink(linkType='derived')` через общий хелпер `decision-task-link.util.ts` (ручной triage + авто-приём) |
| 2026-06-11 | **ChatBox Ф2 — виджет «Чаты в памяти»** (видимость результата анализа переписки в кабинете отсутствовала) | блок A `9fcdc26c` (ветка `feature/remaining-handoff`): новый эндпоинт `GET /api/v1/chatbox/integration/memory-summary` (counts dialogs/sessions/analyzed/inProgress/failed + blocks/tasks + analysisEnabled) + фронт-виджет `ChatboxMemorySummaryCard` на `/chats/integrations/chatbox` |
| 2026-06-11 | **ChatBox Ф3 — метрики по синку/анализу переписки** (наблюдаемости не было) | блок A `9fcdc26c`: prom-метрики `z_chatbox_syncs_total{scope,status}` / `z_chatbox_analyzes_total{status}` / `z_chatbox_pending_sessions` (gauge) / `z_chatbox_last_sync_ts_seconds{scope}` в `BusinessMetricsService` + инкременты в sync/analyze-воркерах и analyze-cron. 2 PromQL-алерта задокументированы в prod-deploy-log |
| 2026-06-10 | **«Кому видно» — доступ к видеовстречам** (раньше встреча видна только создателю; сотрудники не видели встречи владельца) | ТЗ `2026-06-10-meeting-visibility-who-can-see` Ф1–Ф6, ветка `feature/meeting-cabinet-fixes-2026-06-10` (5 код-коммитов): `Meeting.visibilityScope` + `MeetingAccessGrant` (миграция `20260610130000_meeting_visibility`), `MeetingVisibilityService.canView` на 6 READ-поверхностях + `resolveDirectGroupIds`, host-only `assertMeetingHost`, `GET/PATCH /meetings/:id/visibility`, фронт-контрол «Кому видно», kill-switch `MEETING_VISIBILITY_ENABLED`. Остаток (документы Д6 + факты Д7) — в «Открыто» |
| 2026-06-09 | **Ручная загрузка готовых документов — канал узкий** (только PDF/DOCX/MD/TXT, 1 файл, привязка к должности, нет импорта; колонка «Тип» всегда «другое») | ТЗ-4 Волны 1–2 (S3.1/S3.2), ветка `feature/2026-06-08-daily-value-dashboards-uploads`: форматы xlsx/pptx/html/rtf/odt/csv (officeparser+ExcelJS), мультифайл+дедуп `contentHash`+привязка(тема/проект/должность→граф), enum `DocumentType` + `Document.docType`, импорт ZIP/Notion (`POST /documents/import-zip`)+Confluence (`POST /documents/import-confluence`), AI-подсказка `document-attribution-suggest`, doc-citation в чате. Остаток (OCR/docling Волна 3) — в «Открыто» |
| 2026-06-07 | 🔴 ChunkLoadError рушил детальные страницы (Turbopack-prod, HTTP 200) + 🔴 видео-плеер Vidstack вечная крутилка | ТЗ-1 `a14fb1c9`/`286d33aa`/`e48b94e2` — webpack-сборка + `deploymentId` + русский error-boundary с авто-reload + нативный `<video>`, `@vidstack/react` удалён |
| 2026-06-07 | 🟠 Латентность пайплайна ~18-20 мин (потерянный/задержанный egress-вебхук) + бесконечный скелетон на `/result` | ТЗ-2 `9fe489d1`/`865fc2b6`/`ee01c644`/`40c1ee01` — composite-reconcile-крон (ограничивает ≤2 мин) + честный баннер «Отчёт готовится». Прод-верификация ≤2мин — в «Открыто» |
| 2026-06-07 | 🟠 Граф терял связи на невалидном JSON LLM-арбитра (молчаливая деградация, fallback-none без ретрая в entity-graph) | ТЗ-3 `56fc3cce`/`02c59ee8` — entity-graph `tryParseJson`+ретрай×2+метрики + router `validate`-callback→secondary. Forced tool_choice (флаг OFF, нужна проба) — в «Открыто» |
| 2026-06-08 | 🔴 Детальные страницы таблиц `/tables/[id]` не открывались (React #185 рендер-петля Zustand v5 на нестабильных селекторах) + `GET /api/v1/tables/pending-patches → 404` | ТЗ A `26219233` — `useShallow` на `selectVisibleProperties`/`selectVisibleRows` + `PendingPatchesController` зарегистрирован первым в `TablesModule` + регресс-тесты |
| 2026-06-08 | 🟡 Smoke-тест провайдеров шумел (`maxTokens:8` < floor 16 → ложный провал OpenAI каждые 30 мин) + алертинг падал 400 (payload не по схеме `system.message`) | ТЗ B `3a2d0ce4` — `SMOKE_MAX_TOKENS=64` + кламп `max_output_tokens>=16` в openai-proxy + payload алертов по схеме `system.message` + `runOnce` пропускает провайдеров без `baseUrl`. **Хвост Фазы 4 (ollama-401/Express5/JSON-резилиенс/tool_choice) — в «Открыто»** |
| 2026-06-08 | 🟡 Англицизмы и брендинг в UI: типы встреч «Team sync/Sales/Custdev», `<title>` без «— Кора», «AI» в кабинете, дубль `/chat`↔`/chat-v2` | ТЗ C `f1ca83f6` — `MEETING_TYPE_LABEL_RU` + `title.template '%s — Кора'` + убран «AI» + `/chat` рендерит `ChatV2` (`ChatClient` удалён), `/chat-v2` → redirect |
| 2026-06-08 | 🔴 **HIGH chatbox cross-attribution**: факты КЛИЕНТА из переписки писались как `role=subject=менеджер` и текли в его knowledge-клон | ТЗ clone-quality Ф1(A) / enable-shipped Часть B `dbf9b0d4` — атрибуция subject по говорящему сегмента (chatbox шлёт `transcript.turns` с per-message `authorPersonId`; клиент → subject не пишется) + backfill-очистка истории `backfill-chatbox-subject-cleanup.ts` `bb7701dc`. Встречи/одно-авторные источники без изменений |
| 2026-06-08 | 🟡 Качество клона: merge затирал confidence, decay ронял high→low за проход, фабрикованные черты сразу active, разрежённые данные не давали клона, near-dupes черт; ⚪ Долг прошлого — 4 готовых флага лежали OFF | ТЗ clone-quality 8 фаз `dbf9b0d4..4c28bea1` (confidence из дат · якорь · decay 1-шаг + класс-фикс крона · verify-гейт pending_verification · split-floor · арбитраж 0.78 · dedup persona · clone-respond) + ТЗ enable-shipped `bb7701dc` (4 флага ВКЛ дефолтом). Умные таблицы R1-R4 `e84d8ace`. Осознанно-OFF (budget/telegram) — в «Открыто» |
| 2026-06-08 | retest2 **no-golden** батч (ТЗ `2026-06-08-agent-chain-remaining-no-golden`): Ф1 idea direct-path (флаг ON) + guard 3.6 · Ф2 C1–C8 промпты (ASR/calibration/анти-галлюцинация имён/C3 гейт decision+idea/C8 entity-merge) · Ф4.1 goal-task-link (флаг OFF) · Ф5 Р2 task-dedupe (флаг OFF) · Ф6 smoke cache-hit-WARN · TZ B код-хвост (Express5 named-wildcard + JSON-резилиенс speaker/intake) · TZ D Vox extendedResult-парсер | `7430162e`·`0c283e60`·`6dd216f5`·`0dfe170f`·`a933138e`·`e174baaa`·`f28200cb`·`accdfe7b` (остатки — выше в «Открыто») |
| 2026-06-05 | **`IdeaBlockEntity.role='subject'` не писался нигде** → клоны (Specialist 3.7/ExecutablePersona), WHO-ось, дашборд-агенты читали пустую выборку | Ф1 `b4ac1ebd` — шаг `attributeSubject` в `block-ingest` + `resolveSubjectEntityId`/`ensurePersonEntity` + kill-switch `knowledge.subjectAttributionEnabled` + `backfill-subject-attribution.ts`. См. [[../02_architecture/knowledge-core]] §«Детерминированная subject-атрибуция» |
| 2026-06-05 | Identity участника встречи терялась: нет `Participant.personId`, `userId` обнулялся для не-host, speaker→Person связь не доходила | Ф0 `158a33d8` — `Participant` += `userId`/`personId`/`inviteToken`/`invitationStatus`/`invitedAt` + enum `ParticipantInvitationStatus`; `participant-context` отдаёт `userId` всем `isRegisteredUser`; speaker identity в `DialogTurn`. Приглашение сотрудников (Ф2–Ф3) подключено к встрече. См. [[../02_architecture/data-model]] §Participant |

---

[[../index|← index]] · [[../00_system/TASK_WORKFLOW|процесс работы над задачей]] · [[../05_история/|рефлексии]]
