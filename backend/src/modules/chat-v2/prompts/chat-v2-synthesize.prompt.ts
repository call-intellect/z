/**
 * SBA α-5 — основной system-prompt для chat-v2 synthesize.
 *
 * На α-5 мы используем prompt'ы из `knowledge-core/services/chat-v2.service.ts`
 * (BASE_SYSTEM_PROMPT там), поскольку именно тот сервис вызывает LLM.
 * Этот файл — placeholder для будущих mode-специфичных накладок (γ-1
 * clone_style, β-? стиль ответа в Telegram/email).
 *
 * TODO(owner-product): согласовать финальный текст с командой продукта —
 * см. зонтичный SBA §10 (промпты chat-v2).
 *
 * MODE_PROMPTS — текстовые добавки к system prompt в зависимости от mode.
 * SynthesisService подмешивает соответствующий блок в начало вопроса.
 */
export const CHAT_V2_SYNTHESIZE_MODE_PROMPTS = {
  factual: `Режим: «факты».
Отвечай ТОЛЬКО тем, что прямо сказано в найденных блоках.
Если в источниках нет ответа — честно скажи «не нашёл в памяти компании».
Каждое утверждение должно быть подкреплено маркером [BLOCK:id].`,

  synthetic: `Режим: «синтез».
Можешь обобщать данные из нескольких блоков, но строго маркируй уверенность:
- «по нескольким источникам» — если факт подтверждён 2+ блоками;
- «однажды было сказано» — если источник единичный;
- «возможно устарело» — если блок старше 6 месяцев или есть конфликт.
Все ключевые утверждения помечай [BLOCK:id].`,

  clone_style: `Режим: «клон сотрудника».
На α-5 этот режим не реализован — отвечай как в режиме synthetic с явной
пометкой «(clone-style будет в γ-1)».
TODO(γ-1): подключить Persona-промпт под конкретного сотрудника.`,
} as const;

/**
 * Базовый prompt уже живёт в `knowledge-core/services/chat-v2.service.ts`
 * (константа BASE_SYSTEM_PROMPT). Здесь — добавка про uncertainty/conflicts
 * для будущего synthesis сервиса. На α-5 не подключается напрямую к LLM —
 * SynthesisService использует встроенный prompt knowledge-core.
 */
export const CHAT_V2_SYNTHESIZE_SYSTEM_PROMPT_ADDON = `Если среди источников
есть противоречия — обязательно укажи это с маркерами обоих блоков
([BLOCK:id1] vs [BLOCK:id2]). Не сглаживай конфликты — это важная сигнатура
для curator'а.`;
