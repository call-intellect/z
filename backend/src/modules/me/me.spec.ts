import { describe, it } from 'vitest';

/**
 * TODO (Фаза 0a.4): покрыть тестами `MeService`:
 *   - getProfile возвращает null-поля если Person не создан;
 *   - возвращает active Role (validTo=null) с RoleProfile;
 *   - tenant isolation;
 *   - X-Org-Id переключение между Org возвращает разные profile.
 */
describe.skip('MeService', () => {
  it('TODO: покрыть `/api/v1/me/profile` для разных состояний', () => {
    // см. план 2026-05-21-phase-0a-data-model-and-graph-infra.md §7-§10
  });
});
