# LiveKit Egress — ЛОКАЛЬНАЯ РАЗРАБОТКА.
# Запуск: infra/livekit/egress-dev.sh up
#
# ВАЖНО: S3-конфигурация должна совпадать с S3_* в backend/.env!
# По умолчанию — prod S3 (regru.cloud).
# Для MinIO раскомментируй блок ниже.

api_key: devkey
api_secret: secretsecretsecretsecretsecretsecretsecretsecret

# LiveKit Server (запускается через livekit-dev.sh --network host)
ws_url: ws://localhost:7880

# Redis (docker-compose.dev.yml → localhost:56381)
redis:
  address: localhost:56381

# S3 — prod (те же кредиты что в backend/.env)
s3:
  access_key: RZZ7EBMPUSXPOVQS2BY9
  secret: o9Cv0RBnFAHNuz5pBSEDLoduVHVe4kBpxiwBqkHG
  region: ru-1
  bucket: crossmark
  endpoint: https://s3.regru.cloud
  force_path_style: true

# MinIO альтернатива (docker-compose.dev.yml bucket meetings-dev):
# s3:
#   access_key: z_minio_dev
#   secret: z_minio_dev_password
#   region: us-east-1
#   bucket: meetings-dev
#   endpoint: http://localhost:59000
#   force_path_style: true

logging:
  level: debug
