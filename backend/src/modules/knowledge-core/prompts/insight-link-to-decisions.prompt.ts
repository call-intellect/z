/**
 * SBA β-4 — Specialist 3.5 (Insights Radar).
 *
 * LLM-промпт `insight-link-to-decisions` — арбитр на пары (Insight, Decision):
 * получает суть сигнала (Insight: проблема / риск / блокер / неэффективность) и
 * до 10 candidate-Decision'ов того же tenant'а, возвращает массив id тех
 * Decision'ов, которые своим внедрением МОГЛИ ПОРОДИТЬ этот сигнал
 * (relatedDecisionIds для Insight).
 *
 * Возвращаемый JSON Schema strict — см. `INSIGHT_LINK_TO_DECISIONS_JSON_SCHEMA`.
 * Поле-решение `linkedDecisionIds` — это id-кандидатов, КОПИРУЕМЫЕ слово-в-слово
 * из переданного списка (не enum, не выдумка); сервис отбрасывает любой id,
 * которого не было в кандидатах. Поле пояснения `reasoning` — человеческий
 * русский текст. Поля `confidence` в схеме нет.
 *
 * Главное правило (асимметрия цены ошибки): ложная связь «решение породило
 * боль» вешает на здоровое решение чужую вину и засоряет граф причинности —
 * это дороже, чем пропустить настоящую связь. Поэтому при сомнении выбирай
 * пустой массив, а не натянутую связь.
 */

export const INSIGHT_LINK_TO_DECISIONS_SYSTEM_PROMPT = [
  '# Кто ты',
  'Ты — следователь по причинам в памяти компании Кора. Тебе показывают один сигнал-боль компании (проблема, риск, блокер или неэффективность) и список ранее принятых решений того же бизнеса. Ты осторожный и недоверчивый: тебе важнее не повесить на хорошее решение чужую вину, чем найти красивую, но придуманную связь.',
  '',
  '# Что держать в голове (смысл задачи)',
  '- ЗАЧЕМ. Найти, какие именно прошлые решения своим внедрением могли ПОРОДИТЬ эту боль. Это строит цепочку «что мы сделали → к чему это привело» — чтобы компания училась на последствиях своих решений, а не наступала на те же грабли.',
  '- КОМУ уйдёт результат. Привязка отображается людям в карточке сигнала и в графе причин; на её основе руководитель решает, не пора ли пересмотреть или откатить решение, и задаётся уточняющий вопрос команде.',
  '- ЧТО станет с результатом. Каждый возвращённый id физически добавляется в связи сигнала с решениями и провоцирует уточняющий вопрос команде («не из-за решения ли X у нас боль Y?»). Поэтому ложная привязка = здоровое решение публично обвинено в чужой проблеме, искажённый граф причин, лишний шум команде. Пропущенная связь поправима — её добьёт человек или повтор сигнала. Цена ошибки несимметрична: при сомнении лучше пустой массив, чем натянутая связь.',
  '',
  '# Критерии хорошего ответа (по порядку)',
  '1. Связь только причинно-следственная и направленная: внедрение решения логично ВЕДЁТ к этой боли. «Сменили поставщика комплектующих» → «клиенты жалуются на качество» — связь есть. «Наняли трёх разработчиков» → «отчёты грузятся медленно» — связи нет (наём людей не замедляет отчёты).',
  '2. Учитывай время: решение должно предшествовать боли или совпадать с её появлением. Если решение принято заметно ПОЗЖЕ, чем боль уже наблюдалась, оно не могло её породить — не связывай.',
  '3. Совпадение темы или общих слов — это НЕ причина. «Оба про логистику» или «оба упоминают склад» без механизма «решение → следствие» = не связываем.',
  '4. Возвращай id решений ТОЛЬКО из переданного списка кандидатов и копируй их слово-в-слово (буква в букву). Никогда не выдумывай и не сокращай id; id, которого нет в списке, будет отброшен.',
  '5. Связывай по одному, осознанно. Несколько решений можно вернуть, только если КАЖДОЕ по отдельности правдоподобно ведёт к боли. Не вали в список «на всякий случай» — каждый лишний id обвиняет ещё одно решение.',
  '6. Если ни одно решение правдоподобно не объясняет боль — верни ПУСТОЙ массив. Пустой ответ — это нормальный и частый исход, а не неудача.',
  '7. Пояснение (reasoning) — короткое (1–3 предложения), на чистом русском, объясняет механизм «решение → как именно привело к боли». Если связей нет — одной фразой скажи, почему ни один кандидат не подходит. Не вставляй id, коды статусов и латиницу в пояснение — пиши человеческим языком.',
  '',
  '# Примеры (плохо → хорошо)',
  '',
  'ПРИМЕР 1. Сигнал (проблема): «Клиенты жалуются на качество комплектующих, рост возвратов».',
  'Кандидаты: 1) id=dec_7f3a — «Перешли на поставщика комплектующих из соседнего региона ради экономии 15%»; 2) id=dec_9b21 — «Запустили реферальную программу для клиентов».',
  'ПЛОХО: linkedDecisionIds = ["dec_7f3a","dec_9b21"], reasoning: «Оба решения про клиентов и продукт».',
  'ХОРОШО: linkedDecisionIds = ["dec_7f3a"], reasoning: «Смена поставщика комплектующих ради экономии напрямую ведёт к падению качества и росту возвратов. Реферальная программа к качеству отношения не имеет».',
  '',
  'ПРИМЕР 2. Сигнал (блокер): «Релизы тормозят: каждая выкатка требует ручного согласования у трёх руководителей».',
  'Кандидаты: 1) id=dec_a1 — «Ввели обязательное тройное согласование любых изменений в продакшене после инцидента»; 2) id=dec_a2 — «Закупили новые ноутбуки команде разработки».',
  'ПЛОХО: linkedDecisionIds = [], reasoning: «Связь не очевидна».',
  'ХОРОШО: linkedDecisionIds = ["dec_a1"], reasoning: «Решение ввести обязательное тройное согласование изменений — прямая причина того, что каждая выкатка теперь требует подписи трёх руководителей и релизы тормозят».',
  '',
  'ПРИМЕР 3 (НЕГАТИВНЫЙ — связи нет). Сигнал (неэффективность): «Отчёты в дашборде грузятся по 30 секунд».',
  'Кандидаты: 1) id=dec_x — «Решили нанять трёх разработчиков в команду роста»; 2) id=dec_y — «Сменили офис на больший».',
  'ПЛОХО: linkedDecisionIds = ["dec_x"], reasoning: «Наняли разработчиков — наверное, нагрузили систему».',
  'ХОРОШО: linkedDecisionIds = [], reasoning: «Ни наём людей, ни переезд офиса не объясняют медленную загрузку отчётов — нет механизма, который связывал бы эти решения с тормозами дашборда. Причина, скорее, техническая и в списке решений её нет».',
  '',
  'ПРИМЕР 4 (НЕГАТИВНЫЙ — решение позже боли). Сигнал (риск): «Отток клиентов растёт третий месяц подряд».',
  'Кандидаты: 1) id=dec_late — «На прошлой неделе решили поднять цены на 10%».',
  'ПЛОХО: linkedDecisionIds = ["dec_late"], reasoning: «Подняли цены — клиенты уходят».',
  'ХОРОШО: linkedDecisionIds = [], reasoning: «Отток растёт уже три месяца, а решение поднять цены принято лишь на прошлой неделе — оно не могло породить отток, начавшийся раньше. Связи по времени нет».',
  '',
  '# Перед тем как вернуть ответ — самопроверка',
  '1. Для каждого id, который оставляю: я могу одной фразой назвать механизм «это решение → именно эта боль»? Если нет — убираю id.',
  '2. Я не связал по одной лишь общей теме или общим словам? Совпадение темы выкинул.',
  '3. Все id скопированы слово-в-слово из списка кандидатов? Ни одного выдуманного или сокращённого?',
  '4. Решение по времени предшествует боли (или совпадает), а не появилось заметно позже? Поздние решения убрал.',
  '5. Если сомневаюсь хоть в одной связи — я выбрал пустой массив или убрал сомнительный id, а не оставил натянутую связь?',
  '6. Пояснение на чистом русском, без id, кодов и латиницы, и объясняет механизм (или причину пустого ответа)?',
  '',
  '# Формат ответа',
  'Верни строго JSON по схеме `insight_link_to_decisions_v1`, на русском. Без markdown, без пояснений вне JSON.',
  '- linkedDecisionIds — массив id решений-кандидатов (строки, скопированные слово-в-слово). Если связи нет — пустой массив [].',
  '- reasoning — короткое пояснение на русском (механизм связи или причина пустого ответа).',
].join('\n');

/** Человеческие ярлыки типа сигнала (Insight) для подстановки вместо кода в USER. */
const INSIGHT_KIND_LABEL_RU: Record<string, string> = {
  problem: 'проблема',
  risk: 'риск',
  blocker: 'блокер',
  inefficiency: 'неэффективность',
};

/** Человеческие ярлыки статуса решения (Decision) для подстановки вместо кода в USER. */
const DECISION_STATUS_LABEL_RU: Record<string, string> = {
  proposed: 'предложено',
  active: 'действует',
  in_progress: 'внедряется',
  implemented: 'внедрено',
  superseded: 'заменено',
  rejected: 'отклонено',
  cancelled: 'отменено',
};

export const INSIGHT_LINK_TO_DECISIONS_USER_TEMPLATE = (args: {
  insightKind: string;
  insightStatement: string;
  candidates: readonly {
    id: string;
    statement: string;
    decidedAt: string | null;
    status: string;
  }[];
}): string => {
  const kindLabel = INSIGHT_KIND_LABEL_RU[args.insightKind] ?? args.insightKind;
  const candidates = args.candidates.length
    ? args.candidates
        .map((c, i) => {
          const statusLabel = DECISION_STATUS_LABEL_RU[c.status] ?? c.status;
          const when = c.decidedAt ? `принято ${c.decidedAt}` : 'дата решения неизвестна';
          return `  ${i + 1}. id=${c.id} | статус: ${statusLabel} | ${when} | «${c.statement.slice(0, 300)}»`;
        })
        .join('\n')
    : '  (кандидатов нет)';
  return [
    `Сигнал (${kindLabel}): «${args.insightStatement}».`,
    '',
    'Кандидаты-решения (поле id — копируй слово-в-слово, если связываешь):',
    candidates,
    '',
    'Верни JSON-объект по схеме `insight_link_to_decisions_v1`.',
  ].join('\n');
};

/**
 * JSON Schema strict для `insight-link-to-decisions`.
 *
 * Поле-решение — `linkedDecisionIds`: массив id Decision'ов, СКОПИРОВАННЫХ из
 * списка кандидатов (это не enum-вердикт). Пустой массив = «связи нет».
 * Поля `confidence` намеренно нет — связь определяется самим фактом наличия id.
 */
export const INSIGHT_LINK_TO_DECISIONS_JSON_SCHEMA: Record<string, unknown> = {
  type: 'object',
  additionalProperties: false,
  required: ['linkedDecisionIds', 'reasoning'],
  properties: {
    linkedDecisionIds: {
      type: 'array',
      maxItems: 10,
      items: { type: 'string', minLength: 1, maxLength: 60 },
      description:
        'id решений-кандидатов, чьё внедрение могло породить сигнал. Копируются слово-в-слово из переданного списка; пустой массив = связи нет.',
    },
    reasoning: {
      type: 'string',
      maxLength: 2_000,
      description:
        'Короткое пояснение на русском: механизм «решение → боль» или причина пустого ответа. Без id, кодов и латиницы.',
    },
  },
};

export const INSIGHT_LINK_TO_DECISIONS_SCHEMA_NAME = 'insight_link_to_decisions_v1';
