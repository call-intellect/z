---
title: Консолидация извлечения — дедуп сущностей и блоков + граница pain/blocker
date: 2026-07-03
type: tz
status: ready-to-implement
area: knowledge-core / extraction
relates_to:
  - plans/analysis/2026-07-03-strela-extraction-fidelity-results.md
  - plans/analysis/2026-07-03-recall-ceiling-hypotheses.md
  - docs/methodology/synthetic-fidelity-eval-method.md
owner_gate: "Ф1/Ф2 меняют прод-поведение мёржа сущностей/блоков → одобрение владельца ДО реализации"
---

# ТЗ: консолидация извлечения (дедуп сущностей + блоков + граница pain/blocker)

## Зачем

Фиделити-прогон «Стрелы» (44 якоря): полнота **44/44**, но **консолидация сломана** — сущности задвоены
(23 имени как несколько Entity разных типов: Битрикс=technology+vendor+product, Логистик Плюс=client+
customer+vendor, retention=goal+topic+metric), блоки дублируются (блокер 429 ×4, webhook-риск ×3, «Моя
история» ×2). Это фрагментирует граф → **корень ведра ④ recall** (резолв не дотягивается). Фикс чинит и
качество графа, и recall одновременно. Данные — `plans/analysis/2026-07-03-strela-extraction-fidelity-results.md`.

## Границы

- **В scope:** консолидация сущностей (Д1), кросс-канальный дедуп блоков (Д2), граница pain/blocker (Д3).
- **НЕ в scope:** захват/полнота (работает 44/44); агрегирующий summary-узел (Д4, минор — отдельно).
- **Owner-gate:** Ф1/Ф2 меняют прод-поведение мёржа → одобрение владельца ДО кода.
- **Верификация:** прямой read-only запрос (before/after) + fidelity-eval Слоя 0 (когда построен).

## Решения владельца (2026-07-03) — owner-gate снят, вшито в фазы ниже

- **Д1 (Ф1) → (B):** тип остаётся в идентичности; добавить **кросс-типовой merge-проход** (снять type-фильтр в
  выборке кандидатов: `findCandidates` [entity-merge.service.ts:201], cron-пары [entity-resolver.cron.ts:150],
  cache-key/exact/KNN резолва) + арбитр решает «один объект под разными видами»; + детерминированный `client`→`customer`.
- **Д1 (Ф1) → канонический тип:** матрица приоритетов (`customer>client`, domain > generic `topic/metric`) для
  очевидных + арбитр возвращает `canonicalType` для спорных. **Типизированные сабрекорды (Vendor/Customer/Goal/…)
  — дописать миграцию между типами** (сейчас warn-only [entity-merge.service.ts:349-355]; снять hard-guard :310-312).
- **Д2 (Ф2) → (A):** убрать контекст-хедер из дедуп-вектора блока ([embedding.service.ts:14-23]). **Условие:**
  сначала проверить, не критичен ли хедер для retrieval-качества; если критичен — эскалировать владельцу (C) (два
  вектора: чистый дедуп + контекстный retrieval, +колонка/миграция/backfill). Снижение порога 0.85 — отклонено.
- Полное обоснование и `path:line` — [Ф0-записка](../analysis/2026-07-03-extraction-consolidation-f0-diagnosis.md).

## Ф0 — Диагностика корня (read-only, без стенда) `[x]` — done, ждёт owner-решения по развилкам

> Результат: [plans/analysis/2026-07-03-extraction-consolidation-f0-diagnosis.md](../analysis/2026-07-03-extraction-consolidation-f0-diagnosis.md).
> Д1 подтверждён (тип — разделитель на 6 уровнях, `entity-merge.service.ts:201` и др.). Д2: гипотеза per-source/window
> опровергнута — корень в загрязнении дедуп-вектора контекст-хедером (`embedding.service.ts:14-23`). 3 owner-развилки
> по мёржу — в записке; Ф1/Ф2 не начинать без решения владельца.

Установить ТОЧНУЮ причину по коду (path:line + дословно), не угадывая фикс:
- **Задвоение сущностей:** где принимается решение merge/create в `entity-resolution.service.ts` /
  `entity-merge.service.ts` / `entity-resolver.worker.ts`. Гипотеза: ключ мёржа = (нормализованное имя +
  **type**), поэтому одно имя с разным выведенным типом → разные Entity. Подтвердить/опровергнуть.
- **Дубли блоков:** почему `block-distill.worker.ts` (арбитр ~:103) не схлопывает один факт из разных
  каналов/дней — ключ дедупа per-source/per-window? Порог похожести? Подтвердить.
- Выход Ф0: точная причина каждого дефекта + место фикса.

Приёмка: короткая записка (path:line + вывод) в analysis-док; согласование фикса с владельцем (owner-gate).

## Ф1 — Консолидация сущностей (Д1) `[x]`

- **Тип — атрибут канонической сущности, не ключ раскола.** Одно реальное имя → одна Entity; тип
  резолвится в «лучший» (приоритет доменных типов над generic topic/custom), остальные типы — в
  evidence/aliases, не в отдельные строки.
- **Консолидирующий проход** для существующего графа (стиль `graph-reconcile.cron`): найти same-name Entity
  (нормализация имени), слить в одну каноническую (`mergedIntoId`), выбрать тип по приоритету, объединить
  aliases/связи. Идемпотентно, kill-switch, батч-крутилка.
- Сохранить анти-generic (`entity-name-quality.ts`) и анти-over-merge (не слить реально разные —
  negative-cache `markEntityPairDistinct`).
- **Приёмка:** прямой запрос — «имён с несколькими типами» **23 → ≤3**; общее число активных Entity
  «Стрелы» падает (171 → ближе к ~143 уникальным); «Битрикс»/«Логистик Плюс»/«retention» — по одной
  канонической; юнит на консолидатор (same-name→merge, разные→не слиты, идемпотентность).

## Ф2 — Кросс-канальный дедуп блоков (Д2) `[ ]`

- Усилить `block-distill` арбитр: один факт из **разных каналов/дней** → один canonical IdeaBlock с
  несколькими `IdeaBlockEvidence`, а не N параллельных блоков. Матч — семантический (embedding-порог) поверх
  текущего ключа, кросс-source/кросс-window.
- Сохранить `primarySource='transcript'` побеждает report (block-distill.worker.ts:433).
- **Приёмка:** прямой запрос — блокер «Битрикс 429» **4 → 1** canonical (с ≥4 evidence); webhook-риск 3→1;
  «Моя история» idea 2→1. Юнит: два блока одного факта из разных source → merge; разные факты → не слиты;
  идемпотентность второго прогона.

## Ф3 — Граница pain/blocker (Д3) `[ ]`

- Правило в `block-ingest.prompt.ts` (+ классификатор типа): **критичный клиентский инцидент,
  останавливающий работу клиента** (напр. «отчёт не формируется второй день, завтра совет директоров») →
  `blocker`, не `pain`. `pain` — про внутреннюю боль/трение, не про клиент-стоп.
- Cache-friendly: правка в теле правил, стабильный префикс.
- **Приёмка:** golden-фикстура D3-кейса → `blocker`; A/B на затронутых кейсах не роняет остальную типологию;
  прямой запрос — инцидент Ромашки помечен blocker.

## Ф4 — Верификация консолидации `[ ]`

**Стоп-гейт стенда (обязательно).** Стенд — это ЕДИНСТВЕННАЯ локальная dev-БД (Postgres :55435 `z_main`) +
dev-backend + реальный LLM, где живёт эталонная «Стрела» (org `cmr1qbvpx0001pwbwxbgmh1jl`). Ф1/Ф2 backfill
**необратим** (мёрж не разъединить). Агент-исполнитель:
- **НЕ** гоняет backfill/eval против реальной «Стрелы». Смоук консолидатора — только на **отдельном throwaway-тенанте**
  (`bun run scripts/seed-synthetic-company.ts` печатает новый org id), не на baseline.
- Доведя код + зелёные typecheck(вкл .spec)/lint/build + юниты/интеграционные (на prefixed/safe данных, не «Стрела»)
  + throwaway-смоук → **останавливается и рапортует «готов к прогону стенда»** со списком команд ниже. Прогон
  запускает **владелец** (он проверяет, что стенд свободен, и делает snapshot).

**Последовательность прогона стенда (запускает владелец):**
1. Стенд свободен: docker-deps up, dev-backend up, нет другого seed/backfill/eval по «Стреле».
2. **Snapshot-точка отката:** `pg_dump z_main > strela-before-backfill.sql` (локальная БД, дёшево; мёрж необратим).
3. Ф1 консолидатор + Ф2 re-dedup backfill против `STRELA_ORG=cmr1qbvpx0001pwbwxbgmh1jl`.
4. Дать конвейеру устояться (block-distill debounce, cron).
5. **Прямой запрос-гейт:** имён-с-несколькими-типами 23→≤3; «Битрикс 429» 4→1; webhook-риск 3→1; «Моя история» 2→1.
6. **Слой-0 fidelity** повторно (`strela-manifest.batch{1,2,3-rest}.json`) — слепые разметчики + матчер, read-only.
7. **Recall-атрибуция** (`scripts/eval/run-recall-eval.ts`) — реально ли консолидация подняла ведро ④ (открытый вопрос).
8. Полнота НЕ упала (44/44 остаётся). Записать after-числа → обновить Ф0-записку + fidelity-results.

Про «переключение между стендами»: отдельного рантайм-переключателя нет и не нужно. «Два стенда» = throwaway-тенант
агента (смоук) vs эталонная «Стрела» (официальный замер, запускает владелец со snapshot). Один физический стенд —
последовательно, не параллельно.

## Прод-деплой

- Ф1 консолидатор — новый `backfill/reconcile`-скрипт: зарегистрировать в `apply-prod-deploy.ts` STEPS
  (`phase: backfill`, `skipBootstrap`), Шаг 8. Kill-switch + батч-крутилка → реестр флагов + AdminSetting.
- Ф2/Ф3 — правки воркеров/промптов, редеплой backend; миграций нет (если не добавляется поле).
- Прямой прод-эффект: мёрж существующих дублей → строка в prod-deploy-log (одноразовый прогон консолидатора).

## Итог
Реализовано: —. Осталось: Ф0-Ф4. Owner-gate на Ф1/Ф2 (прод-поведение мёржа). Полный повторный fidelity —
под стендом. Ожидаемый эффект: чище граф + подъём ведра ④ recall (двойная ценность).
