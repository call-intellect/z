---
title: BullMQ Workers + Queues + Crons (реестр)
status: living
covers: реестр BullMQ-очередей, воркеров, @Cron заданий
---

# Workers + Queues + Crons

Сжатый реестр BullMQ-очередей, in-process воркеров и `@Cron` заданий backend. **Топология (2026-06-03, уточнено):** воркеры и cron'ы крутятся **IN-PROCESS внутри основного backend'а** — `WorkersModule` импортирован в `AppModule`, отдельного worker-процесса/контейнера НЕТ (см. doc `WorkersModule`; в compose один сервис `backend`, CMD `bun dist/main.js`). Это важно для идемпотентности: in-process lock (Set) реально покрывает гонку cron↔webhook↔producer. _(Прежняя фраза про `bun run worker:dev` / `src/workers/main.ts` устарела.)_

Файл создан 2026-05-25 как часть финального handoff Wave 1-3.

## Очереди BullMQ (Redis-backed)

| Очередь | Producer | Consumer | Concurrency | Назначение |
|---|---|---|---|---|
| `core.raw-events` | `IngestService` (meeting-adapter, tracker-adapter, telegram-adapter, email-adapter, **mail-inbound** T5) | `block-ingest.worker` | 4 | Универсальный ingest в knowledge-core |
| `core.block-distill` | `block-ingest.worker` | `block-distill.worker` | **1** (Б11 [K4], 2026-06-16: сериализация — при concurrency>1 две параллельные обработки одного блока плодили draft-дубли) | Дистилляция блоков |
| `core.block-linker` | cron / on-event | `block-linker.worker` | 1 | KNN+LLM граф знаний |
| `core.entity-resolve` | `block-distill.worker` | `entity-resolver.worker` | 1 | Слияние Entity |
| `core.theme-clusterer` | cron 15 * * * * | `theme-clusterer.cron` | — | KNN-greedy кластеры тем |
| `core.specialist-routing` | `block-distill.worker` (markCanonical/mergeInto — **canonical**-блоки, МТЗ №1 Ф3 2026-06-04), on-event | **ОДИН `SpecialistRoutingDispatcherWorker`** (МТЗ №1 Ф2 2026-06-04) — диспетчеризует по `job.name` в 14 `@Injectable`-handler'ов (`3-1-regulations` … `3-14-goals`, `tracker-ingest`). Раньше было 14 конкурирующих Worker'ов на одной очереди (job мог уйти «не тому» воркеру и пропасть). | 2-4 | Маршрутизация в специалистов слоя 3. **Один Worker на очередь** (BullMQ: один job → один воркер — нельзя per-jobName consumer на общей очереди); неизвестный jobName → throw; skip-метрика `core_specialist_skipped_total{specialist,reason}` во всех 14 handler'ах. Sprint helper (2026-05-27) — jobName `3-13-sprint-helper`, payload `SprintHelperJobData{cycleId, tenantId, reason}`, concurrency=1, jobId-дедуп по cycleId. **Goals (2026-06-02):** jobName `3-14-goals`, concurrency 2, триггер — блоки с `signalType ∈ {commitment, plan_item}` (НЕ decision); авто-добыча целей. **Tasks (unified-task-extraction Ф1, 2026-06-23):** ~~jobName `3-15-tasks` (`Specialist315TasksWorker`), триггер блок `action_item` → `task-extract`.~~ **Снят из диспетчера специалистов (заход B, 2026-06-30):** `Specialist315TasksWorker` и роут `action_item→TASKS` удалены (TASKS убран из `RouterService.SPECIALIST`/`PRIORITY`) — задачи извлекает combo (см. строку `core.specialists-combined`). `Specialist315TasksService` остался для `runClarifySweep`. См. [[sprints]], [[goals-and-strategic-alignment]] §«Goals OKR v2», [[tracker]] §«Combo — единый источник задач». |
| `core.skill-profile-rebuild` | on-event (debounce 60s) | `skill-profile-rebuild.worker` | 1 | γ-1 |
| `core.knowledge-clone-rebuild` | on-event | `knowledge-clone-rebuild.worker` | 1 | β-2 |
| `core.probe-events` | `ProbeService.suggest` | `probe-dispatcher.worker` | 1 | Probe-Agent (формулирует и шлёт через каналы) |
| `core.recognition-formulate` | on-event (badge_awarded, thanks_explicit, ...) | `recognition-formulate.worker` | 2 | Recognition (с fallback на deterministic copy) |
| `core.push-send` | on-event | `push-sender.worker` | 5 | Web Push (jobId dedup по `<userId>_<hash>_<bucketMin>`) |
| `tracker.webhook-delivery` | `IssueWebhookService` | `webhook-delivery.worker` | 8 | Outgoing tracker webhooks (HMAC SHA256, retry 5x exp backoff) |
| `core.intake-auto-triage` | `TaskDraftMaterializerService` (combo→tracker, заход B 2026-06-30; раньше `meeting-extract-actions.worker`) | `intake-auto-triage.worker` | 2 | confidence ≥ 0.92 → auto-Issue. При accept материализует `IntakeIssue.checklistJson` → `IssueChecklist` (`intake-checklist-materialize.util`). |
| `core.card-rollup-v2` | on-event (debounce 60s) | `card-rollup-v2.worker` | 2 | AI rollup карточки |
| (legacy) `core.card-rollup` | on-event (debounce 5s) | `card-rollup.worker` | 2 | Legacy (живёт до Фаз 5/6) |
| **`core.meeting-report-fast` (ТЗ 2026-05-25; единое ядро отчёта с 2026-06-11)** | `merge.worker` (после готовности транскрипта; рядом с `ai.analyze`) | `meeting-report-fast.worker` | 2 | Один LLM-вызов на сыром транскрипте → главы + задачи + summary + quality_score → `Meeting.reportFastStatus`. **С 2026-06-11 — единственное ядро отчёта** (дублирующие очереди `ai.chapters`/`ai.tasks`/`ai.quality-score` удалены): пишет качество в каноничную `MeetingQualityScore` + `Meeting.qualityScoreStatus='ready'`. По готовности (`ready`/`partial`) эмитит событие `meeting.report-fast-ready` → `ReportIngestListener` (отчёт→граф, см. §ниже). ENV kill-switch `MEETING_REPORT_FAST_ENABLED`. См. [meeting-report-pipeline](meeting-report-pipeline.md). |
| **`core.feedback-digest` (2026-05-25)** | `feedback-digest.cron` (`0 1 * * *` UTC) + ручной `POST /admin/feedback/digest/run` | `feedback-digest.worker` | 1 | Ночной AI-прогон по `FeedbackMessage`: кластеризация в `FeedbackTopic` через taskType `feedback-cluster` (DeepSeek V4 Pro). Redis-lock `feedback:digest:lock` (TTL 30 мин). Глобальная фича (без tenant). См. [[feedback]]. |
| **`core.specialists-combined` (Фаза 6 §3, 2026-05-26; основной путь + канало-агностичен с захода A 2026-06-30)** | `block-distill.worker` (после canonical-блока **любого источника** — встреча/chat/chatbox/Bitrix, если `knowledge.specialists_combined_enabled` ON) | `specialists-combined.worker` (`SpecialistsCombinedWorker`) | 2 | Б+ объединённый вызов: один LLM (`knowledge-specialists-combined`, DeepSeek V4 Pro) извлекает 8 типов сущностей сразу — decisions / ideas / insights / experiments / regulations / knowledge_categories / skill_traits / helpfulness_traits. ~3.7× дешевле по эвалу `judge-specialists-bplus-vs-g`. **С захода A (2026-06-30) — ВКЛ по дефолту, ОСНОВНОЙ путь (не A/B):** рубильник переведён в AdminSetting `knowledge.specialists_combined_enabled` (kill-switch ON, `zBool`-фикс — `.env=false` больше не глушит откат), `delayMs` = `knowledge.specialistsCombinedDelayMs` (90с); `SpecialistsCombinedJobData{tenantId,sourceType,externalId}` (контекст из `RawEvent`, не `Meeting`); восстановлены 4 побочки (rebuild профиля клона/навыков, гигиена решений, ProcessTemplate через раздельный `process-detector` — `PROCESS_DETECTOR` убран из `COMBINED_COVERED`); скелет-карта `meeting-skeleton` (in-memory, без своей очереди) подмешивается в шапку `block-ingest`. **С захода B (2026-06-30) combo эмитит ещё и задачи** (9-й выход tool `submit_all_entities`, был `submit_all_8_entities`) → `TaskDraftMaterializerService` (tracker) материализует task-черновики в `IntakeIssue` (+`checklistJson`); meeting-извлечение задач убрано из `analyze.worker` (`MeetingExtractActionsService` снесён). См. [[ai-jobs]] §«Переписывание извлекающего слоя — заход A/B», [[tracker]] §«Combo — единый источник задач», [[../02_architecture/knowledge-core]]. |
| **`onboarding.demo-seed` (ТЗ 2026-05-31)** | `OnboardingService.completeWelcome` (после welcome-онбординга, если `!demoWorkspaceSeededAt`) | `demo-seed.worker` (`DemoSeedWorker`) | 2 | Авто-заливка демо-кабинета «ТехноСтрим» новой Org. jobId=`demo-seed:<orgId>` (идемпотентно). Precondition: `Subscription.status==='DEMO'` (иначе skip). Делегирует в `seedDemoWorkspace`. См. [[onboarding-wizard]]. |
| **`onboarding.demo-cleanup` (ТЗ 2026-05-31)** | `SubscriptionActivatedListener` (on-event `billing.subscription.activated_paid`/`_bonus`, если `demoWorkspaceSeededAt!=null`) | `demo-cleanup.worker` (`DemoCleanupWorker`) | 1 | Авто-стирание демо-данных при первой оплате (DEMO→ACTIVE). jobId=`demo-cleanup:<orgId>`. Делегирует в `resetDemoWorkspace` (удаляет только `externalSource='demo'`); `no_demo_to_reset` = success-skip. См. [[onboarding-wizard]]. |
| **`recording.faststart` (ТЗ 2026-06-03 recording-reliability, Фаза 3)** | webhook `egress_ended`(composite) через `AiQueueService.enqueueRecordingFaststart`, если `RECORDING_FASTSTART_ENABLED` (дефолт ON) **и** размер composite ≥ `RECORDING_FASTSTART_MIN_BYTES` (50 МиБ) | `faststart.worker` (`FaststartWorker`, `recordings/workers/`) | 1 | Переупаковка composite MP4 в faststart (`ffmpeg -c copy -movflags +faststart`) → перезалив по тому же S3-ключу. jobId=`faststart_<meetingId>` (dedup). Идемпотентно/безопасно. Воркер повторно гейтит по `bytesTotal` ≥ порога. См. [[recording]]. |
| **`chatbox.sync` (ChatBox-интеграция, 2026-06-05)** | ручной `POST /chatbox/integration/sync`, `ChatboxSyncCron`, inbound webhook | `chatbox-sync.worker` | — | Синк ChatBox→Кора (channels/customers/channelClients/members/chats/messages) + сегментация на сессии. Upsert по `@@unique([tenantId, externalId])` (идемпотентно). Kill-switch `AdminSetting chatbox.enabled`. **Prom-метрики (блок A, 2026-06-11):** `z_chatbox_syncs_total{scope,status}` + `z_chatbox_last_sync_ts_seconds{scope}`. См. [[chatbox-integration]]. |
| **`chatbox.analyze` (ChatBox-интеграция, 2026-06-05; sweep → 10 мин 2026-06-23)** | `ChatboxAnalyzeCron` (`EVERY_10_MINUTES`) + enqueue из `incrementalSync` (Гарантия A) | `chatbox-analyze.worker` | — | Закрытая сессия чата → LLM-summary (taskType `chatbox-summary`, подмешивает summary предыдущей сессии) → `IngestService.ingest` → `RawEvent(sourceType='chatbox', dataClass='sensitive')`; ставит `analysisStatus=done/failed` + `rawEventId`. Идемпотентно (стабильный idempotencyKey по sessionId). **Prom-метрики (блок A, 2026-06-11):** `z_chatbox_analyzes_total{status}` (воркер/cron) + `z_chatbox_pending_sessions` (gauge накопленных) + `z_chatbox_stuck_analyzing_sessions` (gauge, 2026-06-23 — застрявшие в `analyzing`). 2 PromQL-алерта — см. `docs/operations/prod-deploy-log.md`. См. [[chatbox-integration]], [[ai-jobs]]. |
| **`core.document-import` (ТЗ-4 Ф7–Ф9, 2026-06-09)** | `DocumentImportService` (`POST /documents/import-zip`, `/import-confluence`) | `document-import.worker` (`DocumentImportWorker`, modules/documents) | — | Batch-импорт документов: по `{ importId }` распаковывает ZIP/Notion (`fflate`) или тянет Confluence (API), каждый поддержанный файл → отдельный `Document` (re-use ingest-пути → граф). Пишет прогресс в `DocumentImport.doneFiles`/`failedFiles`/`errorLog`. См. [[../02_architecture/module-map]] §«Батч 5», [[document-ingest]]. |
| **`meeting.upload-ingest` (ТЗ-5, 2026-06-09)** | `MeetingUploadsService` (после `POST /meetings/:id/upload/complete`) | `meeting-upload-ingest.worker` (`MeetingUploadIngestWorker`, modules/meeting-uploads) | 1 | Нормализация загруженного медиа (ffprobe/ffmpeg, любой формат → пригодное для ASR аудио). concurrency=1 (ffmpeg тяжёлый). Затем enqueue `meeting.upload-transcribe`. Рубильник `MEETING_UPLOAD_ENABLED`. См. [[../02_architecture/module-map]] §«Батч 5». |
| **`meeting.upload-transcribe` (ТЗ-5, 2026-06-09)** | `meeting-upload-ingest.worker` | `meeting-upload-transcribe.worker` (`MeetingUploadTranscribeWorker`, modules/meeting-uploads) | — | Vox-**диаризация** (turns + `MeetingUploadSpeaker`) → встреча переходит в `awaiting_speakers` **БЕЗ анализа** (гейт до ручной разметки говорящих через `POST /meetings/:id/speakers/confirm`, который и enqueue'ит обычный анализ). См. [[../02_architecture/module-map]] §«Батч 5». |
| **`assistant.inbound` (ТЗ 2026-06-18 assistant-calendar, Ф1)** | Telegram/MAX webhook-контроллеры (`conversational/adapters/{telegram-bot,max-bot}/*-webhooks.controller.ts`) — **ранний ACK**: контроллер кладёт апдейт в очередь и сразу отвечает `200` | `AssistantInboundWorker` (**in-process**, `conversational/queue/assistant-inbound*.ts`), `attempts:1` | — | Развязка приёма входящих помощника от обработки (чтобы webhook не таймаутил). Дедуп: jobId по `update_id` (Telegram) / `mid` (MAX) + дедуп на входе через Redis `SET NX EX 3600`. Kill-switch `ASSISTANT_INBOUND_ASYNC_ENABLED` (дефолт ON; OFF → старая синхронная обработка). См. [[conversational-channels]]. |
| **`core.goal-embed` (knowledge-core MASTER task-dedup Ф5, 2026-06-16)** | `GoalsService` (после создания/правки цели) + backfill `backfill-goal-embeddings.ts` | `goal-embed.worker` (`GoalEmbedWorker`, modules/knowledge-core) | 4 | Считает `Goal.embedding` (`vector(1536)`, text-embedding-3-small) по `name + description`; пишет `Goal.embeddingHash` (чтобы не пересчитывать без изменений). Питает KNN-дедуп целей в специалисте `3-14-goals`. HNSW-индекс на `Goal.embedding` — в `postgres-init.sql`. См. [[ai-jobs]] §«task-dedup», [[goals-and-strategic-alignment]]. |
| `core.regulation-consolidator` | `RegulationConsolidatorCronService` (`@Cron('*/30 * * * *')`, per-Org × 4 типа) | `RegulationConsolidatorWorker` | **1** | Консолидация накопленных дублей карточек регламентов/процессов/политик/инструкций внутри типа: LATERAL top-1 HNSW → `dedupeArbiter` → merge с `CardVersion(changeReason:'consolidate')` + deprecate loser. Защита `trustTier='human'` + negative-cache (Redis + CurationDecision reject/split). jobId `regconsolidate_<type>_<id>`. kill-switch `aiFeatures.regulationConsolidatorEnabled`. (2026-06-20) |
| **`core.subject-memory-derive` (Слой 3 «Память субъекта», 2026-06-22)** | `ProbeResponseHandler` (после ingest ответа на уточняющий вопрос → enqueue вывода правила) | `SubjectMemoryDeriveWorker` (`probe/subject-memory/`) | — | Выводит правило из ответа на probe через taskType `subject-memory-rule-extract` → `SubjectMemoryService.deriveRuleFromProbeResponse` → `upsertWithSupersede` (supersede по `occurredAt`, дедуп по cosine-порогу). Очередь авто-создаётся `CoreQueueService` из `CORE_QUEUE_NAMES.SUBJECT_MEMORY_DERIVE` (`'core.subject-memory-derive'`). BullMQ вместо fire-and-forget — переживает рестарт, ретраит. kill-switch `subjectMemory.enabled`. См. [[ai-jobs]] §«Память субъекта», [[probe-agent]]. |
| **`message.outbox` (единый чат Ф1)** | `MessageService.sendMessage`/`appendTicketMessage`/`appendSystemMessage` (transactional outbox) + backstop-cron sweep (`*/30 * * * * *`, застрявшие `pending`) | `MessageOutboxRelayWorker` (`messaging/queue`, `ai/workers.module`) | 8 | Доставка сообщения: эмит `message.new` в WS-room (`internal`→staff-room), офлайн-сигнал ФЗ-41 (`notifyOffline`, без тела), `markSent`. После `markSent` — триггер `chat.ingest` (если `feedsGraph` и `authorType!='system'` и `CHAT_INGEST_ENABLED`). jobId `msg-outbox:<id>`. См. [[ai-jobs]] §«chat.ingest». |
| **`chat.ingest` (единый чат Ф5a, 2026-06-28)** | `MessageOutboxRelayWorker.relay` (после `markSent`) + `VoiceTranscribeWorker` (после ASR) | `ChatIngestWorker` (`messaging/queue`, `ai/workers.module`) | 4 | `ChatIngestService.ingestMessage` → `IngestService.ingest(payload.kind='chat_message', sourceExternalId='msg:<id>', dataClass='internal')` под `Source(type='chat', name='Сообщения Коры')` → block-ingest → IdeaBlock. Один источник `Message`, без двойного ingest. Skip: `!feedsGraph` / `authorType='system'` / `deletedAt` / пустой текст. text = `decrypt(content) || voiceTranscript`. Идемпотентно (jobId `chat-ingest:<id>` + RawEvent dedup по `msg:<id>`). kill-switch `CHAT_INGEST_ENABLED`. См. [[ai-jobs]]. |
| **`voice.transcribe` (единый чат Ф5a, 2026-06-28)** | `MessageService.sendMessage`/`insertHistorical` (на создании сообщения с `voiceUrl`) | `VoiceTranscribeWorker` (`messaging/queue`, `ai/workers.module`) | 2 | Читает аудио из S3 (`voiceUrl` → `extractKeyFromUrl` → `S3Service.getObject`), `VoxService.submit`+`poll` → `Message.voiceTranscript`; затем (если `feedsGraph`) пере-enqueue `chat.ingest` (транскрипт втекает в граф через chat-ingest, который берёт `voiceTranscript` при пустом `content`). Skip: нет `voiceUrl` / транскрипт уже есть. jobId `voice-tx:<id>`. См. [[ai-jobs]]. |

## @Cron задания

Все cron-задания читают `Org.workersEnabled` через `WorkerOrgGate.checkOrThrow` — admin может выключать per-Org.

| Cron | Расписание | Файл | Что делает |
|---|---|---|---|
| `theme-clusterer` | `15 * * * *` | `knowledge-core/workers/theme-clusterer.cron.ts` | Кластеризация тем |
| **`theme-summarize` (граф знаний v2, 2026-06-24)** | `@Cron('35 * * * *')` (ежечасно) | `knowledge-core/workers/theme-summarize.cron.ts` | Авто-резюме тем (`Theme.summary`/`summaryUpdatedAt`) через taskType `theme-summarize`. **Инкрементально** — пересчитывает только изменившиеся темы. Kill-switch `knowledge.theme_summary_enabled` (ON). См. [[ai-jobs]] §«Граф знаний v2», [[../02_architecture/knowledge-core]] §«Перестройка ингеста + умный поэтапный поиск». |
| `reframing` | `0 3 * * *` | `knowledge-core/workers/reframing.cron.ts` | Архивация слабых связей + decay |
| `entity-graph-builder` | `0 * * * *` | `knowledge-core/workers/entity-graph-builder.cron.ts` | Co-mentioned пары Entity |
| `chat-v2-cleanup` | `0 3 * * 0` (Sun 03:00) | `chat-v2/workers/chat-v2-cleanup.cron.ts` | TTL 90 дней → archived |
| `knowledge-clone-rebuild` | `0 */6 * * *` | `specialist-3-2-knowledge-clone/cron/...` | β-2 rebuild |
| `insights-recalc` | `0 */6 * * *` | `specialist-3-5-insights/cron/...` | β-4 frequency/dynamic |
| `ideas-clusterer` | каждые 4 часа | `specialist-3-6-ideas/cron/ideas-clusterer.cron.ts` | β-5 KNN cluster Idea |
| **`skill-trait-verify` (TZ#2 Ф3, 2026-06-08)** | `30 3 * * *` (03:30) | `specialist-3-7-skill/cron/...` | `SkillTraitVerifyCron` — grounding-проверка свежих черт в статусе `pending_verification` (LLM `skill-trait-verify`): grounded → `active`, иначе → `held`, FAIL-OPEN на ошибке LLM → `active`. Черта в `pending_verification` НЕ попадает в persona до проверки. Между concept-нормализатором (03:00) и `skill-profile-decay` (05:00). См. [[skill-and-clone]] §«Доработки 2026-06-08», [[ai-jobs]]. |
| `skill-profile-decay` | `0 5 * * *` | `specialist-3-7-skill/cron/...` | γ-1 daily decay (одна ступень за проход — class-фикс 2026-06-08, см. [[../02_architecture/code-pitfalls]]) |
| **`role-principle-synthesis` (clone-method Э1.2, 2026-06-12)** | `30 5 * * *` (05:30) | `knowledge-core/workers/role-principle-synthesis.cron.ts` | `RolePrincipleSynthesisCron` — Reflection-синтез принципов роли. Sweep ролей (union PersonRole+Appointment, MAX 100/проход, Redis-lock) → `RolePrincipleSynthesisService`: subject-reasoning блоки должности → группировка cosine 0.78 → LLM `role-principle-synthesize` (capable) → валидация (sourceBlockIds⊆входных, ≥2; код-гард диагностической лексики) → дедуп по embedding 0.85 (merge/create) → upsert `RolePrinciple`. Пороги — AdminSetting `knowledge.rolePrincipleMinObservations` (5) / `knowledge.rolePrincipleDedupThreshold` (0.85). Kill-switch `ROLE_PRINCIPLE_SYNTHESIS_ENABLED` (ON). Метрики `role_principles_synthesized_total{outcome}`, `role_principles_active_total`. После skill-decay (05:00), до persona-build (вс 06:00). См. [[skill-and-clone]] §«Доработки 2026-06-12», [[ai-jobs]]. |
| `skill-persona-snapshots` | `0 6 * * 0` (Sun) | `specialist-3-7-skill/cron/...` | Weekly snapshots |
| **`persona-layer-validation` (clone-method ВАЛ.1, 2026-06-12)** | `0 7 * * SUN` (вс 07:00) | `knowledge-core/workers/persona-layer-validation.cron.ts` | `PersonaLayerValidationCron` — еженедельная поведенческая валидация клона (после persona-build вс 06:00, MAX 10 ролей): `PersonaLayerValidationService` на реальных кейсах роли (кейс исключается из subgraph) сравнивает ответы клона с persona v1 (in-memory baseline «только черты») vs v2 (все слои метода) через LLM-судью `persona-behavior-judge` (flash; только поведенческий ход, запрет character-суждений, отказ=0.3). Ничего не блокирует (без human-approval, R10). Kill-switch `PERSONA_LAYER_VALIDATION_ENABLED` (ON). Метрики `clone_persona_layer_score{variant}`, `persona_layer_validation_cases_total{outcome}`. См. [[skill-and-clone]] §«Доработки 2026-06-12», [[ai-jobs]]. |
| `skill-manager-digest` | `0 9 * * 1` (Mon) | `specialist-3-7-skill/cron/...` | Weekly digest |
| `social-contribution-profile` | `0 5 * * *` | `specialist-3-8-helpfulness/cron/...` | Wave 2 — агрегат helpCount/etc |
| `helpfulness-spotlight` | `0 9 * * 1` (Mon) | `specialist-3-8-helpfulness/cron/...` | Wave 2 — формирует pending spotlight |
| `helpfulness-trait-decay` | `0 6 * * *` | `specialist-3-8-helpfulness/cron/...` | 30-day decay |
| `helpfulness-probe` | `0 10 * * *` | `specialist-3-8-helpfulness/cron/...` | 4 probe-trigger обёртка |
| `contribution-snapshot` | `0 4 * * *` | `recognition/cron/contribution-snapshot.cron.ts` | Wave 2 — агрегат per user |
| `badge-awarder` | `0 5 * * *` | `recognition/cron/badge-awarder.cron.ts` | Wave 2 — 5 базовых badges |
| `streak-detector` | `0 23 * * *` | `recognition/cron/streak-detector.cron.ts` | Wave 2 — currentCheckinStreak + emit streak_milestone |
| `recognition-weekly-digest` | `0 9 * * 1` (Mon) | `recognition/cron/recognition-weekly-digest.cron.ts` | Wave 2 — manager digest |
| `feed-expire` | `*/15 * * * *` | `activity-feed/cron/feed-expire.cron.ts` | Wave 2 — FSM expired |
| `feed-digest` | daily 09:00 + Mon 09:00 | `activity-feed/cron/feed-digest.cron.ts` | Wave 2 — digest collection |
| `push-cleanup` | `0 3 * * *` | `push/cron/push-cleanup.cron.ts` | Wave 2 — удаляет subscriptions с failureCount ≥ PUSH_MAX_FAILURES |
| `strategic-alignment` (issue-based) | `0 6 * * *` (06:00 UTC) | `goals/cron/strategic-alignment.cron.ts` | Sprint 3 B1-3.2 |
| **`goal-kr-progress` (Goals OKR v2, 2026-06-02)** | `0 5 * * *` (05:00 UTC) | `goals/cron/goal-kr-progress.cron.ts` | `GoalKrProgressCron` — авто-прогресс Key Results. Per-Org/per-goal try/catch, только `status='active'` + `validUntil=null`. По `sourceKind`: `meeting_count`→`meeting.count(completed)`, `issue_rollup`→`issue.count(state.category=completed, goalId)`, `metric_entity`→`Entity.mentionsCount`, `manual`→skip. Checkpoint(recordedBy='auto') только при изменении значения. **M0:** уважает `manualOverride` (`currentValue`∈KR / `progressStatus`∈Goal). `progressStatus` по тренду 14д. Метрика `goal_kr_autoprogress_total{source_kind,status}`. См. [[goals-and-strategic-alignment]]. |
| **`goals-pulse` (Goals OKR v2, 2026-06-02)** | `0 6 * * 1` (пн 06:00 UTC = 09:00 МСК) | `goals/cron/goals-pulse.cron.ts` | `GoalsPulseCron` — еженедельный пульс целей. Идемпотентно по `(tenantId, isoWeek)` в `WeeklyGoalsPulseDigest`. Агрегат счётчиков по `progressStatus` + LLM `goals-pulse-summarize` (сухой fallback). Доставка `owner`/`coo` через `ConversationalService.sendNotification(eventType='goals.pulse')`. Тумблеры `AdminSetting.goals.pulse.enabled` (default true) / `goals.pulse.deliver_to_telegram` (default false). Метрики `goals_pulse_{generated,failed,delivered}_total`. |
| `telegram-digest` | `0 9 * * *` | tracker Wave 3 | Telegram дайджест моих задач |
| **`telegram-proxy-health` (2026-05-26)** | `*/<TELEGRAM_PROXY_HEALTH_INTERVAL_SEC> * * * * *` (default 30с) | `conversational/adapters/telegram-bot/telegram-proxy-health.cron.ts` | Пингует прокси `telegram.crossmark.ru`, пишет `tg:proxy:healthy` в Redis (читает `AdminTelegramBotService.getSettings`). Лидер-выбор через `SET NX EX`. Метрика `telegram_proxy_health_check_total{outcome}`. ТЗ: plans/tz/2026-05-26-telegram-via-crossmark-proxy.md. |
| `cycle-rollover` | (на end of cycle) | tracker | Auto-rollover незакрытых задач |
| **`imap-poll` (T5)** | `MAIL_INBOX_POLL_CRON` default `*/2 * * * *` | `mail-inbound/cron/imap-poll.cron.ts` | **Финальный handoff 2026-05-25.** IMAP fetch unseen → парсит → routing на Project |
| **`operations-weekly-digest` (β-8.1)** | `0 * * * *` (фильтр по `Org.timezone`, понедельник `COO_WEEKLY_DIGEST_LOCAL_HOUR`) | `operations/workers/operations-weekly-digest.cron.ts` | Идемпотентно по `(tenantId, weekStart)`. Отправка `coo+owner` через `ConversationalService` (eventType `operations.weekly_digest`). |
| ~~**`commitment-followup` (β-8.2)**~~ **УДАЛЁН 2026-06-29** | — | ~~`operations/workers/commitment-followup.cron.ts`~~ | **Снесён** ТЗ kora-clarify-questions-overhaul Ф2 (probe-инспектор обещаний, только вопросы): удалены сам cron + `specialist-3-9-promise-keeper.service.ts` + `commitment-response.handler.ts`; флаги `betaOps.commitmentFollowupEnabled`/`commitmentFollowupLocalHour`/`commitmentEscalationDays`; поводы `commitment.followup`/`commitment.silence_escalation`. Кора больше не напоминает/не эскалирует по обещаниям. **Остальной соц-слой обещаний (надёжность / сеть / дашборд / `promise-cascade`) тоже снят** — ТЗ commitment-social-layer-cleanup, 2026-07-01 (вместе с cron'ами `promise-cascade.cron` + `PromiseNetworkAnalyzerCron`). Осталась только граница факта (срок + атрибуция автора/адресата). |
| **`operations-daily-digest` (β-8.3)** | `0 22 * * *` UTC (= 01:00 МСК, час настраивается `COO_DAILY_DIGEST_HOUR_UTC`) — **глобальный, не per-Org** | `operations/workers/operations-daily-digest.cron.ts` | Идемпотентно по `(tenantId, dateLocal)` в окне 1 день МСК. Двухстадийная сборка (агрегат → LLM taskType `operations-daily-digest`). Тумблер `AdminSetting.operations.daily_digest.enabled` (+ kill-switch ENV `COO_DAILY_DIGEST_ENABLED`). Отправка в Telegram **только `coo+owner`** (admin исключён) через `ConversationalService.sendNotification(eventType='operations.daily_digest')` — гейтится `AdminSetting.operations.daily_digest.deliver_to_telegram` (default false). |
| **`operations-monthly-digest` («Месяц компании», 2026-06-30)** | `0 * * * *` + глобальный МСК-гейт (1-е число месяца && час===`betaOps.monthlyDigestLocalHour`, default 6) | `operations/workers/operations-monthly-digest.cron.ts` (`OperationsMonthlyDigestCron`) | Генерит за **прошлый** месяц (`shiftPeriod -1`) через `MonthlyDigestService.getOrGenerate` (свод 4 недельных дайджестов одним capable LLM). Идемпотентно по `(tenantId, periodYm)` + `markDelivered`. Доставка `eventType operations.monthly_digest` ролям `owner`/`coo`, `actionUrl /month`. Kill-switch `AdminSetting betaOps.monthlyDigestEnabled` (ON). Метрики `coo_monthly_digest_generated/failed/delivered_total`. |
| **`feedback-digest` (2026-05-25)** | `0 1 * * *` UTC — **глобальный, не per-Org** | `feedback/workers/feedback-digest.cron.ts` | Producer ночного job'а в очередь `core.feedback-digest`. Сам `runDigest()` берёт Redis-lock `feedback:digest:lock` (SET NX EX 1800), забирает батч `FeedbackMessage` (`processedAt=null AND failedRuns<3`, take 1000), зовёт LLM `feedback-cluster` с 2 попытками, транзакционно создаёт `FeedbackTopic` + `FeedbackItem` + `processedAt=now()`. Метрики `feedback_digest_runs_total{result}` + 3 счётчика. См. [[feedback]]. |
| **`checkin-sentiment-batch` (Фаза 2 §6, 2026-05-26)** | `*/5 * * * *` каждые 5 минут | `operations/workers/checkin-sentiment-batch.cron.ts` | `CheckinSentimentBatchCron` забирает накопленные вечерние чек-ины с `sentiment=null` и обрабатывает одним LLM-батчем `checkin-sentiment-batch` (DeepSeek-flash → gpt-5.4-mini → qwen3.5:9b). Заменяет per-event `CheckinSentimentAnalyzerWorker` для экономии токенов и стабильного RPS — старый воркер можно выключить флагом. `max_tokens` поднят пропорционально размеру батча. **Покрыт unit-тестами 2026-05-26** (см. ниже §«Тесты CheckinSentimentBatchCron»). |
| **`sprint-helper-cron` (2026-05-27)** | `0 */4 * * *` (каждые 4 часа) | `knowledge-core/workers/sprint-helper.cron.ts` | `SprintHelperCron` находит активные циклы (`completedAt IS NULL AND endDate >= now`, cap=50) и enqueue `3-13-sprint-helper` в `core.specialist-routing`. Воркер `SprintHelperWorker` собирает контекст (issues + carryOver + last activity + sourceBlockIds → blocks + история подсказок 7д) и вызывает LLM `sprint-helper-suggest`. См. [[sprints]]. |
| **`pending-actions-reminder` (Action Center Часть B, 2026-06-03)** | `0 * * * *` | `pending-actions/.../pending-actions-reminder.cron.ts` | `PendingActionsReminderCron` — Telegram-напоминания о висящих подтверждениях. Срабатывает в слотах 9/12/15/18/21 локального времени, **только при `total>0`**, батч одним сообщением, уважает quietHours/disabledUntil, Redis-dedup per слот, детерминированный шаблон без LLM. Отправка через `ConversationalService` (eventType `actions.reminder`, registry + `EVENT_TYPE_CHANNEL_POLICY`). Метрика `pending_reminder_sent_total`. Также строка «Ждёт подтверждения: N» добавлена в ежедневный дайджест (per-recipient, best-effort). См. [[../02_architecture/module-map]] §pending-actions. |
| **`curation-item-lifecycle` (Action Center Часть B, 2026-06-03)** | `0 2 * * *` | `pending-actions/.../curation-item-lifecycle.cron.ts` | `CurationItemLifecycleCron` (per-Org) — pending `CurationItem` с `expiresAt < now` → `status='expired'` (оживили мёртвый `expiresAt`). Метрика `curation_item_expired_total` + age-гистограмма, best-effort уведомление owner/admin. См. [[../02_architecture/module-map]] §pending-actions. |
| **`recording-track-reconcile` (ТЗ 2026-06-03 recording-reliability, Фаза 1)** | `*/1 * * * *` | `recordings/cron/recording-track-reconcile.cron.ts` | `RecordingTrackReconcileCron` — pull-сверка per-track аудиодорожек. Проходит по `Recording.status ∈ {requested,recording}` (take 50) → `RecordingsService.reconcileTrackEgress`: `listParticipants` → для `kind=STANDARD`+`TrackType.AUDIO` догоняет недостающие track-egress. Kill-switch `RECORDING_TRACK_RECONCILE_ENABLED` (дефолт ON). Идемпотентность: in-process Set-lock + DB-дедуп. Метрика `recording_track_egress_failed_total`. См. [[recording]]. |
| **`composite-egress-reconcile` (ТЗ-2 prod-stability, 2026-06-06)** | `*/1 * * * *` | `webhooks/.../composite-egress-reconcile.cron.ts` | `CompositeEgressReconcileCron` (модуль webhooks, **HTTP-процесс**) — pull-фоллбэк статуса composite-egress, когда вебхук `egress_ended` потерян. Через `listEgress` догоняет фактический статус и доводит запись до `recording_ready` (промоут через `MeetingFinalizationService`). Kill-switch `RECORDING_COMPOSITE_RECONCILE_ENABLED` (дефолт ON). Идемпотентно. Возраст для отбора — по `Meeting.endedAt` (`Recording` не имеет `createdAt`/`updatedAt`, см. [[../02_architecture/code-pitfalls]]). Метрика `livekit_egress_ended_gap_seconds`. См. [[recording]]. |
| **`track-egress-watchdog` (F5, 2026-06-22)** | (рекуррентный, recordings) | `recordings/cron/...` | Сторож зависших аудиодорожек: completed-встреча с дорожкой-плейсхолдером (`s3://`, `bytes=null`) дольше `recording.trackWatchdogTimeoutMinutes` (20) → деградировать пустой трек + переинициировать финализацию → ASR по готовым дорожкам. Чинит «completed, но не отранскрибирована» (обрыв между `recording.faststart` и стартом ASR). Kill-switch `recording.trackWatchdogEnabled` (ON). Метрика `recording_track_watchdog_total{outcome}`. ТЗ [`meeting-to-tracker-and-models-unified-fix`](../../plans/tz/2026-06-22-meeting-to-tracker-and-models-unified-fix.md) F5. См. [[recording]]. |
| **`meeting-reingest` (МТЗ №1 Ф7, 2026-06-04)** | `*/15 * * * *` | `ingest/cron/meeting-reingest.cron.ts` | `MeetingReingestCron` — fallback-страховка моста встреча→граф. Находит встречи с `transcript.turns`, у которых нет `RawEvent`, и идемпотентно гоняет `ingestMeeting`. Подстраховывает основной путь в `analyze.worker` (который теперь при провале пишет `meeting_ingest_failed_total{reason}` и оставляет встречу в `ai_ready`, а не глотает `null`). Зарегистрирован в `IngestModule`. См. [[ingest-and-sources]], [[../02_architecture/module-map]] §Ingest. |
| **`chatbox-sync` (ChatBox-интеграция, 2026-06-05)** | hourly / daily (по `ChatboxIntegration.syncMode`) | `chatbox/chatbox-sync.cron.ts` | `ChatboxSyncCron` — раскладывает incremental-sync по org согласно `syncMode` (hourly/daily); для `realtime` — редкий поллинг-фолбэк (добор пропущенных вебхуков). Kill-switch `AdminSetting chatbox.enabled`. См. [[chatbox-integration]]. |
| **`chatbox-analyze` (ChatBox-интеграция, 2026-06-05; самолечащий sweep 2026-06-23)** | `EVERY_10_MINUTES` (было `EVERY_DAY_AT_MIDNIGHT` — суточный sweep в 00:00, корень ночного пропуска; ТЗ chatbox-analysis-no-stuck-sessions, Гарантия C) | `chatbox/chatbox-analyze.cron.ts` | `ChatboxAnalyzeCron.sweep` — подбирает сессии `analysisStatus='pending'` с `endedAt!=null` **И** застрявшие в `analyzing` (`updatedAt < now − chatbox.analyze.stuckAnalyzingMin`, дефолт 15) → ставит в `chatbox.analyze`. Крутилка `AdminSetting chatbox.analyze.stuckAnalyzingMin`; gauge `z_chatbox_stuck_analyzing_sessions`. `sweepIntervalMin` НЕ вводился (нет механизма динамического крона — кадэнс фиксированный 10 мин). Kill-switch `AdminSetting chatbox.enabled`. См. [[chatbox-integration]], [[ai-jobs]]. |
| **`graph-materialization-verify` (Ф0a оверхол цепочки агентов, 2026-06-08)** | `@Cron` каждые 30 мин | `knowledge-core/workers/graph-materialization-verify.cron.ts` | `GraphMaterializationVerifyCron` (per-Org) — наблюдаемость материализации графа: сверяет, что извлечённые блоки/сущности встречи реально доехали до графа/проекций, и считает расхождение. Метрика `kc_materialization_gap_total{type}`. Дополняет on-demand `GraphMaterializationService` + REST `GET /api/v1/platform/graph/materialization?meetingId=` (SuperAdmin) + `diag graph --meeting <id>`. Источник: [`plans/tz/2026-06-07-agent-chain-overhaul.md`](../../plans/tz/2026-06-07-agent-chain-overhaul.md) Ф0a. См. [[../02_architecture/module-map]] §«Оверхол цепочки агентов». |
| **`goal-theme-linker` (Ф4.2 оверхол цепочки агентов, 2026-06-08)** | `@Cron` каждые 30 мин | `goals/cron/goal-theme-linker.cron.ts` | `GoalThemeLinkerCron` — авто-привязка Goal↔Theme по провенансу (общие `sourceBlockIds`) + co-mention; пишет `GoalTheme(source='ai')`. Также on-event из специалиста `3-14-goals`. Метрика `goal_theme_autolink_total{method}`. Тумблеры `AdminSetting.goals.themeAutolinkMinWeight` (мин. вес связи) / `goals.themeAutolinkLlmEnabled` (доп. LLM-арбитр). Детерминированная часть Ф4 (LLM-арбитр goal-task-link Ф4.1 отложен). Источник: [`plans/tz/2026-06-07-agent-chain-overhaul.md`](../../plans/tz/2026-06-07-agent-chain-overhaul.md) Ф4.2. См. [[goals-and-strategic-alignment]]. |
| **`goal-task-linker` (Ф4.1 остаток цепочки без golden, 2026-06-08)** | `@Cron` каждые 30 мин | `knowledge-core` (`ai/workers.module`) | `GoalTaskLinkerCron` (per-Org, `WorkerOrgGate`) — LLM-арбитр `goal-task-link` (`deepseek-v4-flash`) привязывает AI-цель встречи к её задачам (`Issue.goalId`, non-destructive). Вызов также on-event из специалиста `3-14-goals`. Сервис `GoalTaskLinkerService`. Флаг `AdminSetting.goals.goalTaskLinkEnabled` (default **OFF** — включает владелец после прод-наблюдения). Метрика `z_goal_task_link_total{result}`. Закрывает «LLM-арбитр Ф4.1 отложен» из Ф4.2. Источник: [`plans/tz/2026-06-08-agent-chain-remaining-no-golden.md`](../../plans/tz/2026-06-08-agent-chain-remaining-no-golden.md). См. [[ai-jobs]], [[goals-and-strategic-alignment]]. |
| **`portfolio-health-snapshot` (ТЗ-2 Ф6.A, 2026-06-09)** | `0 5 * * 1` (пн 05:00) | `operations/workers/portfolio-health-snapshot.cron.ts` | `PortfolioHealthSnapshotCron` (per-Org) — недельный снимок здоровья портфеля целей: `PortfolioHealthService.computeForTenant` (интегральный `healthScore` 0..100 + разрезы по `progressStatus`/MoSCoW) → upsert `PortfolioHealthSnapshot` (идемпотентно по `(tenantId, dateLocal)`). Флаг `operations.portfolio_health.enabled` + крутилки `portfolio.health.{threshold_*,weight_*}`. Метрики `portfolio_health_score`/`portfolio_health_snapshot_total`/`portfolio_priority_set_total`. См. [[director-dashboard]], [[../02_architecture/module-map]] §«Батч 5». |
| **`support-sla` (служба поддержки Ф1, 2026-06-09)** | `*/5 * * * *` | `support/crons/support-sla.cron.ts` | `SupportSlaCron` — проходит по тикетам поддержки с просроченным `firstResponseDueAt`/`resolutionDueAt` (взяты из `SupportSlaPolicy` при создании тикета) и проставляет `slaBreachedAt` (для дашборда «горящих») + эскалация. Kill-switch ENV `SUPPORT_DESK_ENABLED`. Источник: [`plans/archive/2026-06-09-support-desk-clone-and-closed-contour-tz.md`](../../plans/archive/2026-06-09-support-desk-clone-and-closed-contour-tz.md) Ф1. См. [[support-desk]], [[../02_architecture/module-map]] §«support». |
| **`support-curator` (служба поддержки Ф4, 2026-06-09)** | `0 3 * * *` (03:00) | `support/crons/support-curator.cron.ts` | `SupportCuratorCron` (per вендор-Org) — ночной рефлексирующий куратор закрытого контура: по дневным `SupportDraftOutcome` + сигналам (реоткрытия/CSAT) через taskType `support-contour-curate` решает `keep`/`promote`/`fix`/`merge`/`archive` по блокам. **Авто, без ручного «одобри»** (R-INV-6). Destructive — только **soft-archive** (`status='archived'`/`supersededAt`, без физического delete) и **только за verdict'ом `MultiAgentDebateService`** (R-INV-2/R23); каждое действие пишет `SupportCuratorAction` (аудит). Идемпотентен в пределах прогона. Kill-switch ENV `SUPPORT_CURATOR_ENABLED`. Источник: ТЗ support-desk-clone Ф4. См. [[support-desk]], [[ai-jobs]]. |
| **`probe-digest` (Probe Фаза 1, 2026-06-11)** | `@Cron('0 * * * *')` (фактически шлёт 1×/день в час `probe.digestHourUtc`, дефолт 9 UTC) | `probe/probe-digest.cron.ts` | `ProbeDigestCron` — батч-дайджест отложенных probe. Собирает `ProbeStatus.queued_digest`, группирует по `(tenantId, recipient)`, шлёт **ОДНО** уведомление `probe.digest` (≤ `probe.digestTouchCap`, дефолт 5), помечает вошедшие `dispatched` (идемпотентно — повтор=no-op). Текст — детерминированный билдер `buildProbeDigestSummary` (`probe/prompts/probe-digest.prompt.ts`, **без LLM**). Kill-switch `probe.digestEnabled` (ВКЛ). Зарегистрирован в `probe.module.ts`. См. [[probe-agent]] §«Фаза 1». |
| **`exec-morning-push` (Мобильная Кора Ф7, 2026-06-11)** | `@Cron` каждый час (фильтр по таймзоне Org, утреннее окно `operations.daily_digest.webpush_morning_hour` default 9) | `operations/workers/exec-morning-push.cron.ts` | `ExecMorningPushCron` — **ПЕРВОЕ подключение браузерного web-push** (раньше `enqueuePushSend` никто не звал). Для owner/admin с `User`: `N = PendingActionsService.getCount().total` «Требует тебя сегодня», при `N>0` → `CoreQueueService.enqueuePushSend(title='Кора', body='Требует тебя сегодня: N', data.url='/dashboard')`. Redis NX-идемпотентность (1 push/сутки на получателя). Kill-switch `operations.daily_digest.deliver_to_webpush` (default ON). Метрика `z_exec_morning_push_delivered_total{channel}`. **Без VAPID-ENV отправка graceful no-op.** Зарегистрирован в `operations.module.ts`. См. [[frontend-pages]] §«Мобильная Кора», [[../../docs/operations/feature-flags|feature-flags]] §VAPID. |
| **`curation-autotune` (Часть A, 2026-06-03)** | `0 3 * * *` | `curation/workers/curation-autotune.cron.ts` | `CurationAutotuneCron` — лестница доверия. **kill-switch (всегда активен):** если `provisionalWrongRate` (из `getProvisionalAuditStats` — доля аудит-выборок с финалом reject/mark_as_misleading/supersede) > `maxProvisionalOverride` и данных достаточно → `provisionalThresholdByType[type]=1.01` (тип возвращается к человеку). **автоподстройка (opt-in `autotuneEnabled`, с 2026-06-12 seed-дефолт **true**):** двигает `autoThresholdByType` по override-rate в пределах guardrail'ов (`thresholdMin/Max`, `autotuneStep`, `minDecisionsForAutotune`). Все сдвиги порогов — через `updateSettings` + `AuditLogService.log` (`curation.kill_switch` / `curation.autotune`). Метрики `curation_kill_switch_total`, `curation_autotune_adjustment_total`. См. [[curation]]. |
| **`conflict-arbiter` (autonomy W1, 2026-06-12)** | `0 2 * * *` (02:00) | `curation/workers/conflict-arbiter.cron.ts` | `ConflictArbiterCron` (per-Org, до `knowledge.curationConflictArbiterBatchSize`=20 конфликтов за проход) — ночной LLM-арбитр open-конфликтов знаний: дебаты `MultiAgentDebateService` (семейство `debate-conflict-arbiter`) → авто-резолв `keep_old`/`accept_new`/`merge` при консенсусе и средней confidence ≥ `knowledge.curationConflictArbiterMinConfidence` (0.7); актор — владелец Org, post-hoc уведомление `system.message`; вердикты `evolving`/`escalate` остаются человеку (HUMAN). Kill-switch `knowledge.curationConflictArbiterEnabled` (ON). Метрика `z_conflict_arbiter_total{verdict,outcome}`. Маршруты — `seed-llm-task-routes-conflict-arbiter.ts` (в STEPS). См. [[ai-jobs]], [[curation]]. |
| **`block-distill-reconcile` (knowledge-core MASTER Б4, 2026-06-16)** | `EVERY_30_MINUTES` | `knowledge-core/workers/block-distill-reconcile.cron.ts` | `BlockDistillReconcileCron` — догоночная сверка draft-блоков. Аудит-фикс Б4: дистилляция могла оставить блок в `draft` без canonical/evidence (потерянные блоки выпадали из графа); cron находит зависшие draft-блоки и доводит их через штатный путь distill. Регистрация — `ai/workers.module`. См. [[ai-jobs]] §«knowledge-core MASTER». |
| **`task-reconcile` (knowledge-core MASTER task-dedup Ф3, 2026-06-16)** | `0 3 * * *` (03:00) | `operations/workers/task-reconcile.cron.ts` | `TaskReconcileCron` (per-Org, образец `BlockerSynthesisCron`) — суточная сверка задач трекера (закрытые/реоткрытые). Считает метрику `task_closure_reopen_rate{tenant_top}` (gauge 0..1 — доля ложных авто-закрытий после accepted-кандидата `TaskClosureCandidate`). Сервис `TaskReconcileService`, kill-switch. Регистрация — `operations.module`. См. [[ai-jobs]] §«task-dedup», [[../02_architecture/module-map]] §pending-actions. |
| **`day-report-collector` (Дневной план/отчёт из графа, 2026-06-29; заменил снесённый `day-signal-aggregator`)** | `@Cron('0 5 * * *', Europe/Moscow)` (за вчера, по всем Org) | `operations/workers/day-report-collector.cron.ts` | `DayReportCollectorCron` — тонкий **НЕ-LLM** сборщик дневного плана/отчёта из разметки `block-ingest` (граф IdeaBlock + evidence), а не из отдельного свипа. Слой `day-signal-*` (детектор+экстрактор+агрегатор+свип) **СНЕСЁН** (дублировал разметку графа и структурно не видел Bitrix/chatbox из-за противофазы таймингов). Логика: перед сборкой `CheckinExpectationService.ensureForDay` гарантирует morning+evening строки-ожидания employee на рабочий день (явный «не дал» = строка осталась `completedAt=NULL`) → `DayReportCollectorService.assembleAndUpsert` собирает 4 сущности (сделано/не сделано/помешало/идеи; «не сделано» — через переиспользуемый `ClosureVerifierService`, атрибуция по `evidence.authorPersonId`+`sourceTimestamp`) и пишет через `upsertFromDaySignal` (патч-стиль, не затирает личный ответ). Redis NX `dayreport:collect:{tenant}:{date}` (EX 25ч) — один прогон/Org/день. Kill-switch `dayReport.enabled` (ON). Метрики `day_report_{collected,block_dropped_no_person,not_done_verify_calls}_total`. ТЗ [`plans/tz/2026-06-29-checkin-day-report-from-graph.md`](../../plans/tz/2026-06-29-checkin-day-report-from-graph.md). См. [[ai-jobs]] §«Универсальный фиксатор чек-инов». |
| **`subject-memory-activation` (Слой 3 «Память субъекта», 2026-06-22)** | `@Cron('35 * * * *')` (ежечасно) | `probe/subject-memory/subject-memory-activation.cron.ts` | `SubjectMemoryActivationCron` — три прохода активации выученных правил: `promoteShadowRules` (shadow→canary→active за judge-ансамблем `subject-memory-judge`, кворум `subjectMemory.judgeQuorum` 2 разными моделями `subjectMemory.judgeModels`), `evaluateCanaryRules` (авто-rollback canary в окне `subjectMemory.canaryRollbackWindowHours` 48 при refute), `decayStaleRules` (TTL по `subjectMemory.ttlDays` 180). Kill-switch `subjectMemory.enabled` (ON). Метрики `subject_memory_rule_activated_total`/`subject_memory_rule_rolled_back_total{cause}`. См. [[ai-jobs]] §«Память субъекта», [[probe-agent]]. |
| **`company-summary-compiler` (Слой 1 «Память субъекта», 2026-06-22)** | `@Cron('45 * * * *')` (ежечасно, гейт по `companyProfile.summaryRebuildHours` 24) | `company-foundation/workers/company-summary-compiler.cron.ts` | `CompanySummaryCompilerCron` (per-Org) — собирает «Чем занимается компания» из топ canonical-IdeaBlock по графу через taskType `company-summary-compile` → `CompanyProfile.applyAutoSummary` (`summaryJson`). Гейты: не перетирает `summaryPinned` (Р1), пропускает несвежие (`summaryRebuildHours`) и cold-start (< `companyProfile.summaryMinSourceBlocks` 8 блоков). Kill-switch `companyProfile.autoSummaryEnabled` (ON). Метрика `company_summary_compile_total{result}`. Capsule подмешивается в SYSTEM chat-v2/concierge (`company_capsule_injected_total{surface}`). См. [[ai-jobs]] §«Память субъекта». |
| **`raw-event-recovery` (task-decision-execution-unified Ф5, 2026-06-27)** | `@Cron('*/15 * * * *')` | `knowledge-core/workers/raw-event-recovery.cron.ts` | `RawEventRecoveryCron` — страховка от потери `RawEvent` при затяжном флапе LLM. Сканирует `RawEvent` застрявшие в `processingStatus='received'` дольше `knowledge.rawEventRecoveryStaleMinutes` (30) (block-ingest исчерпал BullMQ-ретраи и не довёл событие) → реэнкьюит в block-ingest через `CoreQueueService.enqueueRawReceived(id,{suffix:'recovery'})`. Идемпотентно (block-ingest гейтит по `'received'`). Древние (> `knowledge.rawEventRecoveryMaxAgeHours` 24) НЕ реэнкьюим (анти-бесконечность) → dead-letter ERROR-алерт оператору. Батч `knowledge.rawEventRecoveryBatchLimit` (200), in-process Set-lock. Kill-switch `knowledge.rawEventRecoveryEnabled` (ON). Метрики `raw_event_recovery_reenqueued_total` / `raw_event_recovery_dead_lettered_total`. Регистрация — `ai/workers.module`. Источник: [`plans/tz/2026-06-27-task-decision-execution-unified-tz.md`](../../plans/tz/2026-06-27-task-decision-execution-unified-tz.md) Ф5. См. [[ai-jobs]] §«Надёжность пайплайна». |
| **`morning-tasks-digest` (Утренняя сводка задач, 2026-06-29)** | `@Cron('0 * * * *', Europe/Moscow)` (ежечасный тик + гейт по часу `tracker.morningDigest.hourMsk` дефолт 9 МСК) | `tracker/workers/morning-tasks-digest.cron.ts` | `MorningTasksDigestCron` — персональный утренний дайджест открытых задач. В нужный МСК-час перебирает `Org(deletedAt=null)`, по каждому: активные сотрудники (`Membership × User.deletedAt=null`) + открытые `Issue` по `IssueAssignee` (через `MorningTasksDigestService`), группирует по сотруднику и в группы Просрочено/Срок сегодня/В работе/Запланировано (`buildTasksDailyOpenPayload`, лимит `maxItemsTotal` 50 + «и ещё N»). Per-user дедуп по существующей `Notification` (eventType `tasks.daily_open`, `createdAt ≥ начало МСК-суток` — **без новой таблицы**) → отправка через `ConversationalService.sendNotification` (`dataClass:'internal'`, `preferredChannelKinds` = `tracker.morningDigest.channels`, `priorityTier:3`). Пустой день → «всё чисто» (если `sendWhenEmpty`). Per-user try/catch (best-effort). **Без LLM** (детерминированный билдер). Kill-switch `tracker.morningDigest.enabled` (ON). Метрика `z_tracker_morning_digest_total{is_empty}`. Рендер `tasks.daily_open` в telegram/max/email-адаптерах + кабинет. Источник: [`plans/tz/2026-06-29-morning-tasks-digest.md`](../../plans/tz/2026-06-29-morning-tasks-digest.md). См. [[tracker]]. |
| **`task-clarify-sweep` (Пересмотр уточняющих вопросов, 2026-06-29)** | `@Cron` hourly `Europe/Moscow` (фактически шлёт в час `tracker.taskClarifySweep.hourMsk` дефолт 10 МСК) | `tracker/workers/task-clarify-sweep.cron.ts` (зарегистрирован в `src/modules/ai/workers.module.ts`) | `TaskClarifySweepCron` — ежедневный добор уточняющих вопросов по задачам: pending `IntakeIssue` старше `tracker.taskClarifySweep.minAgeHours` (20) без исполнителя/срока → `Specialist315TasksService.runClarifySweep` → probe `task.assignee_unresolved`/`task.due_date_missing` **постановщику** (автору реплики, резолвер `resolveSetterRecipient`, фолбэк owner Org). Дедуп probe = идемпотентность (повтор не плодит вопрос). Закрывает то, что не сработало на извлечении. **Без LLM.** Kill-switch `tracker.taskClarifySweep.enabled` (ON). Источник: [`plans/tz/2026-06-29-kora-clarify-questions-overhaul.md`](../../plans/tz/2026-06-29-kora-clarify-questions-overhaul.md) Ф4. См. [[tracker]], [[probe-agent]], [[probe-observers-catalog]]. |

## Финальный handoff Wave 1-3 — новые воркеры (2026-05-25)

### T5 — Email-to-task (mail-inbound)

**Cron `ImapPollCron`** — `@Cron(MAIL_INBOX_POLL_CRON)` (default `*/2 * * * *`):
1. Открывает IMAP connection (imapflow, lifecycle: connect → fetch unseen → mark seen → close).
2. Для каждого `unseen` сообщения: mailparser → дёргает `MailInboundService.handleMessage(parsed)`.
3. `MailInboundService` ищет `Project.emailInboxAlias` по `To:`, идемпотентность по `Message-ID @unique` → routing → `Issue` или `IntakeIssue` + S3 attachments → log в `MailInboundLog`.
4. Метрики `z_mail_inbound_{received,routed,rejected,duplicates}_total`.

**ENV (9 новых):** `MAIL_IMAP_HOST/PORT/USER/PASSWORD/TLS/MAILBOX`, `MAIL_INBOX_POLL_CRON`, `MAIL_INBOX_DOMAIN`, `MAIL_ATTACHMENT_MAX_BYTES` (default 26214400 = 25 MB).

**ВАЖНО:** cron зарегистрирован в `@nestjs/schedule` и тикает **in-process внутри основного backend'а** (отдельного worker-процесса нет — см. §«Топология» в шапке), процесс один, двойного pull нет. Guard `cfg.mailInbox.enabled` дополнительно проверяется в `ProjectInboxService.pollInbox()` — на dev'е cron всё равно тикает, но сразу выходит. Файл — `src/modules/mail/inbound/imap-poll.cron.ts`.

### T4 — Voice Streaming (без воркера)

**`VoiceStreamGateway`** в `concierge/gateways/voice-stream.gateway.ts` (namespace `/ws/voice`).

⚠ **Это WebSocket gateway, НЕ воркер и НЕ cron.** Сессии хранятся **in-memory** (`Map<userId, VoiceSession>`), TTL 60s, 1 session per user, buffer cap 5 MB. При `voice.end` синхронно отдаём в `VoxAdapter.transcribe()` (poll-модель).

Не используется BullMQ — задержка важна, очередь только добавила бы latency. Risk: при рестарте процесса все активные сессии теряются (приемлемо для voice-капчи — пользователь просто повторит).

**TODO:** миграция на streaming ASR (Whisper realtime / GigaAM streaming) — тогда понадобится отдельный stateful worker или per-session pod.

## Принципы

1. **Воркеры и cron'ы — in-process в основном backend'е.** `WorkersModule` импортирован в `AppModule`, отдельного worker-процесса/контейнера нет (см. §«Топология» в шапке). HTTP-запросы обслуживает тот же процесс.
2. **Все cron используют `WorkerOrgGate`** — admin может выключить per-Org через `Org.workersEnabled`.
3. **Идемпотентность через jobId** — все важные воркеры используют детерминированный `jobId` (формат `<entityType>_<entityId>_<hash>`) для дедупликации.
4. **HNSW pgvector индексы** — для всех embedding-полей. Создаются скриптом `bun run apply-postgres-init` (НЕ в `schema.prisma`).
5. **Метрики Prometheus** — каждая очередь имеет `<queue>_total{tenant, status}` counter.

## SBA β-8.1 + β-8.2 — добивка панели COO + Хранитель обещаний (2026-05-25)

**Источник:** [`plans/archive/2026-05-24-sba-beta-8-1-coo-dobivka.md`](../../plans/archive/2026-05-24-sba-beta-8-1-coo-dobivka.md), [`plans/archive/2026-05-24-sba-beta-8-2-promise-keeper.md`](../../plans/archive/2026-05-24-sba-beta-8-2-promise-keeper.md).

### Воркеры (event-driven, без BullMQ-очереди)

| Worker | Триггер | Файл | Что делает |
|---|---|---|---|
| `CheckinSentimentAnalyzerWorker` | `@OnEvent('checkin.created')` после `CheckinResponseHandler.upsertFromParser` или ручного `POST /me/check-ins` | `operations/workers/checkin-sentiment-analyzer.worker.ts` | Один вызов LLM `checkin-sentiment` (timeout 30с). Только вечерние чек-ины. Best-effort: при ошибке — `sentiment=null`, чек-ин остаётся. |
| `PersonalRelationBuilderWorker` (β-8, уже было) | event-driven | `operations/workers/personal-relation-builder.worker.ts` | — |
| ~~`CommitmentResponseHandler` (β-8.2)~~ **УДАЛЁН 2026-06-29** | — | ~~`operations/services/commitment-response.handler.ts`~~ | **Снесён** ТЗ kora-clarify-questions-overhaul Ф2 (раньше: LLM `commitment-extract-status` → `IdeaBlock(signalType='commitment_status')` + `IdeaBlockLink(type='resolves')` + обновление статуса). Механизм надзора над статусом обещания удалён целиком. |
| `MeetingCheckinListener` (Дневной план/отчёт из графа, переписан 2026-06-29) | `@OnEvent('meeting.ai_ready')` | `operations/workers/meeting-checkin.listener.ts` | Реактивный мост встреча→чек-ин, переписан на **graph-derive** (БЕЗ `DaySignalDetector`/`DaySignalExtractor`): резолв участников-сотрудников встречи (`EntityResolutionService.resolveSubjectPersonId`, гости без `personId` пропускаются) → `DayReportCollectorService.assembleAndUpsert(personIds)` за день встречи (same-day) из её разметки `block-ingest`. Kill-switch `dayReport.enabled`. ТЗ [`plans/tz/2026-06-29-checkin-day-report-from-graph.md`](../../plans/tz/2026-06-29-checkin-day-report-from-graph.md). См. [[ai-jobs]] §«Универсальный фиксатор чек-инов». |

~~Маршрутизатор `knowledge-core/services/router.service.ts` для `signalType='commitment_status'` эмитит `commitment.status_received`.~~ **Снято** (ТЗ commitment-social-layer-cleanup, 2026-07-01, Ф1): case `commitment_status` удалён из роутера; enum-значение `SignalType.commitment_status` осталось осиротевшим.

## История

- **2026-05-25:** создан в рамках финального handoff Wave 1-3. Добавлен `imap-poll` cron (T5), документировано отсутствие воркера для voice WS (T4).
- **2026-05-25 (β-8.1/β-8.2):** добавлены `operations-weekly-digest` и `commitment-followup` cron'ы + event-driven worker'ы `CheckinSentimentAnalyzerWorker` и `CommitmentResponseHandler`.
- **2026-05-25 (β-8.3):** добавлен глобальный cron `operations-daily-digest` (`0 22 * * *` UTC = 01:00 МСК) + сервис `DailyDigestService` (двухстадийная сборка). См. [`plans/archive/2026-05-25-sba-beta-8-3-coo-daily-and-doelka.md`](../../plans/archive/2026-05-25-sba-beta-8-3-coo-daily-and-doelka.md).
- **2026-05-26 (миграция LLM на DeepSeek V4 Pro, Фазы 0-8):** добавлен `CheckinSentimentBatchCron` (`*/5 * * * *`, batch-режим вместо per-event) и `SpecialistsCombinedWorker` на новой очереди `core.specialists-combined` (Б+ объединённый вызов 8 сущностей одним LLM, флаг `SPECIALISTS_COMBINED_ENABLED`). См. [`plans/archive/2026-05-25-llm-architecture-changes-from-experiments.md`](../../plans/archive/2026-05-25-llm-architecture-changes-from-experiments.md).
- **2026-05-26 (follow-up Задача 1 + Задача 3):** добавлены unit-тесты `CheckinSentimentBatchCron` + парсера (8 + 10 кейсов) и откатный скрипт миграции LLM (см. ниже).
- **2026-06-02 (Goals OKR v2):** в `core.specialist-routing` добавлен consumer `3-14-goals` (авто-добыча целей, триггер commitment+plan_item). Новые cron'ы `goal-kr-progress` (`0 5 * * *`, авто-прогресс KR) и `goals-pulse` (`0 6 * * 1`, еженедельный пульс целей через `ConversationalService`). См. [[goals-and-strategic-alignment]] §«Goals OKR v2», ТЗ `plans/archive/2026-06-02-goals-okr-v2.md`.
- **2026-06-03 (Action Center Часть B):** добавлены `PendingActionsReminderCron` (`0 * * * *`, Telegram-напоминания о висящих подтверждениях, слоты 9/12/15/18/21, eventType `actions.reminder`, метрика `pending_reminder_sent_total`) и `CurationItemLifecycleCron` (`0 2 * * *`, per-Org expiry `CurationItem.expiresAt`). Модуль `backend/src/modules/pending-actions/`. Источник: [plans/tz/2026-06-02-action-center-pending-confirmations.md](../../plans/tz/2026-06-02-action-center-pending-confirmations.md) (Часть B).
- **2026-06-04 (МТЗ №1 «Разблокировка конвейера»):** `core.specialist-routing` переведён на **один `SpecialistRoutingDispatcherWorker`** вместо 14 конкурирующих Worker'ов (Ф2); диспатч специалистов перенесён с draft-блоков на **canonical** в `block-distill.worker` (Ф3); добавлен fallback-cron `meeting-reingest` (`*/15`, Ф7). Новые метрики `core_specialist_skipped_total{specialist,reason}`, `meeting_ingest_failed_total{reason}`, `kc_typed_entity_failed_total{type,reason}`. Источник: [plans/tz/2026-06-04-razblokirovka-konveyera.md](../../plans/tz/2026-06-04-razblokirovka-konveyera.md). Подробности — [[../02_architecture/module-map]] §«Разблокировка конвейера».
- **2026-06-06 (ТЗ-2 надёжность записи):** новый cron `composite-egress-reconcile` (`*/1`, модуль webhooks, HTTP-процесс) — pull-фоллбэк потерянного вебхука `egress_ended` через `listEgress`, флаг `RECORDING_COMPOSITE_RECONCILE_ENABLED` (дефолт ON), метрика `livekit_egress_ended_gap_seconds`. Промоут-логика встречи (FSM `completed`→`recording_processing`→`recording_ready` + enqueueTranscribe + faststart) вынесена в `MeetingFinalizationService` (webhooks) — общий вход и для вебхука, и для reconcile-cron. Ack-first вебхуков — за флагом `LIVEKIT_WEBHOOK_ACK_FIRST_ENABLED` (дефолт OFF). См. [[../02_architecture/module-map]], [[recording]].
- **2026-06-08 (оверхол цепочки агентов, ветка `feature/retest2-agent-chain-overhaul`):** два новых `@Cron` (каждые 30 мин): `graph-materialization-verify` (`GraphMaterializationVerifyCron`, Ф0a, наблюдаемость материализации графа, метрика `kc_materialization_gap_total{type}`) и `goal-theme-linker` (`GoalThemeLinkerCron`, Ф4.2, авто-привязка Goal↔Theme, метрика `goal_theme_autolink_total{method}`). Также: диспетчер `core.specialist-routing` теперь оборачивает специалистов слоя 3 в pipeline-контекст `KNOWLEDGE_GRAPH` с `traceId=mtg_<id>` (Ф0b — видны в `diag chain`) + логи created/skipped/merged. Источник: [`plans/tz/2026-06-07-agent-chain-overhaul.md`](../../plans/tz/2026-06-07-agent-chain-overhaul.md). Подробности — [[../02_architecture/module-map]] §«Оверхол цепочки агентов», [[ai-jobs]].
- **2026-06-08 (качество клона сотрудника, TZ#2, ветка `feature/2026-06-08-tz-batch-tables-clones-shipon`):** новый `SkillTraitVerifyCron` (`30 3 * * *`, grounding-проверка свежих черт в `pending_verification` через LLM `skill-trait-verify`). Класс-фикс decay (одна ступень за проход) — в `runDecay` и `SkillProfileRecalibrateCron`. Источник: [`plans/tz/2026-06-08-clone-quality-improvements.md`](../../plans/tz/2026-06-08-clone-quality-improvements.md). См. [[skill-and-clone]] §«Доработки 2026-06-08».
- **2026-06-09 (батч 5 — дашборды + загрузка/импорт, ветка `feature/2026-06-08-daily-value-dashboards-uploads`):** 3 новые очереди — `core.document-import` (`DocumentImportWorker`, batch ZIP/Notion/Confluence → Document'ы), `meeting.upload-ingest` (`MeetingUploadIngestWorker`, ffmpeg-нормализация, concurrency=1) и `meeting.upload-transcribe` (`MeetingUploadTranscribeWorker`, Vox-диаризация → гейт `awaiting_speakers` без анализа). Новый cron `PortfolioHealthSnapshotCron` (`0 5 * * 1`, недельный снимок здоровья портфеля целей). Источники: ТЗ-2/ТЗ-4/ТЗ-5. Подробности — [[../02_architecture/module-map]] §«Батч 5», [[../02_architecture/data-model]] §«Батч 5».
- **2026-06-11 (Мобильная Кора Ф7 + ChatBox блок A, ветка `feature/remaining-handoff`):** новый cron `ExecMorningPushCron` (`exec-morning-push.cron.ts`, hourly, утреннее окно по таймзоне) — **первое подключение браузерного web-push** через `enqueuePushSend` (раньше очередь `core.push-send` никто не наполнял из крон-слоя): owner/admin с `N>0` ожидающих → push «Требует тебя сегодня: N» → `/dashboard`, NX-идемпотентность 1/сутки, kill-switch `operations.daily_digest.deliver_to_webpush`, крутилка часа `operations.daily_digest.webpush_morning_hour`, метрика `z_exec_morning_push_delivered_total{channel}`; зарегистрирован в `operations.module.ts`. Без VAPID-ENV — graceful no-op. Также: prom-метрики ChatBox-синка/анализа (`z_chatbox_syncs_total`/`z_chatbox_analyzes_total`/`z_chatbox_pending_sessions`/`z_chatbox_last_sync_ts_seconds`) добавлены в `BusinessMetricsService` + инкременты в `chatbox-sync.worker`/`chatbox-analyze.worker`/`chatbox-analyze.cron`. Источник: [`plans/tz/2026-06-11-mobile-cora-exec-manager.md`](../../plans/tz/2026-06-11-mobile-cora-exec-manager.md) (Ф7). См. [[frontend-pages]] §«Мобильная Кора», [[api-layer]] §ChatBox.
- **2026-06-11 (Probe-система Фаза 1, ветка `svdev`, коммиты `5ed78b54..fa950cd1`):** новый cron `ProbeDigestCron` (`probe/probe-digest.cron.ts`, `@Cron('0 * * * *')`, фактически 1×/день в `probe.digestHourUtc`) — собирает отложенные probe в статусе `queued_digest` и шлёт ОДНО уведомление `probe.digest` (≤ `probe.digestTouchCap`), детерминированный билдер без LLM, kill-switch `probe.digestEnabled`. Также: `ProbeDispatcherWorker` получил recheck повода перед dispatch (`suppressed_stale`, Ф4); `ProbePriorityCron` теперь инжектит `RedisService`+`TypedConfigService` (Ф5 adaptive fatigue: engagement-снимок + topic cooldown `probe:cooldown:*`); метрика `probe_outcome_total{outcome,reason}`. Источник: [`plans/tz/2026-06-11-probe-system-upgrade-phase1.md`](../../plans/tz/2026-06-11-probe-system-upgrade-phase1.md). Подробности — [[probe-agent]] §«Фаза 1».
- **2026-06-12 (единый помощник в каналах + автономизация, ветка `feature/assistant-channels-and-autonomy`):** новый cron `ConflictArbiterCron` (`0 2 * * *`, ночной LLM-арбитр конфликтов знаний, см. таблицу выше). Также: `PendingActionsReminderCron` — новые дефолты каденции (окно 9..9, шаг 12 → единственный слот 09:00, одна сводка/день); `ProbeDispatcherWorker`/`ProbeService` — гейт ценности `probe.minValuePriority` (`dropped_low_value`), маршрутизация NUDGE/низкого приоритета в дайджест (`routed_to_digest`, порог `probe.immediatePushMinPriority`), cold-start теперь откладывает в дайджест, а не дропает; `intake-auto-triage.worker` — авто-приём задач из ВСЕХ каналов (порог 0.75, null-исполнитель → владелец Org с пометкой, дефолт-проект «Входящие», метрики source/via_default_project). Новый событийный мост (не cron): `AssistantChannelBridge` (concierge) — inbound `assistant_turn` из Telegram/MAX → ConciergeService. Источники: [`plans/archive/2026-06-11-autonomy-remove-manual-confirmations.md`](../../plans/archive/2026-06-11-autonomy-remove-manual-confirmations.md), [`plans/archive/2026-06-11-assistant-channels-telegram-max.md`](../../plans/archive/2026-06-11-assistant-channels-telegram-max.md). См. [[conversational-channels]] §«Единый мозг помощника», [[ai-jobs]].
- **2026-06-09 (служба поддержки Ф1–Ф4, модуль `support`):** два новых `@Cron` — `SupportSlaCron` (`*/5 * * * *`, проставляет `slaBreachedAt` просроченным тикетам поддержки, kill-switch `SUPPORT_DESK_ENABLED`) и `SupportCuratorCron` (`0 3 * * *`, ночной куратор закрытого контура памяти: soft-archive/fix/merge за debate-гейтом, пишет аудит `SupportCuratorAction`, kill-switch `SUPPORT_CURATOR_ENABLED`). **Без новых BullMQ-очередей** — клон-черновик/critic/classify считаются синхронно в пути деска. Источник: [`plans/archive/2026-06-09-support-desk-clone-and-closed-contour-tz.md`](../../plans/archive/2026-06-09-support-desk-clone-and-closed-contour-tz.md). См. [[support-desk]], [[../02_architecture/module-map]] §«support».
- **2026-06-12 (слой метода клона, ветка `feature/clone-persona-method-layer`):** два новых `@Cron` — `RolePrincipleSynthesisCron` (`30 5 * * *`, Reflection-синтез принципов роли `RolePrinciple` из subject-reasoning блоков должности, kill-switch `ROLE_PRINCIPLE_SYNTHESIS_ENABLED`) и `PersonaLayerValidationCron` (`0 7 * * SUN`, поведенческая A/B-оценка persona v1-vs-v2 через судью `persona-behavior-judge`, kill-switch `PERSONA_LAYER_VALIDATION_ENABLED`). **Без новых BullMQ-очередей** — детекторы `value-motivation-detect` / `process-marker-detect` живут вторым/третьим проходом ВНУТРИ существующего конвейера `core.skill-profile-rebuild` (`Specialist37Service.rebuildProfile`), CDM-интервью — через существующую probe-систему (`core.probe-events`). Источник: [`plans/archive/2026-06-11-clone-persona-method-layer.md`](../../plans/archive/2026-06-11-clone-persona-method-layer.md). См. [[skill-and-clone]] §«Доработки 2026-06-12», [[ai-jobs]].
- **2026-06-16 (один человек = один клон должности + 21 баг конвейера M5, ветка `devsv`):** **новых cron'ов и очередей НЕТ** — изменения внутри существующих кронов/конвейера M5 (Раздел 8 ТЗ). Поправлено: `SkillProfileRecalibrateCron`/`runDecay` (перевод застрявших `pending_verification` → `archived`, Б1; `orderBy`+курсор, Б5); `verifyPendingTraits` (выборка FIFO `orderBy createdAt asc`, Б2; предфильтр `<2` цитат без LLM, Г3); нормализатор концептов (`traitCount=COUNT(active)`+`recomputeTraitCount`, Б6; архив по `NOT EXISTS active`, Б7; P2002-коллизия имени, Б8); `RolePrincipleSynthesisCron` (`confidence` из `distinctDays`, Б9; embedding-update обёрнут, Б10; бюджет только при LLM-вызове, Б11); `ExecutablePersonaBuildCron`+`ExecutablePersonaTriggerWatcherCron` (`orderBy lastBuildAt asc nulls-first`+курсор, Б14; предфильтр `layer='skill'`, Б15). **Модель носителей клона** (Раздел 7): `executable-persona-build.service.ts buildForRole` строит клон из ОДНОГО носителя + freeze прошлой версии (`PersonaStatus.frozen`); замороженные персоны исключены из decay/recalibrate/build/нормализации (И6); Redis-лок `persona:rebuild:role:<id>`. ⚠ **Role-scope staleness-watcher не покрыт** (см. [[../04_не-сделано/README]]). Источник: [`plans/tz/2026-06-16-clone-agents-prompt-revision.md`](../../plans/tz/2026-06-16-clone-agents-prompt-revision.md). См. [[skill-and-clone]] §«Доработки 2026-06-16», [[ai-jobs]].
- **2026-06-16 (knowledge-core MASTER, ветка `feature/knowledge-core-master` от `origin/dev`):** **новая очередь** `core.goal-embed` (`GoalEmbedWorker`, concurrency 4 — embedding целей для KNN-дедупа, task-dedup Ф5) + **удалена мёртвая очередь** `core.idea-clusterer` (аудит Б9 [K10] — on-create-дедуп никем не питался; кластеризация идей полностью на `@Cron` `IdeaClustererCron` `30 */4 * * *`). **3 новых `@Cron`:** `BlockDistillReconcileCron` (`EVERY_30_MINUTES`, аудит Б4 — догон зависших draft-блоков) и `TaskReconcileCron` (`0 3 * * *`, task-dedup Ф3 — суточная сверка задач + метрика `task_closure_reopen_rate`). **Новый event-handler** (не cron): `TaskCompletionHandler` (`@OnEvent('task.completion_signalled')`, эмитит `RouterService`) → создаёт `TaskClosureCandidate`. **`core.block-distill` concurrency 2→1** (аудит Б11 [K4] — сериализация против draft-дублей). `core.specialists-combined` removeOnComplete count поднят 1000→20000 (Б33 [K6]). Также волна 2 — 30 промптов knowledge-core переписаны по методологии (code-промпты, без seed). Источник: [`plans/tz/2026-06-16-knowledge-core-MASTER.md`](../../plans/tz/2026-06-16-knowledge-core-MASTER.md) + [`plans/tz/2026-06-16-task-dedup-and-tracker-reconcile.md`](../../plans/tz/2026-06-16-task-dedup-and-tracker-reconcile.md). Подробности — [[ai-jobs]] §«knowledge-core MASTER» / §«task-dedup», [[../02_architecture/data-model]] §«task-dedup». **G5 (DLQ + per-queue override `CORE_DEFAULT_JOB_OPTIONS`) — отложено** (ТЗ-заглушка `plans/tz/2026-06-17-knowledge-core-queue-dlq-per-queue-config.md`; острое снято Б32/Б33).
- **2026-06-30 («Месяц компании», ветка `feature/month-company-and-report-navigation`):** **новый `@Cron`** `OperationsMonthlyDigestCron` (`operations/workers/operations-monthly-digest.cron.ts`, `@Cron('0 * * * *')` + глобальный МСК-гейт «1-е число && час===`betaOps.monthlyDigestLocalHour`, default 6») — генерит `MonthlyOperationsDigest` за прошлый месяц (`shiftPeriod -1`) через `MonthlyDigestService.getOrGenerate` (свод 4 недельных дайджестов одним capable LLM `operations-monthly-digest`), идемпотентно по `(tenantId, periodYm)` + `markDelivered`, доставка `eventType operations.monthly_digest` ролям `owner`/`coo`, `actionUrl /month`. Kill-switch `betaOps.monthlyDigestEnabled` (ON). **Без новых BullMQ-очередей.** Метрики `coo_monthly_digest_generated/failed/delivered_total`. Источник: `plans/tz/2026-06-29-month-company-monthly-brief.md`. См. [[director-dashboard]] §«Месяц компании», [[ai-jobs]] §«operations-monthly-digest».
- **2026-06-23 (убрать лишние подтверждения, ветка `feature/2026-06-23-remove-manual-confirmations`):** **новый `@Cron`** `company-profile-completeness` (`company-profile-completeness.cron.ts`, in-process через WorkersModule, Блок B) — при пустых миссии/видении/стратегии профиля компании поднимает probe с готовым черновиком из памяти (reason'ы `companyprofile.missing_mission/vision/strategy`, запись через `CompanyProfileService`), kill-switch `companyProfile.completenessProbeEnabled` (OFF → cron no-op). **Без новых BullMQ-очередей.** Также (не cron): `SubjectMemoryService.sweepPendingDuplicates` гасит висящие pending-probe по выученному правилу (статус `ProbeStatus.suppressed_by_memory`, kill-switch `subjectMemory.sweepPendingOnLearnEnabled`, метрика `subject_memory_pending_swept_total`); судья курации видит первоисточник + серая зона (Блок A — внутри существующего пути `CurationService`, не cron). Источник: [`plans/tz/2026-06-23-remove-manual-confirmations-master-tz.md`](../../plans/tz/2026-06-23-remove-manual-confirmations-master-tz.md). Подробности — [[probe-agent]] §«Наблюдаемость самообучения SubjectMemory», [[curation]] §«ИИ-судья видит первоисточник», [[ai-jobs]] §«Probe-черновики из памяти».
- **2026-06-29 (Дневной план/отчёт из графа, ветка `work/2026-06-29`, коммиты `114fc0f4..0ab7090b`):** **СНЕСЁН** весь слой `day-signal-*` (`DaySignalAggregator/Detector/Extractor` + `DaySignalAggregatorCron` + spec + prompt + taskType `day-signal-detect` + метрики `day_signal_*`). **Новый `@Cron`** `DayReportCollectorCron` (`day-report-collector.cron.ts`, `@Cron('0 5 * * *', Europe/Moscow)`) — собирает план/отчёт за вчера из разметки `block-ingest` (тонкий НЕ-LLM сборщик `DayReportCollectorService.assembleAndUpsert`), перед сборкой `CheckinExpectationService.ensureForDay` (строки-ожидания employee → явный «не дал»). `MeetingCheckinListener` переписан на graph-derive (резолв участников `EntityResolutionService` → `assembleAndUpsert(personIds)`). «Не сделано» — через переиспользуемый `ClosureVerifierService` (вынесен из `task-completion.handler`). Крутилки `daySignals.*` → `dayReport.enabled` (kill-switch ON) + `dayReport.completenessQualityThreshold` (0.5). Метрики `day_report_{collected,block_dropped_no_person,not_done_verify_calls}_total`. **Без новых BullMQ-очередей.** Новые спеки: `day-report-collector` / `closure-verifier` / `checkin-expectation`. Источник: [`plans/tz/2026-06-29-checkin-day-report-from-graph.md`](../../plans/tz/2026-06-29-checkin-day-report-from-graph.md). Подробности — [[ai-jobs]] §«Универсальный фиксатор чек-инов», [[../02_architecture/data-model]] §«`DailyCheckIn` += 4 сущности отчёта».
- **2026-07-01 (снос соц-слоя обещаний, ТЗ commitment-social-layer-cleanup):** удалены остатки надзора над обещаниями — cron'ы `promise-cascade.cron` (`PromiseCascade*`) и `PromiseNetworkAnalyzerCron` (@Cron Mon 05:00), event-handler `CommitmentResponseHandler` (см. выше, помечен), сервисы `PromiseNetworkService`/`CommitmentReliabilityService`/`commitments.service`, маршрут роутера `commitment_status`, taskType `commitment-extract-status`, метрика `promise_cascade_alert_total` и 5 метрик β-8.2 «Хранитель обещаний». Дроп колонок `IdeaBlock.commitmentStatus/AskedAt/EscalatedAt` + модели `PromiseNetworkSnapshot` (миграция `20260701000000_drop_commitment_social_layer`). Осталась **граница факта**: `commitmentDueDate` + атрибуция автора/адресата. **Без новых cron/очередей** (только снос). Коммиты Ф1-Ф6.
- **2026-06-29 (Пересмотр уточняющих вопросов Коры, ветка `work/2026-06-29`):** **МИНУС** cron `commitment-followup` (probe-инспектор обещаний снесён, Ф2) — заодно изнутри `specialist-3-3-probe.service.ts` удалён внутренний 05:00 UTC-проход решений (поводы `decision.*` сняты, Ф1; это не была отдельная строка @Cron-таблицы). **ПЛЮС новый `@Cron`** `TaskClarifySweepCron` (`task-clarify-sweep.cron.ts`, hourly `Europe/Moscow`, в `src/modules/ai/workers.module.ts`) — ежедневный добор pending-задач без исполнителя/срока → probe постановщику (Ф4). **ПЛЮС событийный триггер `task-method-capture`** (НЕ cron) — хук в `IssuesService.transitionState` при ПЕРВОМ переходе задачи в completed (gate `!existing.completedAt`) → `maybeRaiseMethodCaptureProbe` шлёт исполнителю probe `task.method_capture` «расскажи, как решал» на значимых задачах (эмиттеры `task-method-capture` / `specialist-3-15-tasks-sweep`); ответ помечается `signalTypeHint:'reasoning'`. 5 крутилок `tracker.taskClarifySweep.{enabled,hourMsk(10),minAgeHours(20)}` + `tracker.methodCaptureEnabled` + `.methodCaptureMinComplexity(0.5)` + `.methodCapturePriorityHint(0.7)`. **Без новых BullMQ-очередей.** Источник: [`plans/tz/2026-06-29-kora-clarify-questions-overhaul.md`](../../plans/tz/2026-06-29-kora-clarify-questions-overhaul.md). Подробности — [[probe-agent]] §«Пересмотр уточняющих вопросов», [[probe-observers-catalog]], [[tracker]].

## Тесты CheckinSentimentBatchCron (2026-05-26)

**Источник:** [`plans/archive/2026-05-26-checkin-batch-cron-tests.md`](../../plans/archive/2026-05-26-checkin-batch-cron-tests.md). Закрытие gap из Фазы 2 миграции LLM.

- `checkin-sentiment-batch.cron.spec.ts` — 8 кейсов (батчинг 25→10+10+5, отсутствие tool_calls, невалидные элементы с подсчётом скипов, `sentimentEnabled=false`, окно 60 минут, два tenant, дубликат checkInId, пустой findMany).
- `checkin-sentiment.prompt.spec.ts` — 10 кейсов (валидный, невалидный input ×5, results не массив ×4, невалидный enum, не-строковой checkInId, rationale ×3, пустой results, дубликат checkInId). Итого 18 passed.

**Поведенческие решения** (2026-05-26):
- Silent skip невалидных элементов парсером + инкремент `coo_sentiment_failed_total{reason="invalid_element"}` на каждый пропуск — видимость без падения батча.
- Throw на дубликат `checkInId` в парсере + внешний try/catch cron'а считает весь батч failed (дубликат = модель сглючила, всему батчу веры нет).

**Расширение `BusinessMetricsService`:** `cooSentimentFailedTotal` получил опц. label `reason` (`'invalid_element' | 'other'`), default `'other'` — обратная совместимость со всеми существующими вызовами. Cardinality `2 × ≤101 tenant_top = ≤202 series` (безопасно).

## Скрипт отката миграции LLM на DeepSeek-Pro (2026-05-26)

**Источник:** [`plans/tz/2026-05-26-llm-migration-smoke-checklist.md`](../../plans/tz/2026-05-26-llm-migration-smoke-checklist.md) §5.

`backend/scripts/patch-rollback-to-deepseek-flash.ts` — идемпотентный массовый откат taskType'ов с `deepseek-v4-pro` на `deepseek-v4-flash` при инциденте после миграции (Фаза 4 ТЗ архитектурных изменений).

**Цели (26):**
- `ROLLBACK_TARGETS` (20) — зеркало `patch-mass-migrate-to-deepseek-pro`: merge (9) + cron (5) + formulate (3) + chat (1) + rollup (1) + classifier (1).
- `EXTRA_TARGETS` (6) — chat-v2 + 5 dialog-layer (chat-v2 Фаза 4).
- **Закомментированы:** `clone-respond-v2`, `knowledge-specialists-combined` (под флагами `CLONE_V2_ENABLED` / `SPECIALISTS_COMBINED_ENABLED`, выключаются ENV'ом, а не откатом провайдера).

**Идемпотентность:**
- `editedByAdmin=true` → skip всегда (не перебиваем ручную правку через UI `/admin/llm/routes`).
- Already `deepseek-flash` → ok, без записи.
- Not-found → skip (не создаём новые записи).
- Без `--update-existing` → no-op (защита от случайного запуска).
- Повторный запуск после успешного отката = no-op.

**Флаги:** `--dry-run`, `--update-existing`, `--task <name>` (один taskType для отладки). Работает только с `tenantId=null` (глобальные дефолты); per-tenant откат — отдельный сценарий через `/admin/llm-routes`.

[[../index|← index]]

## Консолидация отчёта + отчёт→граф: удалённые очереди + listener (2026-06-11)

**Источник:** ТЗ [`plans/tz/2026-06-11-meeting-report-consolidation-graph-and-prompts.md`](../../plans/tz/2026-06-11-meeting-report-consolidation-graph-and-prompts.md) (Ф1) + суб-ТЗ [`plans/tz/2026-06-11-report-to-graph-phase2.md`](../../plans/tz/2026-06-11-report-to-graph-phase2.md) (Ф2). Коммиты `2501d72b`, `13a6ac69`. Подробно — [[ai-jobs]] §«Консолидация отчёта встречи + отчёт→граф», [[../02_architecture/knowledge-core]] §«Отчёт встречи → граф».

### Удалены очереди `AiQueueService` (Фаза 1)
- `ai.chapters` (+ воркер `chapters.worker`, метод `enqueueChapters`),
- `ai.tasks` (+ воркер `tasks-extract.worker`, метод `enqueueTasksExtract`),
- `ai.quality-score` (+ воркер `quality-score.worker`, метод `enqueueQualityScore`).

Дублировали главы/задачи/качество — теперь это делает единый `meeting-report-fast.worker`. Качество пишется в каноничную `MeetingQualityScore`. ⚠ `task-extraction.service` оставлен (использует `chatbox-analyze.worker`).

### Новый listener `ReportIngestListener` (Фаза 2, отчёт→граф)
**`ReportIngestListener`** (`@OnEvent('meeting.report-fast-ready')`, в ingest/knowledge-core) — НЕ новая BullMQ-очередь и НЕ cron, а событийный listener. По готовности быстрого отчёта (`status ∈ {ready, partial}`) вызывает `ReportIngestAdapter.ingestReport(meetingId)` → отдельный `RawEvent(sourceType='meeting_report')` → штатная `core.raw-events` → block-ingest. Best-effort (сбой моста не откатывает `reportFastStatus`). Kill-switch `REPORT_INGEST_ENABLED` (Ship-On, default ON). На `'failed'` событие не эмитится.

[[../index|← index]]
