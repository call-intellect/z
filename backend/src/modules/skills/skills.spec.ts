import { describe, it } from 'vitest';

/**
 * TODO (Фаза 0a.4): покрыть тестами `SkillsService` (CRUD + soft-delete + P2002).
 */
describe.skip('SkillsService', () => {
  it('TODO', () => {
    // см. план 2026-05-21-phase-0a-data-model-and-graph-infra.md §7-§10
  });
});
