export type TaskDecisionPair = {
  domain: string;
  idea: string;
  decision: string;
  task: string;
};

export const TASK_VS_DECISION_PAIRS: readonly TaskDecisionPair[] = [
  { domain: 'Закупки', idea: 'А давайте попробуем поставщика упаковки «ТараПро» — говорят, дешевле', decision: 'Переходим на поставщика упаковки «ТараПро» — на 15% дешевле при том же качестве', task: 'Запросить у «ТараПро» договор и прайс на квартал' },
  { domain: 'HR', idea: 'Может, попробуем взять одного сильного senior вместо пары джунов?', decision: 'Нанимаем одного senior-разработчика вместо двух джунов — критична скорость', task: 'Опубликовать вакансию senior-разработчика на hh.ru' },
  { domain: 'Финансы', idea: 'Предлагаю срезать часть бюджета маркетинга в Q3 и переложить в R&D', decision: 'Урезаем бюджет маркетинга в Q3 на 20% в пользу R&D', task: 'Подготовить пересмотренный бюджет маркетинга к понедельнику' },
  { domain: 'IT', idea: 'А что если перевести БД на PostgreSQL — там удобный JSONB?', decision: 'Переносим БД с MySQL на PostgreSQL ради JSONB и партиционирования', task: 'Написать скрипт миграции схемы на PostgreSQL' },
  { domain: 'Продажи', idea: 'Давайте подумаем, не сфокусироваться ли нам на среднем бизнесе', decision: 'Фокусируемся на среднем бизнесе, малый сегмент замораживаем', task: 'Обновить скрипт холодного звонка под средний бизнес' },
  { domain: 'Юристы', idea: 'Предлагаю добавлять пункт о NDA в типовой договор по умолчанию', decision: 'В типовой договор добавляем пункт о NDA по умолчанию', task: 'Подготовить шаблон NDA к пятнице' },
  { domain: 'Производство', idea: 'Может, пока не запускать вторую смену, а догрузить первую?', decision: 'Вторую смену не запускаем, пока первая не выйдет на 80% загрузки', task: 'Составить график первой смены на следующую неделю' },
  { domain: 'Маркетинг', idea: 'А давайте сделаем ставку на контент-маркетинг вместо контекста', decision: 'Основной канал привлечения — контент-маркетинг, контекстную рекламу сворачиваем', task: 'Подготовить контент-план блога на месяц' },
  { domain: 'Продукт', idea: 'Предлагаю мобильное приложение делать после веба, а не параллельно', decision: 'Мобильное приложение делаем после веба, не параллельно', task: 'Собрать требования к мобильному приложению' },
  { domain: 'Логистика', idea: 'Может, отдать доставку на аутсорс СДЭК вместо своего курьера?', decision: 'Доставку отдаём на аутсорс СДЭК вместо своего курьера', task: 'Заключить договор со СДЭК и настроить интеграцию' },
  { domain: 'Поддержка', idea: 'А давайте введём SLA на ответ для приоритетных клиентов', decision: 'Вводим SLA 4 часа на ответ для приоритетных клиентов', task: 'Настроить в хелпдеске правило приоритизации тикетов' },
  { domain: 'Ценообразование', idea: 'Может, поднимем цены с нового года — рынок вроде позволяет?', decision: 'Поднимаем цены на 10% с нового года — рынок позволяет', task: 'Обновить прайс на сайте и уведомить клиентов' },
  { domain: 'Безопасность', idea: 'Предлагаю закрыть доступ к проду только через VPN и 2FA', decision: 'Доступ к проду — только через VPN и 2FA', task: 'Настроить 2FA для всей команды разработки' },
  { domain: 'Партнёрства', idea: 'А давайте пойдём в партнёрство с интегратором X, а не нанимать своих?', decision: 'Идём в партнёрство с интегратором X, а не нанимаем своих внедренцев', task: 'Подготовить презентацию для партнёра X' },
  { domain: 'Офис', idea: 'Может, попробуем гибрид — несколько дней в офисе, остальное удалённо?', decision: 'Переходим на гибрид: 3 дня в офисе, 2 удалённо', task: 'Составить график присутствия команды в офисе' },
  { domain: 'Аналитика', idea: 'Предлагаю сделать ключевой метрикой квартала retention, а не выручку', decision: 'Ключевая метрика квартала — retention, а не выручка', task: 'Настроить дашборд retention в аналитике' },
  { domain: 'Онбординг', idea: 'А что если проводить новичков через недельный буткемп?', decision: 'Новых сотрудников проводим через недельный буткемп', task: 'Подготовить материалы буткемпа для новичков' },
  { domain: 'Качество', idea: 'Давайте попробуем ввести обязательное код-ревью перед мержем', decision: 'Внедряем обязательное код-ревью перед мержем', task: 'Настроить требование ревью в правилах ветки' },
];

export const TASK_VS_DECISION_RULE = [
  '# Различай задачу и решение (жёсткое правило)',
  'Решение фиксирует ВЫБОР между вариантами («берём Б», «уходим к поставщику Y», «решили НЕ делать X») и несёт обоснование.',
  'Задача — конкретное действие к исполнению, назначенное кому-то («подготовь смету», «настрой мониторинг»), в том числе со словами «задача», «поручаю», «ставлю задачу», «назначаю ответственным», «возьми на себя».',
  'Согласие исполнителя взять задачу («подходит», «беру», «да») НЕ делает её решением.',
  'Идея/предложение — ещё НЕ принятый набросок («а давайте», «предлагаю», «может, попробуем»); это НЕ решение (выбор не зафиксирован) и НЕ задача (исполнение не назначено).',
  'Формула: идея = что ПРЕДЛОЖИЛИ (ещё не выбор); решение = что ВЫБРАЛИ; задача = КТО что делает. Предложение + поручение реализовать — это идея И задача, а не одно решение.',
].join('\n');

export const SELF_ASSIGNMENT_RULE = [
  '# Само-назначение исполнителя (правило)',
  'Если говорящий берёт задачу на себя («беру», «сделаю», «займусь», «на мне», «я подготовлю/пришлю») — assigneeHint = имя автора этой реплики.',
  'Автор указан в цитате в конце как «— автор: <ИМЯ>». Бери имя оттуда, ровно как написано, не нормализуй.',
  'Если говорящий пересказывает или цитирует чужие слова, а не обязуется сам — НЕ подставляй его исполнителем.',
  'Если явного само-обязательства нет — не выдумывай исполнителя (assigneeHint — пустая строка).',
].join('\n');

const PROMPT_SAMPLE_SIZE = 8;
const BLOCK_INGEST_SAMPLE_SIZE = 3;

function promptSample(): readonly TaskDecisionPair[] {
  return TASK_VS_DECISION_PAIRS.slice(0, PROMPT_SAMPLE_SIZE);
}

export function renderExamplesForDecisionExtractor(): string {
  return [
    '# Примеры: что решение, а что задача (НЕ путай)',
    ...promptSample().flatMap((p) => [
      `- РЕШЕНИЕ (isDecision=true): «${p.decision}» — зафиксированный выбор.`,
      `- ЗАДАЧА/ПОРУЧЕНИЕ (isDecision=false): «${p.task}» — действие к исполнению, не выбор; это не решение.`,
      `- ИДЕЯ/ПРЕДЛОЖЕНИЕ (isDecision=false): «${p.idea}» — ещё не зафиксированный выбор, это предложение.`,
    ]),
  ].join('\n');
}

export function renderExamplesForIdeaExtractor(): string {
  return [
    '# Задача — это НЕ идея (НЕ путай)',
    'Идея — ещё не принятое ПРЕДЛОЖЕНИЕ нового подхода/возможности. Поручение/постановка задачи (в т.ч. со словом «задача», «поручаю», «сделай», «подготовь», «настрой») — это ЗАДАЧА → isIdea=false, её берёт трекер.',
    ...promptSample().flatMap((p) => [
      `- ИДЕЯ (isIdea=true): «${p.idea}» — ещё не принятое предложение.`,
      `- ЗАДАЧА (isIdea=false): «${p.task}» — действие к исполнению, не идея.`,
      `- РЕШЕНИЕ (isIdea=false): «${p.decision}» — это зафиксированный выбор, не идея.`,
    ]),
  ].join('\n');
}

export function renderExamplesForTaskExtractor(): string {
  return [
    '# Примеры: что задача, а что решение (НЕ путай)',
    ...promptSample().flatMap((p) => [
      `- ЗАДАЧА (isTask=true): «${p.task}» — конкретное действие к исполнению.`,
      `- РЕШЕНИЕ (isTask=false): «${p.decision}» — зафиксированный выбор, не задача; его берёт другой агент.`,
      `- ИДЕЯ (isTask=false): «${p.idea}» — предложение, не задача к исполнению.`,
    ]),
  ].join('\n');
}

export function renderRuleForBlockIngest(): string {
  return [
    TASK_VS_DECISION_RULE,
    'Классификация сигнала:',
    ...TASK_VS_DECISION_PAIRS.slice(0, BLOCK_INGEST_SAMPLE_SIZE).flatMap((p) => [
      `- «${p.decision}» → signalType=decision (зафиксированный выбор).`,
      `- «${p.task}» → signalType=action_item (поручение/действие, НЕ decision).`,
      `- «${p.idea}» → signalType=idea (предложение, ещё не принято; не decision и не action_item).`,
    ]),
  ].join('\n');
}
