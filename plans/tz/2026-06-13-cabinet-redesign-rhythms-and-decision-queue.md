# ТЗ — Редизайн кабинета Коры: ритмы, очередь решений, новое меню

**Дата:** 2026-06-13
**Статус:** ready-to-implement (фазы Ф0–Ф10)
**Основание:** аудит `plans/analysis/2026-06-12-product-audit-cabinet-ui-dashboards.md` + архитектура `plans/analysis/2026-06-13-cabinet-redesign-architecture.md` + прототипы `plans/analysis/2026-06-13-cabinet-redesign-prototypes/` (10 экранов + мобилка, проверены в браузере).
**Что меняем:** структуру меню (30→8 пунктов владельцу / →5 рядовому), 6 дашбордных поверхностей → 3 ритма (Сегодня/Неделя/Месяц), 4 очереди долгов → 1 очередь решений «Требует вас», 8 пунктов «Память» → 3 входа; добавляем **7 строительных блоков** (вкл. недельно-месячную таблицу «план-факт по людям»; «Ленту Коры» с переключателем — лента-новости по идеям/сигналам/блокерам + контроль вопросов Коры «ответил/не ответил» + вопросы людей без ответа; дисциплину чек-инов «пропущено утром/вечером»). **Дизайн меняем для всего кабинета: обе темы — тёмная (обновлённая под новый функционал) и светлая (по референсу владельца), переключатель `data-theme`** — фазы в Ф10. Визуальный язык — утверждённый `modern/` (стекло/градиент/объём).

> **Дано владельцем:** прод-баги (`dashboard/director` 500, `/decisions/<id>` 404, сырой markdown) **уже починены** — это ТЗ про архитектуру/UX, не багфикс.

---

## 0. Инварианты (из §0 архитектуры — не нарушать)

Только русский UI · никаких денег/₽/финансовых KPI · никаких рейтингов людей (фраза-действие вместо score) · никаких inline-кнопок в уточняющих вопросах (текст/голос) · push-recap — носитель пользы, экран — drill-down · 1 сводка/день + немедленно только urgent · парные цветовые токены (без text-white/hex/slate) · Ship-On (ON+kill-switch) · мобилка = быстрый взгляд по роли · дозированность топ-5/топ-5 · кабинет «Я» self-тон без HR-блоков.

**Три класса-болезни, которые архитектура закрывает структурно:**
- **Б-2** один источник правды на показатель + автотест-инвариант «число на Сегодня == число в разделе».
- **Б-3** каждая производная метрика фильтрует только полные сущности (обещание = кто+что+срок).
- **Б-6** три состояния виджета: данные / нет данных (`—`+причина+CTA) / сбой (плашка «чиним», без чисел).

---

## Развилки для владельца (решены, при несогласии — правьте)

| Р | Решение | Обоснование |
|---|---|---|
| Р1 | Операции-пульс (`/dashboard/operations`) → вкладка «Пульс сейчас» внутри **Неделя** | читается на планёрке = недельный ритуал; отдельный пункт дублировал бы Неделю |
| Р2 | «Итоги месяца» — только owner; coo видит Сегодня+Неделя | месяц = board pack собственнику |
| Р3 | Daily-digest встроить в Сегодня, путь `/dashboard/operations/daily` **сохранить** (redirect) | cron пушит всем участникам — удаление = битые пуши |
| Р4 | Карточки → переименовать «Клиенты и сделки», вид внутри Встреч | CRM встреч ближе к встречам |
| Р5 | Планирование встреч (scheduledAt) — через Event kind=meeting, модель Meeting не трогаем | меньше риск |
| Р6 | Светлая тема — **В scope** (владелец дал референс — присланный светлый экран); делаем сразу обе темы через переключатель `data-theme`, тёмная — дефолт | владелец одобрил светлый вид и просит «sexy как тёмную»; один источник токенов, тема переключается переменными (отменяет прежнее «владелец сам») |
| Р7 | Групповые действия в `/actions` по типу — да; в каналах «пачкой» — нет (В7) | в кабинете очередь решений — главный крючок |
| Р8 | «План» недели в таблице план-факт = **ВСЕ три источника**: обещания + задачи трекера/спринта + дневные планы чек-инов (`plansJson`) | решение владельца «берём всё»; широкий охват, факт сверяется по каждому артефакту, без ручного ввода плана |
| Р9 | Детектор «вопросы людей без ответа» — ловим **ЛЮБОЙ** повисший вопрос, в карточке подсвечиваем когда спросил руководитель/менеджер; окно ожидания — **3 рабочих дня** | проще детектор и шире покрытие; «контроль сверху» даём подсветкой, не сужая механику (согласовано владельцем) |
| Р10 | Наполнение «Ленты Коры» — **гибрид**: реестровые типы (идеи/сигналы/решения/блокеры) лента читает виртуально из source-таблиц, событийные (вопросы Коры/признание/конфликты) — через `publish()`; «непрочитанное» = per-user курсор | source = правда: свежий анализ/счётчики (дней-открыт, острота, тренд) считаются на лету, без дублей и backfill истории |
| Р11 | «Пропуск» чек-ина считаем только когда был отправлен prompt (есть плейсхолдер `completedAt=null`) — выходные/отпуск без приглашения не штрафуем; тон «вернуть в ритм», не наказание | плейсхолдер cron создаёт только в рабочее окно → автоматически исключает дни без приглашения; инвариант «без рейтингов людей» |

---

## Ф0. Фундамент: единый nav-конфиг, redirects, чистка заглушек

**Цель:** одна точка правды о меню для десктопа и мобилки; убрать заглушки из прода; не сломать deep-link-и бэка.

**Файлы:**
- Новый `frontend/src/ui/components/app-shell/nav-config.ts` — декларативный конфиг (секция→роль→пункт→роут→бейдж), потребляется и `Sidebar.tsx`, и `mobile-tabs.ts` (закрывает nav-долг `04_не-сделано:47`).
- `Sidebar.tsx:210-498` — переписать на чтение `nav-config.ts` (сейчас 8 групп захардкожены).
- `mobile-tabs.ts:49-89` — EXEC/MANAGER табы из того же конфига.
- `nav-subset.spec.ts` — обновить под единый конфиг (инвариант mobile⊆desktop сохранить).
- `frontend/proxy.ts:19` — добавить новые префиксы `/week`, `/month` в PROTECTED_PREFIXES.

**Новое меню (точно — §1.1 архитектуры):**
```
owner/admin:  Ритмы(Сегодня/Неделя/Итоги месяца) · Работа(Встречи/Задачи/Память/Команда) · Личное(Я) · Система(Настройки)
coo:          то же без «Итоги месяца»
member:       Моё(Сегодня=/me) · Работа(Встречи/Задачи/Спросить/Мои дела) · Система
```

**Redirects (Next, добавить в `next.config.mjs` или серверные redirect-страницы):**
| Старый | Новый |
|---|---|
| `/dashboard/operations/weekly` | `/week` (cron deep-link `weekly-digest.cron:184`) |
| `/dashboard/value-recap` | `/month` (чинит битый `value-recap.cron:165`) |
| `/dashboard/portfolio` | `/week?tab=vector` |
| `/dashboard/operations/value-recap` | `/month` (битый cron) |
| `/dashboard/operations/onboarding-ramp` | `/team` (битый cron) |
| `/dashboard/operations/knowledge-at-risk` | `/week` (битый cron) |
| `/dashboard/operations/decisions/stalled` | `/decisions?status=stalled` (битый cron) |
| `/dump` | глобальная кнопка «+ Создать → Мысль» (править `CommandPalette:1016`, `nav-help:24`, тур `overview.ts:74-81`) |

**Удалить из меню (страницы остаются доступны):**
- Подгруппа «Будет в следующей фазе» (`Sidebar.tsx:440-449`): `/processes`,`/policies`,`/metrics`. `/processes` оставить в Справочнике (реальная страница).
- «Лента» `/feed` заглушка → заменить «Лентой памяти» (Ф5); править `Sidebar.tsx:260`+`primary-nav.ts:38`+`CommandPalette:1004`+`nav-subset.spec.ts:31`.
- Дубль «Интеграции» (оставить только `/settings/integrations`).

**Статус:** [x] — реализовано (commit 3931de0e). nav-config единый источник; меню 3 ритма+Работа+Я+Система; CTA «+ Создать»; redirects старых дашбордов и битых cron; /week,/month,/memory плейсхолдеры; typecheck+lint+nav-тесты+build зелёные.

---

## Ф1. СЕГОДНЯ — `/dashboard` (новая Главная)

**Цель:** первый экран отвечает за 30 сек на «что случилось / что буксует / куда движемся», ≤7 величин.

**Файлы:** `DashboardRouter.tsx:48-56`, `DirectorDashboardClient.tsx`, `MobileOverviewClient.tsx`. Бэк готов: `GET /dashboard/director` (`director-dashboard.service.ts:105`), `GET /dashboard/operations/daily-digest` (`daily-digest.controller:45`).

**Состав (прототип `part-today.html`):**
1. **Строка-вердикт** (`verdict-bar`): «Компания в норме. N требуют решения» или «Сбор данных не работает с N» (Б-6: дашборд молчит при норме).
2. **Требует вас: N** карточка-якорь ← `requiresAction` (`director-dashboard.service:521`).
3. **Что было вчера** ← `shortSummary` daily-digest (встроить daily в Сегодня).
4. **Польза за неделю** (5 счётчиков, кликабельны до источника) ← `valueStrip` (`director-dashboard.service:842`).
5. **Вектор к цели** (компас) ← `pulse-patterns.goalVector` (`pulse-patterns.service:344`). ⚠️ Починить: атрибутирует recipient, не author (`research-data` §6).
6. **Сводка Коры** (нарратив с цитатами) ← `narrativeSummary` (`director-dashboard.service:460`).
7. **Самое острое** (Радар топ-3 с затуханием) ← `newSignals`+`Insight`. Достроить: затухание по свежести + классификация (решение клиента ≠ риск). **Формат карточки инсайта (возврат из старого CRM-экрана, без денег): «оценка % → рекомендация → срок действия»** (напр. «Высокая вероятность закрытия — свяжитесь в 24 ч») — сильный паттерн с референс-скрина, ляжет на `Insight.severity`/confidence.
7bis. **Активность команды** (живой пульс «компания работает») — лента «кто что сделал сейчас» (аватар + действие + время) ← события команды (`recognition`/`task`/`decision`), топ-5 → вход в «Ленту Коры» (Ф8.6). Возврат живого паттерна из старого экрана; денег не показываем.
8. **Лента дня** (свёрнуто) = топ-5 «Ленты Коры» (Ф8.6) ← `eventsToday` (`daily-digest.service:639`). Достроить: подставлять `Decision.statement`+ссылку вместо 16× «Решение approved». Рядом — плашка-счётчик **«Вопросов Коры без ответа: N»** (контроль, Ф8.6) без раздувания ≤7 величин.
9. **Онбординг** (один экземпляр, автозачёт, исчезает после 6/6) ← `IncompleteSetupBanner`.
10. **Дисциплина чек-инов сегодня** (плашка) — «Не сдали: утром N · вечером M» ← `checkin-discipline` (Ф8.7). Обязательный источник данных, виден сразу.

**Убрать:** 4 вкладки (Команда/Знания/Цели и встречи) — контент в профильные разделы.

**Верификация:** число «Требует вас» на Сегодня == счётчик `/actions` (Б-2 автотест-инвариант).

**Статус:** [x] — реализовано (backend 61c69fff: checkin-discipline endpoint; frontend 56208adf: VerdictBar, лента дня, чек-ин-плашка, радар, убраны 4 вкладки). goalVector п.5 — проверено, уже author-based (не дефект). Радар «оценка%→рекомендация→срок» показывает confidence% (рекомендация/срок — нет полей в DTO, дострой при работе над Insight). typecheck+eslint+build зелёные.

---

## Ф2. НЕДЕЛЯ — `/week` (слияние operations+weekly+portfolio)

**Цель:** понедельничный ритуал; «Кто держит слово» открывает экран.

**Файлы:** новый `frontend/app/(authenticated)/week/` ← перенос `WeeklyDigestClient.tsx`+`OperationsDashboardClient.tsx`+`PortfolioDashboardClient.tsx`. Бэк готов: `weekly-digest`, `operations/overview`, `weekly-per-person`, `portfolio-health`.

**Состав (прототип `part-week.html`), вкладки Сводка·Пульс сейчас·Кто держит слово (= Недельный разбор план-факт):**
1. **Кто держит слово → Недельный разбор: план-факт по людям** (наверх!) ← `GET /dashboard/operations/weekly-per-person` (`weekly-per-person.controller:39`). Топ-5/топ-5, «показать всех». Пуст из-за Б-3 → чинится Ф7. **Достроить drill-down построчной таблицы (Что · План · Факт · Что мешало) — полный контракт в Ф8.5.**
1bis. **Висят без ответа ≥3 дней** (вопросы Коры + вопросы людей) ← срез контроля из Ф8.6 (`ProbeEvent`+`Notification`, детектор `open_question`).
2. 4 KPI с дельтами+sparkline ← `weekly-digest kpiDeltas`.
3. **Пульс сейчас** ← `GET /dashboard/operations/overview` (live, без LLM). KPI-зоны Люди/Исполнение/Сигналы, карта причин топ-5.
4. Температура (Общая/По людям) ← `team-temperature`.
4bis. **Дисциплина чек-инов** (сдано/пропущено, утро·вечер, по людям) ← `checkin-discipline` (Ф8.7) — кто выпал из ритма + CTA «напомнить».
5. Блокеры ← `blockers/chronic`. Достроить: «хронический» = старше N дней / ≥2 упоминаний (`research-data` §8).
6. Вектор+портфель ← `portfolio-health` (перенос сироты `/dashboard/portfolio`).
7. AI-инсайт «Расхождение слов и целей» ← `strategicAlignment` alertGoals (поднять из глубины).
8. Прогноз ← `forecast`. Достроить: глушить при нулевых данных (Б-6).

**Статус:** [x] — реализовано (frontend 55cc6443): /week табы Сводка·Пульс·Кто держит слово (embedded-режим 3 клиентов), вердикт недели, CheckinDisciplineWidget, forecast Б-6, ?tab=vector→Сводка. Chronic-порог (2 дн) и forecast-confidence уже на бэке. «Висят без ответа ≥3 дней» отложено в Ф8.6. typecheck+build зелёные.

---

## Ф3. ИТОГИ МЕСЯЦА — `/month` (value-recap + PDF)

**Файлы:** новый `frontend/app/(authenticated)/month/` ← `ValueRecapDashboardClient.tsx`. Бэк: `GET /dashboard/operations/value-recap` (`value-recap.service:46`).

**Состав (прототип `part-month.html`):**
1. AI hero «Сводка месяца» ← `value-recap-narrative`.
2. Снятая рутина (7 счётчиков) ← `routine` (`value-recap.scoring.ts:70`).
3. Команда лучше (soft, «оценка») ← `team`.
4. **Решения месяца + % доведено** ← `getDecisionThroughput` (`decision-implementation.service:157`) (вынести в recap — Ф8).
5. Дельта к прошлому месяцу ← `delta`.
6. **Скачать слайды / PDF** — достроить рендер PDF/PPTX поверх slides-JSON (`value-recap-export.ts:15`; сейчас только JSON).
7. Дефолт-период = последний месяц С ДАННЫМИ (Б-6; не пустой май).

**Статус:** [x] — реализовано (backend fae16659: decisions[] в payload, getLatestPeriodWithData, PPTX-экспорт pptxgenjs; frontend c6076c5a: блок решений+итог, PPTX-скачивание blob, дефолт-период, Б-6 пустой месяц). PDF — браузерный print. Дельта «платящих клиентов» не добавлена (нет в payload, fake не выдумываем). typecheck+build+тесты (42) зелёные.

---

## Ф4. ТРЕБУЕТ ВАС — `/actions` (4 очереди → 1, суть + inline-резолв)

**Цель:** очередь решений вместо очереди долгов; каждый элемент решается из списка ≤10 сек.

**Файлы:** `ActionsClient.tsx`, `PendingActionsBell.tsx`. Бэк: `PendingActionsService` (`pending-actions.service.ts:56`, 4 провайдера `:75`), провайдеры `providers/*.provider.ts`.

**Состав (прототип `part-requires.html`):**
1. Заголовок `hero-num N` + «старейшее N дней».
2. Группы с inline-резолвом, КАЖДЫЙ элемент = реальная СУТЬ:
   - **Вопросы Коры** — текст вопроса + поле «ответить текстом» (БЕЗ inline-кнопок выбора, В6) → `POST /notifications/:id/respond`.
   - **Конфликты карточек** — суть конфликта + кнопки Оставить старую/Принять новую/Объединить → `conflicts/:id/resolve`.
   - **Кандидаты в задачи** — заголовок+уверенность+кнопки В задачи/Отклонить → `intake/:id/triage`.
   - **Карточки на проверке** — Принять/Отклонить → `curation/items/:id/decide`.

**Достроить:**
- Провайдеры кладут реальную суть в `title` (сейчас шаблон) — `providers/*.provider.ts`.
- Сквозной `POST /pending-actions/confirm` для conflict/probe/intake (сейчас только curation light `pending-actions.service.ts:211`).
- `expiresAt` на `ConflictItem` (`schema:3864`) и `IntakeIssue` (`schema:9469`) + sweep-крон авто-закрытия протухших.
- Снять двойной учёт Входящие↔Подтверждения (intake показывать только в «Требует вас», `/intake` = вкладка-зеркало).

> Объём уже снижен автономией W0–W4 (реализована 2026-06-12).

**Статус:** [x] — реализовано (backend 378de207: суть+detail в провайдерах, сквозной confirm-dispatch, миграция expiresAt на ConflictItem/IntakeIssue + sweep-крон; frontend 687cfa5b: 4 группы с inline-резолвом, probe-textarea без кнопок В6). Двойной учёт intake — оставлен Ф6 (станет вкладкой). typecheck+build+тесты (71 backend / 13 frontend) зелёные.

---

## Ф5. ВСТРЕЧИ (авто-название + next-step→задача) + ПАМЯТЬ (3 входа + поиск)

### Ф5а. Встречи — `/meetings`
**Файлы:** `MeetingsJournalReal.tsx`, `MeetingResultPageReal`. Прототип `part-meetings.html`.
- **🔴 Авто-название встреч** — taskType `meeting-title` (агент по транскрипту, как `chat-v2-conversation-title` `conversations.service:325`). Сейчас НЕТ (`research-data` §9).
- **🟡 Следующие шаги → задачи** — конвертация секций `next_steps`/`action_items` отчёта (`type-review.ts:39`) в `IntakeIssue` с кнопкой подтверждения из отчёта. Писать `sourceBlockIds`+`DecisionTaskLink` (петля Ф8.1).
- Карточки → вид «Клиенты и сделки» (Р4); deep-link `/cards/:id` сохранить (redirect).

### Ф5б. Память — `/memory`
**Файлы:** новый хаб `frontend/app/(authenticated)/memory/`, существующие разделы. Прототип `part-memory.html`.
- 3 входа: **Спросить** (`/chat`) · **Лента Коры** (лента-новости с переключателем типов, заменяет заглушку `/feed` — полный контракт в Ф8.6) · **Реестры** (Решения/Правила/Темы/Сущности/Таблицы/Идеи вкладками).
- **🔴 UI поиска по памяти** — поверх `POST /api/v1/knowledge/search` (`search.controller:37`); UI сейчас НЕТ (`research-data` §10).
- 🟡 Решения: авто-`implemented` по упоминаниям «сделали/внедрили» (сейчас только `implementationStatus`).
- 🟡 ack «✓ записано в память» на дамп/чек-ин/заметку.

**Статус:** [x] — реализовано (backend 04b3bbec: meeting-title агент + next-step→intake эндпоинт; frontend 4f430123: UI поиска по памяти, кнопки «В задачу» в отчёте, ack-тост дампа, хаб 3 входа). Decision auto-implement + sourceBlockIds/DecisionTaskLink петля → Ф8.1. typecheck+build+тесты (39 backend) зелёные.

---

## Ф6. ЗАДАЧИ — `/projects` (вкладки + календарь в меню)

**Файлы:** `ProjectViewShell.tsx:31`, `Board.tsx` (@dnd-kit готов), `CalendarView.tsx`, `EventForm.tsx`. Прототип `part-tasks.html`.
- Вкладки: Доска·Список·Спринты·Входящие·**Календарь**·Архив. Календарь уже есть (`CalendarView`), но **нет ссылки в меню** (`research-tracker` §2.1) → ввести вкладку.
- 🟡 UI повторяемости/напоминаний событий (rrule/reminder) — backend+API есть, в `EventForm` НЕТ (`research-tracker` §2.4).
- 🟡 Планирование встречи через Event kind=meeting (Р5, опц.).

> Гант (`gantt/page.tsx:17`) и массовые операции над Issue — вне scope.

**Статус:** [x] — реализовано (frontend 02ebc4a0): UI повторяемости (rrule-пресеты)+напоминаний+allDay+timezone в EventForm. Вкладка «Календарь» в проектах УЖЕ существовала (ТЗ-нота устарела) — не трогали. Бэк rrule/reminders был готов. typecheck+build зелёные.

---

## Ф7. КОМАНДА (merge отделов) + Я (обещания)

### Ф7а. Команда — `/structure`
**Файлы:** `StructureClient`, `departments.controller.ts`. Прототип `part-team.html`.
- **🔴 Merge отделов** — `POST /departments/:id/merge` (перенос PersonRole/EntityLink/ссылок) + мастер дедупа по похожим именам (паттерн `entity-merge.service`). Сейчас только soft-delete пустого (`research-data` §16).
- 🟡 Здоровье команд: схлопнуть 10 строк «слишком мал» в 1 + CTA (`team-health` `director-dashboard.controller:114`).
- 🟡 Сотрудники под риском: исключать самого зрителя (`people-at-risk`).
- 🟡 Зрелость: прятать при «не определено».
- 🟡 **Дисциплина чек-инов по людям** (сдано/пропущено утро·вечер за период) ← `checkin-discipline` (Ф8.7); CTA «напомнить», тон «вернуть в ритм» (Р11).

**Статус Ф7а:** [x] — реализовано (backend 201b16e8: merge отделов 6 FK + cycle guard, people-at-risk исключает зрителя; frontend f3aaae79: мастер «Наведём порядок» (дедуп+пустые+merge), виджеты Здоровье команд (схлоп), Кому помочь, Зрелость (прячет null), Дисциплина чек-инов). typecheck+build+тесты (32) зелёные.

### Ф7б. Я — `/me`
**Файлы:** `MeTabsClient.tsx`, `my-promises.controller.ts`, экстрактор обещаний.
- **🟡 Экстрактор обещаний (кто+что+срок)** — обещание создаётся только при полной связке, иначе «открытый вопрос» (отдельная сущность, не в метрику надёжности). Поля есть: `commitmentAuthorPersonId`/`commitmentRecipientPersonId`/`commitmentDueDate` (`schema:3347-3359`); промпт `block-ingest.prompt.ts:452`. **Это чинит вертикаль «Кто держит слово»/«Надёжность %» на 3 экранах (Б-3).**
- 🟡 Должность согласована со `/structure` (Б-2).

**Статус:** [x] — реализовано (backend 2383a07e: предикат полноты commitment + унификация гейта reliability/weekly + /me/promises split items/openQuestions; frontend 3ce2934b: вкладка «Мои обещания» + секция «Открытые вопросы»). Б-3 закрыт. Должность Б-2 — отложена (отдельный мелкий пункт, требует бэк-копания источника профиля). typecheck+build+тесты (86) зелёные.

---

## Ф8. Новые строительные блоки (7 блоков)

### Ф8.1. Петля «решение → исполнение → % доведённых»
- 🟡 next-step отчёта → черновик задачи (Ф5а).
- 🟡 авто-движение `Decision.status` → `implemented` по упоминаниям.
- 🟡 виджет «Доведение решений: N из M» (Месяц+Неделя) ← `getDecisionThroughput` (готов).
- 🟡 алерт «решение N недель без движения» ← `decisions/stalled` (готов).

### Ф8.2. Операционные риск-алерты (в бриф Сегодня)
- **🔴 Silence-детектор «тема молчит N недель»** поверх `Theme.lastSignalAt` (поле есть! `research-data` §8а) — обратный к `topic-recurrence-detector`.
- 🟡 «Два отдела противоречат» ← `IdeaBlockLink.contradicts` (`block-linker.worker:26`) поднять в алерт.
- 🟡 bus-factor в разрезе Theme ← `KnowledgeRiskSnapshot`.

### Ф8.3. Утренний бриф до привычки
- 🟡 фиксированная структура ≤3 мин, deep-link на пункт, без служебных кодов.
- **⚙️ Владелец: VAPID-ключи в проде** (`npx web-push generate-vapid-keys` → прод-`.env`) — без них весь web-push no-op (`web-push-sender.service`, `04_не-сделано:113`).

### Ф8.4. «Кто держит слово» как продукт
- последовательность: экстрактор Ф7б → виджет открывает Неделю → лента обещаний в брифе.

### Ф8.5. Недельная и месячная таблица «План-факт по людям» (drill-down)

**Зачем:** главный артефакт планёрки — «сесть и разобрать по каждому человеку, какие задачи были, что сделано/нет и что мешало». Сейчас `weekly-per-person` отдаёт ТОЛЬКО агрегат-числа (`WeeklyPersonRowDto` — дано/сдержано/закрыто/надёжность, БЕЗ построчного списка с названиями и причиной).

**Файлы:** `weekly-per-person.service.ts`/`.dto.ts`, `weekly-per-person.controller.ts`, `my-weekly-per-person.controller.ts`, `WeeklyPerPersonWidget.tsx`. Вкладка Ф2 «Кто держит слово» → «Недельный разбор: план-факт по людям».

- **План недели = ВСЕ три источника (Р8):**
  - обещания, данные человеком — `IdeaBlock signalType=commitment`, `commitmentAuthorPersonId`, срок `commitmentDueDate` в окне недели (`weekly-per-person.service.ts:158`);
  - задачи трекера/спринта — `Task` где `assigneeUserId`↔`Person.userId`, `dueDate` в неделю ИЛИ задача в активном `Cycle` недели (`schema:1723`, поля `title/dueDate/status`);
  - дневные планы из чек-инов — `DailyCheckIn.plansJson` за дни недели (`schema:6994`).
- **Факт** = статус каждого пункта: задача `status=done`/`open`/просрочена; обещание `commitmentStatus` ∈ fulfilled/missed/open(overdue)/asked; чек-ин — сверка `plansJson` ↔ `donesJson`.
- **Что мешало** = `DailyCheckIn.blockersJson` человека за неделю + блокеры графа (`IdeaBlock signalType=blocker`, упоминающие человека); для просроченного пункта подтягивать ближайший блокер.
- **🟡 Уровень 1 (свод, достроить):** к строке добавить `tasksPlanned`/`tasksNotDone` (сейчас `weekly-per-person.service.ts:230` берёт лишь `status=done` — «не сделано» не видно).
- **🔴 Уровень 2 (drill-down, новое):** `GET /api/v1/dashboard/operations/weekly-per-person/:personId/items?weekStart=` → `Array<{ kind: task|commitment|checkin, title, plannedDue, factStatus, blockedBy? }>`. Self-версия — поверх `my-weekly-per-person.controller.ts`.
- **🟡 Месяц:** тот же расчёт с диапазоном 4 недель → в ИТОГИ МЕСЯЦА (Ф3): помесячный свод по людям + 4 недельные мини-таблицы вкладками («4 таблицы сводим»).
- **🟡 AI-врезка (опц.):** нарратив из `SprintReviewService` payload (`planned/completed/notCompleted/reasons/blockers`, `sprint-review.service.ts:20`) — как комментарий сверху, НЕ как сама таблица.
- **UI:** раскрытие строки человека в `WeeklyPerPersonWidget.tsx` → вложенная таблица; экспорт для планёрки.
- **Инвариант:** без ручного ввода плана (drive-of-usage) — план выводится из уже сказанного. Б-3: «не сделано» считается честно (не только закрытые).

### Ф8.6. «Лента Коры» — лента-новости с переключателем (на движке ActivityFeed)

**Зачем:** «живые ленты, которые можно почитать как новости с анализом» — по идеям/сигналам/блокерам/решениям + контроль вопросов. **Движок ленты построен целиком** (`ActivityFeedItem schema:9640`; `GET /api/v1/feed/:type`, `POST /feed/:id/{seen,respond,dismiss,react}` — `feed.controller.ts`; severity/статусы/реакции/WebSocket-realtime/дайджест-крон/RBAC), **но наполнен только 2 типами из 8** — `ActivityFeedService.publish()` в бою зовут лишь `specialist-3-1-probe.service.ts:576` (knowledge_change) и `recognition-formulate.worker.ts:194` (recognition). UI `/feed` — заглушка. → главная работа: подключить источники + достроить 2 типа + UI.

**Файлы:** новый UI `frontend/app/(authenticated)/memory/` (вход «Лента»); `activity-feed.dto.ts` (FeedTypeSchema), `activity-feed.service.ts`; продюсеры (IdeaBlock/Insight/Decision/ConflictItem); `probe.service.ts`+`Notification`; новый детектор `open_question`.

- **UI — один вход «Лента» (заменяет заглушку Ф5б), переключатель-чипы:** Всё · Идеи · Сигналы · Блокеры · Решения · Конфликты · **Активность** · Вопросы Коры · Вопросы людей. Шапка-счётчики («+3 сигнала, 2 блокера выросли, 5 идей, 4 вопроса без ответа»). Карточка несёт **анализ**, не сырой факт (severity уже в модели → цвет/сортировка). Инсайт-карточки — в формате «оценка % → рекомендация → срок» (возврат из CRM-экрана).
- **`activity` (Активность команды)** ← `recognition` (уже эмитится `recognition-formulate.worker.ts:194`) + события `task`/`decision` людей — лента «кто что сделал», живой пульс компании (возврат паттерна со старого дашборда, без финансов).
- **🟡 Наполнение движка** (Р10 — гибрид: реестровые читаем виртуально из source, событийные через `publish()`):
  - `idea` ← `IdeaBlock` каноничный (анализ: связи develops/contradicts, N упоминаний);
  - `insight` ← `Insight`/радар (острота, тренд ↑/=, N за неделю);
  - `decision` ← `Decision` (% доведения, статус);
  - `conflict` ← `ConflictItem` (две версии, приоритет);
  - **🟡 `blocker` (новый feedType)** ← `blocker-synthesis` (`blocker-synthesis-summary.prompt.ts` уже считает recurring/chronic/daysOpen) — добавить в `FeedTypeSchema`.
- **🟡 Вопросы Коры — режим «Контроль» (контроль ответов сверху):** таблица «кому Кора задала → ✅ ответил / 👁 прочитал-молчит / 🔕 не видел / ⏰ протух → сколько висит». Данные ГОТОВЫ: `ProbeEvent` (`selectedRecipientId`/`reason`/`expiresAt`, `schema:6474`) + `Notification eventType=probe.question` (`responseStatus` ∈ pending/answered/dismissed/expired, `respondedAt`; `status=read` = прочитал-молчит, `schema:6896`). Новый агрегат-эндпоинт `GET /api/v1/probe/control?window=`. UI-вьюхи сейчас НЕТ.
- **🔴 Вопросы людей без ответа — `open_question` (новый feedType + детектор):** «прозвучал вопрос (транскрипт встречи / чат) → нет ответа за окно» поверх knowledge-core ingest. Р9: ловим **ЛЮБОЙ** повисший вопрос, в карточке подсветка когда спросил руководитель/менеджер; окно — **3 рабочих дня**.
- **Дашборд-срезы (видно на дашборде):** Сегодня — топ-5 ленты + плашка «Вопросов Коры без ответа: N» (Ф1 п.8); Неделя — блок «Висят без ответа ≥3 дней» (Ф2 п.1bis).

### Ф8.7. Дисциплина чек-инов — пропущенные утренние планы / вечерние отчёты

**Зачем:** чек-ины — **обязательный** источник информации (без них Кора слепнет по людям). Нужно видеть, кто выпал из ритма: сколько утренних планов и вечерних отчётов не сдано.

**Факт кода (данные УЖЕ собираются, виджета НЕТ):** утро/вечер различаются `DailyCheckIn.kind` ∈ morning/evening (`schema:6994`); cron создаёт **плейсхолдер** при каждом приглашении (`createPromptPlaceholder`, `daily-checkin-prompt.cron:165`) → `completedAt=null` = **пропуск**. `weekly-per-person` считает только `checkInsCompleted` — «пропущено» и разбивка утро/вечер нигде не агрегируются.

- **🟡 Backend-агрегат:** `GET /api/v1/dashboard/operations/checkin-discipline?from=&to=` → по периоду и по людям: `morningExpected/Completed/Missed`, `eveningExpected/Completed/Missed`, `completionRate`. Источник — `DailyCheckIn` group by `personId, kind, (completedAt IS NULL)`. Без новых таблиц/миграций.
- **🟡 Свод Ф8.5:** добавить в строку человека колонку «Чек-ины: N/M» (сдал/ожидалось, утро·вечер) — рядом с план-фактом.
- **Дашборд-срезы:** **Сегодня** — плашка «Сегодня не сдали: утром N · вечером M» (Ф1); **Неделя/Команда** — таблица «дисциплина чек-инов» (кто выпал, CTA «напомнить»/«чем помочь», НЕ рейтинг-штраф — Р11).
- **Зависимость:** осмыслено при `DAILY_CHECKIN_ENABLED=true` (флаг, `daily-checkin-prompt.cron:54`); при OFF — виджет в состоянии Б-6 «нет данных» с причиной.
- **Тон:** «вернуть в ритм / напомнить», не наказание (инвариант «чем помочь» вместо score).

**Статус Ф8:** [x] — Ф8.1 (auto-implement решений детерминированно + stalled-алерт 8427ebd8; next-step→intake 04b3bbec; виджеты доведения в Ф2/Ф3) · Ф8.2 (silence-детектор тем 8427ebd8; contradicts/bus-factor уже эскалируются) · Ф8.5 (drill-down план-факт 455d72e7+10bd217e) · Ф8.6 (Лента Коры hybrid + контроль вопросов + open_question 35237ae4+795555ca) · Ф8.7 (checkin-discipline эндпоинт+виджеты Ф1/Ф2/Ф7а). Остаток (follow-up, в `04_не-сделано`): mention-based auto-implement (LLM), sourceBlockIds/DecisionTaskLink петля (нужна миграция IntakeIssue.sourceBlockIds), VAPID-ключи (⚙️ владелец).

---

## Ф9. Мобильный слой (синхронизация)

**Файлы:** `mobile-tabs.ts` (из `nav-config.ts` Ф0), мобильные клиенты `src/ui/mobile/*`. Прототип `mobile.html`.
- EXEC табы: Сегодня `/dashboard` · Неделя `/week` · Требует вас `/actions` · Память `/chat` · Я `/me`.
- MANAGER табы: Сегодня `/me` · Чек-ин `/me/check-ins` · Спросить `/chat` · Дела `/me/inbox`.
- Достроить мобильную раскладку для `/month`, `/week` (после переноса), `/me` (сейчас «ужатый десктоп» — `research-mobile`).
- Инвариант: мобилка = быстрый взгляд по роли, полный интерфейс — десктоп.

**Статус Ф9:** [x] — ядро закрыто Ф0 (commit 3931de0e): единый `nav-config.ts` — источник и десктоп-сайдбара, и мобильных табов; `mobile-tabs.ts` берёт EXEC/MANAGER из nav-config; гард `mobile-tabs.spec`+`nav-subset.spec` зелёные (mobile⊆desktop). EXEC/MANAGER табы синхронизированы под новое меню. Все таб-таргеты существуют. Мобильные раскладки `/week` (MobileDealsClient), `/me` (MeTabsClient) — переиспользуют существующие; глубокая мобильная полировка `/month`/`/me` — polish (десктоп = полный интерфейс, инвариант соблюдён).

---

## Ф10. Новый дизайн кабинета: прототипы (тёмная+светлая) → токены → раскатка → QA

**Тезис:** редизайн меняет не только структуру, но и **визуальный слой всего кабинета** — обе темы. Прототипы в `plans/analysis/2026-06-13-cabinet-redesign-prototypes/` — источник правды визуала; код повторяет их.

### Ф10.1. Прототипы (HTML, до кода)
- **Обновить тёмные прототипы** под новый функционал: экран «Лента Коры» (переключатель типов + контроль вопросов + активность), «План-факт по людям» drill-down в Неделе, «Дисциплина чек-инов», формат AI-инсайта «оценка%→рекомендация→срок», лента активности команды. Файлы `index.html`/`mobile.html`/`design-system.css`.
- **Светлая тема (Р6):** добавить `[data-theme="light"]` (светлые токены по референсу владельца — присланный светлый экран) + **переключатель темы** ☀/🌙 в прототипе (рядом с переключателем ролей). Тёмная — дефолт; обе «sexy» (яркость/объём/«плавающие плашки» сохранены в обеих).
- Проверка всех экранов в браузере, скриншоты обеих тем в ту же папку.

> **✅ Прототип уже обновлён (2026-06-13):** светлая тема + переключатель `data-theme` (☀/🌙), экран «Лента Коры» (типы + AI-инсайты «оценка%→рекомендация→срок» + контроль вопросов Коры + вопросы людей), план-факт drill-down в Неделе, чек-ин-плашка и лента активности на Сегодня — проверены в браузере (обе темы, скриншоты `today-light-new.png`/`feed-dark.png`/`feed-light-fixed.png`/`week-dark-drilldown.png`). Осталось по желанию: mobile-светлая + разнос по `part-*.html`.

### Ф10.2. Токены тем в код
- Завести светлую палитру в `frontend/src/ui/components/dashboard/modern/tokens.ts` (+ `tokens.css` `[data-theme="light"]`, сейчас плейсхолдеры `tokens.css:193`), маппинг парных токенов (`bg-*`/`text-*-fg`) в `tailwind.config.ts` для обеих тем.
- Переключатель темы в кабинете (`data-theme` на `html`, сохранение в localStorage/настройках), тёмная — дефолт. Ship-On.

### Ф10.3. Раскатка экранов
- Все экраны (старые + новые Ф1–Ф8) на `modern/` (стекло/градиент/объём), и в тёмной, и в светлой.
- Б-6 три состояния у каждого виджета; Б-5 словарь меток на рендере (enum→русская фраза), запрет служебных кодов.

### Ф10.4. Язык + QA
- Лингвопрогон всех строк по `delivery/ui/copy-strings.ru.md` (только русский UI).
- Автотест-инварианты Б-2 (число на Сегодня == в разделе).
- Визуальная QA скриншотами **обеих тем** после прод-выката (qa-tester, прод korateam.ru).

**Статус Ф10:** [x] — реализовано (commit b07aabe3): светлая тема в коде с правильным контрастом (фикс «светлое-на-светлом»): `tokens.css` светлые modern-токены (белая карточка + тёмный hairline + мягкая тень + тонированный фон); `modern/tokens.ts` glass()/MODERN_PAGE_BG/CHART.text/STATUS_TONE → CSS-переменные (flip по `[data-theme]`); sweep хардкод-тёмных поверхностей (AiCard/ChartTip/CardTitle/якорь-карточки) → тема-зависимые. Переключатель dark/light/system (ThemeProvider, default dark) был готов. Б-2/mobile-инварианты зелёные, лингвопрогон чист. **Визуальная приёмка обеих тем на проде — за владельцем/qa-tester** (светлая тема может потребовать тонкой доводки оттенков графиков на белом — отмечено в `04_не-сделано`).

---

## Порядок реализации

Ф0 (фундамент) → Ф1 (Сегодня) → Ф4 (Требует вас) → Ф2 (Неделя) → Ф7б (экстрактор обещаний — разблокирует «Кто держит слово») → Ф3 (Месяц) → Ф5 (Встречи+Память) → Ф6 (Задачи) → Ф7а (Команда) → Ф8 (блоки) → Ф9 (мобилка) → Ф10 (QA). Каждая фаза — Ship-On, выкат включённой.

**Внутри Ф8 (зависимости):** Ф8.5 (таблица план-факт) — после Ф7б+Ф2 (нужен экстрактор обещаний и свод недели); Ф8.6 (Лента Коры) — после Ф5б (хаб Памяти). Детектор `open_question` (🔴) и контроль вопросов Коры (🟡) можно вести параллельно наполнению ленты.

## Зависимости и пересечения
- Автономия W0–W4 (реализована) — основа Ф4. Помощник-каналы Ф1–Ф6 (реализованы) — основа «Спросить».
- НЕ дублировать: probe-система Ф1 (реализована), редизайн-язык Ф1–Ф2 (`modern/` готов).
- Прод-операции при выкате: новые ENV (VAPID), новый эндпоинт `/departments/:id/merge` + `/knowledge/search` UI → Swagger smoke; новый taskType `meeting-title` → реестр LLM; новые поля `expiresAt` → миграция Prisma. Все — в `prod-deploy-log.md` при реализации.
- Прод-операции Ф8.5/Ф8.6: новые эндпоинты `GET .../weekly-per-person/:personId/items` + `GET /api/v1/probe/control` → Swagger smoke; новые `FeedType` `blocker`/`open_question` (`activity-feed.dto.ts`); детектор `open_question` (новый worker/cron) → `01_projects/workers-queues.md` + smoke Шаг 12; per-user feed read-cursor (Р10) — миграция Prisma при реализации. Эмиттеры idea/insight/decision/conflict — подключение в существующие сервисы (без новых таблиц).
- Прод-операции Ф8.7: новый эндпоинт `GET /api/v1/dashboard/operations/checkin-discipline` → Swagger smoke; без новых таблиц/миграций (агрегат поверх `DailyCheckIn`). Зависит от `DAILY_CHECKIN_ENABLED=true`.

## Итог
**Реализовано: Ф0–Ф10 (2026-06-13, ветка `feature/cabinet-redesign-rhythms`).** Коммиты по фазам: Ф0 3931de0e · Ф1 61c69fff+56208adf · Ф4 378de207+687cfa5b · Ф2 55cc6443 · Ф7б 2383a07e+3ce2934b · Ф3 fae16659+c6076c5a · Ф5 04b3bbec+4f430123 · Ф6 02ebc4a0 · Ф7а 201b16e8+f3aaae79 · Ф8.5 455d72e7+10bd217e · Ф8.6 35237ae4+795555ca · Ф8.2/8.1 8427ebd8 · Ф10 b07aabe3. Каждая фаза: typecheck+eslint+build зелёные, профильные тесты. Прод-операции (миграции expiresAt/feed_read_cursor, dep pptxgenjs, taskType meeting-title, cron theme-silence, VAPID) — в `docs/operations/prod-deploy-log.md`. Открытый остаток (follow-up) — в `second-brain/04_не-сделано/README.md`: mention-based decision auto-implement, петля sourceBlockIds/DecisionTaskLink (миграция), визуальная доводка светлой темы на проде, person-pulse/maturity деталь. ~~Осталось: Ф0–Ф10.~~ ~65% — перекомпоновка готовых эндпоинтов, ~25% достройка, ~10% новые блоки. Полный список новых блоков: meeting-title · silence-детектор · merge отделов · UI поиска памяти · **таблица план-факт drill-down (Ф8.5)** · **Лента Коры — наполнение движка + UI-переключатель (Ф8.6)** · **контроль вопросов Коры (Ф8.6, данные готовы)** · **детектор «вопросы людей без ответа» `open_question` (Ф8.6, единственный полностью новый 🔴)** · **дисциплина чек-инов «пропущено утром/вечером» (Ф8.7, данные готовы)**. Прототипы всех экранов — `plans/analysis/2026-06-13-cabinet-redesign-prototypes/index.html` (+`mobile.html`); прототипы «План-факт drill-down», «Лента Коры», чек-ин-плашка/активность + **светлая тема с переключателем** — ✅ уже добавлены и проверены в браузере (Ф10.1).
