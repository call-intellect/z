# Локальная конфигурация LiveKit Egress.

api_key: devkey
api_secret: secretsecretsecretsecretsecretsecretsecretsecret
ws_url: ws://127.0.0.1:7880

redis:
  address: 127.0.0.1:6379

s3:
  access_key: z_minio_dev
  secret: z_minio_dev_password
  region: us-east-1
  bucket: meetings-dev
  endpoint: http://127.0.0.1:9000
  force_path_style: true

logging:
  level: info
