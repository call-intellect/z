# Боевой тест модуля создания клона сотрудника — прод (korateam.ru)

> Дата: 2026-06-22 · Тестировщик: Claude (qa-tester) · Окружение: **прод**, Org «ооо ромашка» (`cmpuz4gbs000201mvfbf3k2zk`)
> Метод: через интерфейс кабинета (Playwright) + сверка с бэком через read-only `diag`/admin-API. Решения владельца: гибрид (owner-носитель), дефолтные пороги, diag в прод разрешён, ручной запуск штатных cron'ов разрешён.

## Цель
Проверить сквозную цепочку создания клона должности: дамп мысли → block-ingest (разметка signalType + subject) → диспетчер 3-7-skill → SkillProfile → rebuild (черты) → концепты → verify → ExecutablePersona → ответ клона + анти-фейк. Посмотреть, что реально попадает в БД на каждом звене.

## Окружение и носитель
- Должность: «[QA] Руководитель маркетинга» (`roleId cmq0y5v0300j301qxvnv3gr1p`), носитель — **Алексей** (owner, employee, `personId cmpzl0mee00065gnq9vm3ajv1`).
- Канал входа: «Создать → Мысль» → `/dump` → `POST /api/v1/ingest/dump` `{text, nonce}` (sourceType web_form, subject-атрибуция на автора). Подтверждено вручную (201) + пакетно тем же эндпоинтом (нужен заголовок `x-org-id`, иначе 403 `tenant_required`).

## Ход и находки

### Звено 1 — ingest → граф: РАБОТАЕТ ✅
- Залито 18 заметок «руководителя маркетинга» (решения с обоснованием).
- Метрики графа (`GET /api/v1/org-admin/knowledge/metrics`): rawEvents 44, blocks 81 (canonical 80), entities 102, links block 341 / entity 52, LLM за 24ч 580 вызовов.
- Реестр «Сущности»: появились CAC (8 упоминаний — дедуп объединил из 8 заметок), LTV, LTV первой покупки, лендинг, ICP, когортный анализ, атрибуция продаж, промокоды, небрендовые запросы, Алексей (сотрудник). **Дедуп сущностей и резолв работают.**

### Звено 2 — диспетчер 3-7-skill → SkillProfile: РАБОТАЕТ ✅
- `GET /clones/persons/:id/skill-profile`: профиль создан (`profileId b62454f1…`, status active). Значит первый reasoning-subject блок дошёл до диспетчера и создал профиль 1:1.

### 🔴 Находка №1 — формулировки-«решения» не кормят клон (классификация signalType)
- `buildVersion: 0`, `traits: []` спустя ~55 мин — rebuild упёрся в гейт `blocks < skillProfileMinObservations (5)`.
- Причина: реестр «Решения» показал **15 из 18 моих заметок как Decision** (signalType `decision`). А навыковый профиль кормят только `reasoning/rationale/decision_basis` (`loadSubjectReasoningBlocks`). `decision` ≠ `decision_basis` → reasoning-блоков набралось < 5.
- **Смысл:** заметки в стиле «сегодня решил X, потому что Y» уходят в реестр решений, а не в клон должности. Клон кормится формулировками про **подход/принцип** («я в принципе так работаю / мой принцип / я обычно рассуждаю так»).
- Реакция: долито 11 заметок в reasoning-стиле (про подход). Ожидаем reasoning-блоки → rebuild → черты. (в процессе)

### 🔴🔴 Находка №2 (ГЛАВНАЯ) — клон должности не наполняется: рассогласование таксономии signalType
**Симптом:** после долива 11 reasoning-заметок профиль клона так и остался `buildVersion 0`, `traits []` (поллер 8×40с).

**Диагностика (через UI «Сущности» → сущность «Алексей» → блоки знаний):** subject-атрибуция РАБОТАЕТ — к Алексею привязано 20 блоков знаний. Но их signalType:
- `methodology_step` («Шаг методологии») ×13 — «Data-driven подход», «Выбор канала по юнит-экономике», «Не масштабировать непроверенное», «Найм по ключевому навыку» и т.д.
- `motivation` ×2, `decision` ×2, `fact` ×1, `regulation` ×1, `metric_change` ×1, `result` ×1.
- **`reasoning`/`rationale`/`decision_basis` — 0 блоков.**

**Корень (по коду):**
- `specialist-3-7-skill.service.ts:530` — `loadSubjectReasoningBlocks` фильтрует строго `signalType: { in: ['reasoning','rationale','decision_basis'] }`.
- `specialist-3-7-skill.service.ts:318` — если таких блоков `< skillProfileMinObservations (5)` → **полный `return` (skip)**, включая проходы слоя метода (value/motivation/process_marker на :362/:382 работают на тех же reasoning-группах).
- block-ingest классифицирует «подход к работе» как `methodology_step`/`motivation`/`decision` (что осмысленно!), но эти типы НЕ входят в гейт-набор клона.

**Смысл находки:** `methodology_step` — это буквально «как сотрудник делает работу» = ядро клона должности. Но из-за узкого гейта (`reasoning/rationale/decision_basis`) эти блоки до клона НЕ доходят. Канал «Мысль/Память» (`/dump`) практически не генерирует `reasoning`-блоки → **клон должности через этот канал не наполняется вообще.** Это объясняет, почему в проде у активной Org 0 клонов.

**Варианты доработки (на решение владельца):**
- A (рекомендую) — расширить набор в `loadSubjectReasoningBlocks:530` на `methodology_step` (+ возможно `motivation`/`competence`/`expertise`); проверить, что role-principle-synthesis берёт тот же расширенный набор. Прямо устраняет рассогласование.
- B — доработать промпт block-ingest, чтобы рассуждения про подход метились `reasoning`/`rationale`.
- C — снизить/убрать гейт `<5` (частично; не решает, если reasoning-блоков 0).

### ✅ Решение находки №2 — оформлено в ТЗ
Полный доказательный разбор (Вариант A vs B vs C, 4 независимых прохода по коду, матрица, точки правки, backfill, риски, семантика и история узкого набора) → **ТЗ `plans/tz/2026-06-22-clone-signaltype-methodology-step.md`** (+ orchestrator-prompt). Решение: расширить набор-потребитель на `methodology_step` (единая константа + 3 рубежа + backfill, без правки LLM-классификатора и без миграций). Реализация — по явному «погнали» через tz-orchestrator + выкат владельцем; прод-acceptance замкнётся на этой же Org.

## Дальше по плану (после выката ТЗ — продолжить прод-приёмку на этой Org)
- [ ] Звено 3 — rebuild построил черты (pending_verification).
- [ ] Звено 4 — концепты (SkillTraitConcept) через normalizer cron.
- [ ] Звено 5 — force `SkillTraitVerifyCron.tick` → черты active.
- [ ] Звено 6 — ExecutablePersona on-demand при первом вопросе клону.
- [ ] Звено 7 — ответ клона (от первого лица, дисклеймер) + анти-фейк (отказ на вне-темном вопросе).
- [ ] Звено 8 — слой метода (RolePrinciple через `RolePrincipleSynthesisCron.tick`).

---

## ГЕЙТ 0 (прогон 2026-06-23, qa-tester) — ❌ ФИКС НЕ ВЫКАЧЕН В ПРОД

> Прогон: 2026-06-23, прод `korateam.ru`, Org «ооо ромашка». Доступ к diag в прод подтверждён владельцем в сессии. Носитель: персона owner-аккаунта = **Алексей** `cmpzl0mee00065gnq9vm3ajv1` (подтверждено `/api/v1/auth/me`: userId `cmpuz4ga3000101mvi1820wbj`, имя «Алексей»).

**Вердикт (промежуточный, СКОРРЕКТИРОВАН — см. «UPDATE» ниже): клон на проде НЕ строится. Первичная гипотеза «код не выкачен» ОПРОВЕРГНУТА владельцем: файл `skill-signal-types.ts` с `methodology_step` присутствует в работающем контейнере (`docker compose exec backend cat src/...`), монтирования `src` томом в compose нет → образ собран уже с фиксом. Остаётся одно из двух: (а) пропущен обязательный Шаг 8 выката (one-off backfill пересборки накопленных профилей), либо (б) образ собран, но backend-контейнер не перезапущен с новой `dist`-сборки. Разводящий тест — прогон backfill на проде (ждём вывод).**

> ⚠️ **Замечание по методологии (для себя на будущее):** «фикс не выкачен» — преждевременный вывод. `git merge-base ... main` показал, что фикса нет на `main`, но прод собирается из `dev` (`prod-deploy-log.md:20` `git pull origin dev`), а `dev` фикс содержит. Поведенческий тест (rebuild не сработал) согласуется и со «старой сборкой», и с «забыли backfill» — их нужно разводить, а не схлопывать в один вывод.

### UPDATE (2026-06-23, после прогона backfill владельцем) — ФИКС ВЫКАЧЕН И РАБОТАЕТ ✅

- Владелец показал `docker compose exec backend cat src/.../skill-signal-types.ts` → файл с `methodology_step` ЕСТЬ в контейнере; монтирования `src` томом в `docker-compose.yml` нет → образ собран с фиксом.
- Прогнали `backfill-skill-profiles-rebuild.ts --tenant=...` на проде (`active-профилей 1, enqueued=1`). После этого профиль Алексея: **`buildVersion 0 → 1`, `lastBuildAt 2026-06-23T08:52:12`**.
- **Доказательство, что в работающем `dist` именно фикс (не «на слово»):** в `specialist-3-7-skill.service.ts` `buildVersion` инкрементится (`:405-409`) ТОЛЬКО после гейта `blocks.length ≥ profileMinObservations(5)` (`:319` иначе ранний `return` без инкремента). Раз версия стала 1 → `loadSubjectReasoningBlocks` вернул ≥5 блоков → `methodology_step`-блоки имеют `role='subject'` И попадают в выборку клона. На старом узком наборе их было бы 0 (+2 rationale) < 5 → версия осталась бы 0. **Q.E.D. фикс активен в рантайме.**

### Почему при этом `traits=[]` (0 черт) — НЕ про код

1. **Второй гейт — кластеризация.** Черта формируется только из группы **≥3 семантически похожих** наблюдений (`specialist-3-7-skill.service.ts:332-335`, `clusterMinObservations=3`, в AdminSetting нет → code-fallback). Первая синтетика (12 заметок §7) — 12 РАЗНЫХ принципов → ни одного кластера ≥3 → rebuild 08:52 отработал за 193 мс БЕЗ LLM-вызовов (`detectTrait` не звался) → 0 черт. Это видно в логах: `kc.skill-profile-rebuild: старт 08:52:12.398` → `rebuild завершён 08:52:12.591`.
2. **jobId-дедуп блокирует повторную перестройку 24 ч.** `enqueueRebuildSkillProfile` всегда `jobId=skill-profile-rebuild_<profileId>`; очередь `removeOnComplete age:86400`. Завершённый backfill-джоб 08:52 занял jobId → последующие 15 «кластерных» заметок (по 5 формулировок на 3 принципа, залиты 08:58–09:01, корректно легли `methodology_step`+subject) **не смогли запустить новую перестройку** (событийный enqueue схлопнулся). Нужна одна свежая перестройка на новых данных (повторный backfill после освобождения jobId / очистки ключа в Redis, либо штатно в течение суток).

**Итог диагностики (промежуточный):** конвейер клона на проде ИСПРАВЕН и фикс активен (звенья 1–3 ✅ доказаны). Осталось технически догнать построение черт (звенья 4–8) — упирается только в jobId-дедуп очереди, не в логику.

### FINAL (2026-06-23) — фикс выкачен и работает; черты не материализуются из-за порога кластеризации синтетики

Через прод-SSH (user → `su`, реквизиты gitignored) сняли jobId-дедуп (`redis-cli DEL` + `ZREM` ключа `bull:core.skill-profile-rebuild:<jobId>`) и прогнали backfill ещё 3 раза. **`buildVersion` рос 1→2→3→4** — то есть rebuild каждый раз исправно проходил гейт «≥5 subject-блоков `methodology_step`» (повторное доказательство, что фикс активен в рантайме). Но `traits=[]` сохраняется, каждый rebuild ~200 мс БЕЗ LLM-вызовов.

**Корень (доказан прямым SQL/pgvector на проде):** черта строится только из кластера ≥3 блоков, попарно похожих ≥`GROUP_SIMILARITY_THRESHOLD=0.78` (`specialist-3-7-skill.service.ts:59,332-335,596-624`). Запрос по 37 subject-блокам `methodology_step` Алексея: `total=37, blocks_with_≥2_neighbors@0.78 = 0, max_pair_sim = 0.896`. То есть отдельные ПАРЫ выше порога есть, но **ни одной тройки** в пределах 0.78 → `eligibleGroups` пуст → `detectTrait` не зовётся → 0 черт. Эмбеддинги у всех 37 блоков ЕСТЬ (проверено: `has_emb=t, count=37`) — дело не в них.

**Вывод:** это характеристика ДАННЫХ, не кода. Короткие синтетические заметки-однострочники (даже пере-формулировки одного принципа) дают блоки с попарной похожестью преимущественно 0.7–0.78 — кластер из 3 не складывается. Реальные встречи, где носитель развёрнуто проговаривает один подход в разных ситуациях, порождают плотные кластеры и черты строятся. Для синтетической демонстрации нужны почти-идентичные формулировки (риск слияния block-dedup) либо понижение `GROUP_SIMILARITY_THRESHOLD`/`clusterMinObservations` (это крутилки логики, не для прод-теста).

**Что доказано по факту (звенья 1–3 + механика 4):**
- Фикс `methodology_step` ВЫКАЧЕН и АКТИВЕН в рантайме прода (buildVersion инкрементится за гейтом, потребляющим methodology_step) — ✅ окончательно.
- ingest→классификация(methodology_step)→subject-атрибуция→router(→SKILL)→specialist-3-7→rebuild — все звенья работают, проверены на проде.
- Звенья 5–8 (verify→active, persona, ответ клона, role-principle) проверить на этих данных НЕ удалось — нет ни одной черты, т.к. кластер не складывается на синтетике. Не блокер кода.

**Прод-операции этой сессии (для отката/памяти):** залито 27 заметок (`/dump`, tag `QA0623*`) на персону Алексея `cmpzl0mee…`; снимался jobId-дедуп в Redis 3×; backfill прогонялся 4× (`buildVersion=4`). Данные синтетические, на проде безвредны (одна тестовая Org «ооо ромашка»).

### FINAL-2 (2026-06-23) — ✅ КЛОН ОТВЕЧАЕТ КАК СОТРУДНИК (звенья 5–7 закрыты)

Сбросили дневную квоту дампов (`redis-cli DEL quota:<userId>:dump_per_day_per_user:<window>` → `1`), долили **2 кластера по 5 почти-идентичных формулировок** (найм + когорты, tag `QA0623G`). SQL: блоков в кластерах ≥3 стало **9**. После снятия дедупа + backfill (buildVersion=6) построилось **6 активных черт** по 3 навыкам:
- «валидирует гипотезы дёшево и быстро» / «тестирует гипотезу дешёво перед масштабированием»
- «анализирует через когорты вместо средних» / «разбивает данные на когорты»
- «нанимает под задачу по ключевому навыку» (×2 концепта)
Все `confidence=medium`, `observationCount=4`, `status=active`. 6 ≥ `MIN_TRAITS_FOR_ANSWER=3` → клон открылся для ответа.

**Q&A на проде (`POST /api/v1/clones/persons/cmpzl0mee…/ask`):**
- **Тематический вопрос** («как подходишь к проверке гипотез?») → HTTP 201, ответ **от первого лица в стиле должности**, **6 цитат `[BLOCK:id]`** (grounding), синтез нескольких черт (когорты + дешёвая проверка + юнит-экономика + письменная фиксация допущений + воронка), в конце дисклеймер «(ответ — от клона должности; могу ошибаться…)». **Клон реально подтягивает и использует накопленные навыки.**
- **Вне-темный вопрос** (рецепт борща) → HTTP 201, **`refused:true`, `refusalReason:'topic_starved'`**, текст «У оригинала недостаточно высказываний по этой теме… Спроси напрямую». **Анти-фейк работает.**

**Итог:** сквозная цепочка клона сотрудника на проде доказана ПОЛНОСТЬЮ (ingest→классификация→subject→router→специалист-3-7→rebuild→черты→verify→active→ExecutablePersona→ответ от 1 лица с цитатами + отказ вне темы). Фикс `methodology_step` подтверждён в бою. Осталось (отдельный заход, не блокер): role-клон + живое подтягивание Regulation (Способ C) + KnowledgeProfile (знания) — нужны роль с носителем, карточка-регламент и блоки expertise.

### Доказательная база (бинарный тест выката)
1. **Baseline до дампа:** клонов в Org `0` (`GET /api/v1/clones` → `total:0`); профиль Алексея `buildVersion:0`, `traits:[]`, `lastBuildAt:null`, `isEmpty:true`.
2. **Залито 12 свежих заметок «подход/принцип»** через «Мысль» (`POST /api/v1/ingest/dump`, заголовок `x-org-id`, пауза 750 мс) — все HTTP 201, уникальные `rawEventId`, не идемпотентные. Метрики графа выросли (rawEvents 44→69, blocks 81→96, entities 102→126).
3. **block-ingest классифицировал их штатно как `methodology_step`** (проверено `GET /api/v1/knowledge/entities/<Алексей>`: у Алексея теперь 17× `methodology_step` + 2× `rationale` + 1× `task_overdue`; свежие блоки 08:15–08:16 — «цель→одна метрика», «гипотезы дёшево/быстро», «когорты вместо средних» и т.д.). Блоки привязаны к персоне «Алексей» (subject-атрибуция работает, как и в прошлом прогоне).
4. **Конвейер block-ingest/distill/linker полностью отстоялся к 08:21:51** (прод-логи `diag logs`; дальше только admin-login от diag). Ни одной записи specialist-3-7 / rebuild / skill-profile.
5. **Событийный rebuild НЕ запустился:** профиль Алексея спустя ~10 мин — `buildVersion:0`, `lastBuildAt:null`, `traits:[]`. `lastBuildAt=null` означает, что rebuild не отработал **ни разу** (иначе версия бы инкрементнулась даже при held-чертах).
6. **Форс штатных cron-ов не помог** (`SkillTraitConceptNormalizerCron.sweep` 211 мс, `SkillTraitVerifyCron.tick` 10 мс, `SkillProfileRecalibrateCron.sweep` 186 мс — отработали мгновенно, обрабатывать нечего). После форса: `buildVersion:0`, клонов `0`.

### Локализация причины (по коду)
- Гейт воркера `specialist-3-7-skill.worker.ts:70` в **dev** использует `SKILL_SUBJECT_SIGNAL_TYPE_SET.has(block.signalType)` (набор включает `methodology_step` — файл `constants/skill-signal-types.ts` присутствует на `dev`).
- Условия для `enqueueRebuildSkillProfile` мои блоки выполняют ПОЛНОСТЬЮ: `status canonical` ✅ + `signalType=methodology_step` ✅ (в dev-наборе) + `role=subject` на персоне-employee «Алексей» ✅. На dev-коде rebuild был бы поставлен в очередь → `buildVersion≥1`. Этого не произошло → **выкаченный воркер отвергает `methodology_step` как `signal_out_of_scope` (старый узкий набор)**.
- **Git-подтверждение:** фикс `364cc546` (и исходный `ff1d09db`) есть на ветке `dev`, но **отсутствует на `main`** (`git merge-base --is-ancestor 364cc546 main` → NO; main содержит несвязанные «standalone-режим / фаза 9»). Прод собран НЕ из ветки, содержащей фикс.

### Что нужно сделать владельцу (разблокировка)
1. Выкатить код с фиксом в прод (промоутить `dev`-фикс на деплой-ветку и) `docker compose up -d --build backend`.
2. `docker compose exec backend bun run scripts/backfill-skill-profiles-rebuild.ts --tenant=cmpuz4gbs000201mvfbf3k2zk` — пересобрать профиль(и) из накопленных `methodology_step`-блоков (у Алексея их уже 17). Без backfill историю не подхватит (событийный rebuild — только на новый блок).
3. Форс `SkillTraitVerifyCron.tick` через админку cron-ов → промоут черт в `active`.
4. После этого qa-tester доигрывает звенья 1–8 + Q&A клона на этой же Org (профиль Алексея уже наполнен синтетикой этого прогона).

### Синтетика этого прогона (для последующего backfill/приёмки)
12 заметок с тегом nonce `QA0623-0..11` на Алексея (rawEventId `cmqqdbhqx017s…` … `cmqqdbqp1018r…`); итог — 17 `methodology_step`-блоков на персоне «Алексей» `cmq2b964s00de01qj1kfpy3cs`, готовы к пересборке.
