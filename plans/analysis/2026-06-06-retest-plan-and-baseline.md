# План повторного тестирования (re-test) + базовые результаты — 2026-06-06

> Цель: пройти заново ВСЕ баги, найденные в QA-прогонах 04–06 июня, и проверить, какие из них закрылись после выката фиксов на прод.
> Прод: `https://meet.crossmark.ru` · аккаунт `mazursv39@gmail.com` (Владелец Org «ооо ромашка», tenant `cmpuz4gbs000201mvfbf3k2zk`).
> Инструменты: **Playwright** (UI/console/network как живой пользователь) + **diag.ts** (read-only сверка с БД/логами: `bun run --env-file=c:/work/z/.env c:/work/z/backend/scripts/diag.ts <cmd>`).
> Источник находок: workflow-сбор 7 кластеров по `plans/analysis/2026-06-05-manual-qa-RESULTS.md` + рефлексии 05_история + ТЗ `plans/tz/2026-06-0{5,6}-*`.

**Легенда статуса фикса:** ✅ заявлен пофикшеным (есть коммит/ТЗ) · 🔧 частично · ⬜ фикса нет (тестируем, ожидая, что ещё открыто).
**Легенда результата ре-теста:** 🟢 PASS · 🔴 FAIL · 🟡 частично/под вопросом · ⏳ не проверено.

---

## 0. Состояние выката (подтверждено эмпирически в браузере 2026-06-06)

Фронт+бэк ветки **sergdev ВЫКАЧЕНЫ на прод** — подтверждено живым кабинетом:
- бренд «Кора» в `<title>` и сайдбаре; группа меню «Задачи» (A3) на месте; «Спринты»+«Архив спринтов» (A2); один значок «Помощник компании» (ТЗ-2 Ф3).
- git-лог: коммиты A1–A7 (`daf1ef8d`…`25fe0739`) и B1–B5 (`86d0b5d8`,`b56dd097`,`ef207417`,`18ef4580`) **закоммичены** (рефлексия join-share, где «реализация не начата», — устарела).

⚠️ Прод-операции трекера (миграция `project_system_generated` авто на `up -d`, patch шаблонов Шаг 6, backfill Шаг 8) — проверить, что прогнаны (см. `docs/operations/prod-deploy-log.md`).

---

## 1. Что уже подтверждено в этой сессии (baseline)

| Находка | Результат | Доказательство |
|---|---|---|
| **S1-11 / S1-06 / S0-06 / S9-03** корень: рассинхрон подписки (DEMO vs tier) | 🟢 PASS | `GET /billing/subscription` → `status:ACTIVE, paymentMode:paid`; `GET /me/entitlements` → `tier_standard`, все `feature.*:true`. Источники согласованы, демо-баннера нет. |
| **S4-01 / S3-01 / S2-01 / S10-01** доступ к ядру (встречи/чат/инвайты/проекты) | 🟢 база снята | подписка ACTIVE → гейт `@RequireSubscription` не режет. Точечно прокликать каждое действие в ре-тесте. |
| **S1-02 / S1-10 / S0-02** бренд «Z» → «Кора» в title | 🟢 (login/meetings) | `Вход — Кора`, `Кора — память компании`. ⏳ проверить detail-страницы `/tables/[id]`, `/settings`, `/dump`. |
| **A3** группа меню «Задачи» + «Память компании» (Дамп/Таблицы) | 🟢 PASS | сайдбар: «Задачи» (Проекты/Спринты/Входящие/Архив спринтов); «Память компании» (Дамп/Таблицы/Идеи/…). |
| **A2** «Спринты»+«Архив спринтов» в навигации | 🟢 (nav) | пункты на месте; ⏳ контент `/sprints/archive` (термины, длительность). |
| **S1-03 / S1-04 / ТЗ-2 Ф3** один плавающий помощник | 🟢 PASS | единственный FAB «Помощник компании»; второго (Concierge) нет. ⏳ проверить отсутствие «Концьерж»/«Concierge» в туре. |
| **S6-12 / A2** честная длительность | 🟢 (список) | в журнале «1м», «<1 мин» вместо «0м». ⏳ проверить шапку/`Обзор` на `/result`. |
| **S0-07** шум 401 в консоли | 🟡 частично | boot-`GET /accounts/me → 401` ещё есть (1 раз), но НЕ повторяется на каждой странице, как было. |
| **S5-01** видео-плеер (крутилка) | ⏳ | проверить на `/meetings/<id>/result` (range-GET к composite.mp4 206, `<video>` readyState>0). |

**Кандидаты-FAIL, замеченные в превью результата встречи `01KTDHPTHS9T5YGXFANKES765J` (проверить на полной странице):**
- английские заголовки **«Summary», «Action items», «host», «Team sync»** в превью результата — англицизмы убраны не везде;
- «**1 участников**» — неверная плюрализация счётчика участников;
- «Action items: **6 всего**», в списке показано 5 — проверить S6-03 (показ всех задач) на `/result`;
- diag-trace старой встречи (04:07): `aiResult.create()` → `Unique constraint failed on meetingId` (гонка fast, S6-01) + `block-linker invalid JSON — повтор` (S6-02 retry — ожидаемое поведение фикса). Встреча 04:05 может быть **старше** прод-выката → чистый ответ даст свежая встреча.

---

## 2. Протокол свежей встречи (владелец проводит новую встречу → берём её на анализ)

Как только встреча проведена и дошла до `ai_ready`, по её `<id>`/`<traceId>` прогнать:

**diag (БД/цепочка):**
```
diag trace   --meeting <id>      # статус, транскрипт, reportFast, ошибки цепочки
diag chain   --trace mtg_<id>    # полный технический след; ИСКАТЬ: НЕТ "Unique constraint failed ... aiResult" (S6-01)
diag report  --meeting <id>      # reportStatuses.reportFast != 'partial'; summaryFast != null (S6-01);
                                 #   transcript.totalDurationSeconds > 0 ИЛИ честный fallback (S5-02);
                                 #   audioTracks[].participantName = имя, не "Participant" (S6-08);
                                 #   число задач = таблице Task (S6-03)
diag llm-calls --meeting <id>    # vox-вызов: есть ли word-timestamps (S5-02)
diag logs --at-least WARN --from 2026-06-06   # vox.no_words / block-linker / прочие WARN
```

**Playwright (UI на `/meetings/<id>/result`):**
- видео грузится сразу, без вечной крутилки (S5-01); в Network — 206 к `composite.mp4`.
- вкладка «Отчёты» → «Открыть» основного → открывается контент, НЕ «Не удалось загрузить отчёт» (S6-07, был 404).
- счётчик задач одинаков в превью / «Action items» / вкладке «Задачи» и = реальному числу; показаны все задачи, не только слабая fast (S6-03).
- спикер в «Транскрипт»/«Аудиодорожки» = имя участника, не «Participant» (S6-08).
- длительность не «0м» (S5-02/S6-12); «Поведение участников» — либо ненулевое, либо честное «недоступна», без «Тишина 100%».
- `Обзор`/`structuredData` — русские заголовки (Задачи/Блокеры/Решения), не сырой JSON и англо-ключи (S6-04).
- «1 реплика» (не «1 реплик») — плюрализация (S6-09); вкладки «Заметки»-дубля нет (S6-10); вкладка чата = «Чат комнаты» (S6-11).

**Дедуп участников (если на встрече были приглашённые):**
- пригласить 2 чел. → оба вошли → счётчик показывает присутствовавших (хост+2), НЕ 5 (PCD-MAIN/F1/F2/F3).
- `diag trace --meeting <id>` — на приглашённого одна строка (переиспользованная `invitee:`, статус joined), без пары `invitee:`+`guest:`.

---

## 3. Матрица повторного тестирования по кластерам

### 3.1 Доступ к продукту и пэйвол (корень S1-11)

| ID | sev | фикс | Критерий PASS / как проверить |
|---|---|---|---|
| S1-11 | crit | ✅ | `GET /billing/subscription`=ACTIVE согласован с entitlements; нет демо-баннера. **🟢 уже PASS** |
| S1-06 | high | ✅ | баннера «Демо-режим» нет ни на одной странице; `/dashboard` не сводится к «Оплатить подписку». |
| S4-01 | crit | ✅ | `POST /api/v1/meetings`→201 (создать встречу проходит); на DEMO — PaywallModal, не молчание. |
| S3-01 | crit | ✅ | `/chat`: вопрос → ответ 200 (не молчит). |
| S2-01 | crit | ✅ | `/structure`→Пригласить → успех; при 403 — заметная PaywallModal. |
| S10-01 | crit | ✅ | `/projects/new`→Создать → 201; доски/задачи доступны. |
| S10-02 | high | ✅ | все 4 платных действия при 403 поднимают одну PaywallModal (единый паттерн). |
| S2-03 | med | 🔧 | роли создаются (201) — согласовано с баннером; аудит `@RequireSubscription` — vNext. |

### 3.2 Честный UI (ТЗ-2)

| ID | sev | фикс | Критерий PASS / как проверить |
|---|---|---|---|
| S1-01 | crit | ✅ | открыть `/tables/<id>`, `/structure/persons/<id>`, `/roles/<id>`, `/sprints/<id>` — данные грузятся, в Network нет `.../undefined`. |
| S1-05 | med | 🔧 | на detail-странице нет серии 404 `/undefined` в консоли за минуту. |
| S1-02/S1-10/S0-02 | med | ✅ | `<title>` `/dump`,`/tables`,`/settings`,`/feed/probe-questions` оканчиваются «— Кора». |
| S1-03 | high | ✅ | одна плавающая кнопка помощника, кликается; на мобайле тоже одна. |
| S1-04 | med | ✅ | в туре и на FAB нет «Concierge»/«Концьерж» — только русское. |
| S4-02 | med | ✅ | `/meetings/create` опции отчёта без англицизмов (Follow-up/action items/assignee/prompt). |
| S10-03 | med | ✅ | `/projects/new` категории по-русски, «Технологии» вместо «technology». |
| S1-07 | high | ✅ | `/dump` после сохранения — видимое подтверждение + ссылка «куда попало». |
| S1-08 | high | ✅ | `/actions`→«Открыть» Вопроса Коры → раскрытый вопрос в `/me/notifications`, не пустая заглушка. |
| S1-09 | med | ✅ | `/actions` есть строка про канал/уведомление; заголовок `/feed/probe-questions` = «Вопросы Коры». |
| S3-02 | med | 🔧 | заголовок `/chat` = «Помощник компании»; ⬜ дубль `/chat` vs `/chat-v2` — фикса нет, проверить остался ли баннер. |

### 3.3 Надёжность отчёта встречи + видео (ТЗ meeting-report-reliability)
> Основная проверка — на **свежей встрече** (см. §2). Старая `01KTDHPTHS9T5YGXFANKES765J` могла предшествовать выкату.

| ID | sev | фикс | Критерий PASS |
|---|---|---|---|
| S5-01 | crit | ✅ | видео играет сразу (load=eager), 206 к composite.mp4; то же на share-странице. |
| S6-07 | crit | ✅ | «Открыть» основного отчёта → 200 с контентом (был 404). |
| S6-01 | high | ✅ | `reportFast`≠'partial', `summaryFast`≠null; в логах НЕТ Unique-constraint на aiResult (атомарный upsert). |
| S6-03 | high | ✅ | число задач едино во всех поверхностях = таблице Task; показаны все, не только fast. |
| S6-08 | high | ✅ | спикер = имя, не «Participant» (на НОВОЙ встрече; старые без бэкфилла). |
| S5-02 | high | 🔧 | длительность не «0м» (fallback на recording); поведение либо ненулевое, либо честно «недоступна». Корень word-ts ASR — под вопросом. |
| S6-04 | med | ✅ | `Обзор`/structuredData — русские заголовки и списки, без сырого JSON/англо-ключей. |
| S6-09 | med | ✅ | «1 реплика» (плюрализация); бейдж осмыслен; хедер без «0 мин». |
| S6-10 | med | ✅ | вкладки «Заметки»-дубля нет. |
| S6-11 | med | ✅ | вкладка чата = «Чат комнаты»; правая AI-панель сворачивается. |
| S6-12 | low | ✅ | длительность из записи (≈«<1 мин»), не «0м». |
| S6-02 | med | ✅ | block-linker: ретрай + метрика `kc_block_linker_invalid_json_total`; связи строятся даже на грязном JSON. |

### 3.4 Дедуп счётчика участников (ТЗ participant-count-dedup)

| ID | sev | фикс | Критерий PASS |
|---|---|---|---|
| PCD-MAIN | high | ✅ | «пригласил двоих → оба вошли» → счётчик присутствовавших (не 5). |
| PCD-F1 | high | ✅ | приглашённый по personId без `?inv=` → одна строка (переиспользована invitee:), повторный вход — no-op. |
| PCD-F2 | med | ✅ | вход по `/m/<id>?inv=<token>` через гостевую форму → тело `POST /join` содержит `invite_token`. |
| PCD-F3 | high | ✅ | счётчик present-only течёт и в ROI-метрики/AI (не завышен). |
| PCD-F5 | low | ✅ | у `invitee:`-участника проставлен `joinedAt` через webhook; служебные (egress/agent) не как люди. |
| PCD-F4 | med | ⬜ | DB partial-unique + backfill — **ОТЛОЖЕНО**; физические дубли в БД остаются (счётчик их не считает). |

### 3.5 Вход/возврат/ссылка встречи (B1–B5)
> Коммиты есть в git; проверять на **joinable** встрече (scheduled/active).

| ID | sev | фикс | Критерий PASS |
|---|---|---|---|
| B1 | — | ✅ | у active/scheduled встречи видны «Войти», «Скопировать ссылку», «Пригласить»; у ai_ready — только результат. |
| B2 | high | ✅ | «Скопировать ссылку» в комнате → тост; при отказе clipboard — fallback с ссылкой, не молча. |
| B3 | high | ✅ | у хоста «Выйти» и «Завершить» раздельно; после «Выйти» → «Вернуться в встречу» (`POST /join`→200), работает многократно. |
| B4 | — | ✅ | на лобби `/m/<id>` у хоста есть «Скопировать ссылку». |
| B5 | — | ✅ | «Пригласить» после создания/в комнате → `POST /meetings/:id/invitees`→200; повтор не плодит дубль; виден в Swagger. |
| B-host-guest-copy | low | ⬜ | финальный экран хосту не показывает «попросите организатора отправить отчёт». |
| B-webrtc-noise | low | n/a | RTCErrorEvent при disconnect — норма, не баг (не путать с регрессией). |

### 3.6 Трекер задач (A1–A7)

| ID | sev | фикс | Критерий PASS |
|---|---|---|---|
| A1 | crit | ✅ | `/intake`→«Принять» открывает пикер проекта → `POST triage` с targetProjectId → 200, карточка уходит; при 400 — тост. |
| A2-archive | high | ✅ | `/sprints/archive`: title/h1 = «Архив спринтов»; нет «гипотез/Подтвердились»; тайлы «Завершены/Отменены/В процессе». |
| A2-duration | med | ✅ | спринт 6→20 июн = «14 дней» (не 15). |
| A2-term | high | ✅ | единый термин «Спринт/Спринты» везде; «Цикл/Циклы» в UI нет. |
| A3-menu | high | ✅ | группа «Задачи» (Проекты/Спринты/Входящие/Архив); порядок групп корректен. **🟢 видно в baseline** |
| A3-inbox-names | high | ✅ | `/me/inbox` = «Мои задачи»; проектный intake фильтрован по проекту. |
| A4-subtabs | med | ✅ | у проекта ≤6 inline-вкладок + «Ещё ▾» с остальными. |
| A5-language | med | ✅ | `/intake` «Разбор входящих…», «ваша задача»; «Оценка сложности»; правильное склонение; роли продаж без SDR/BANT/CHAMP; бэк-промпт «Кора». |
| A5-dump-link | med | ✅ | баннер дампа ведёт на `/ideas` («Идеи»), не на `/intake`. |
| A6-shadow-project | low | ✅ | после общего спринта компании в `/projects` нет «Спринт компании» (systemGenerated). |
| A7-badge-polling | low | ✅ | бейдж-запросы дедуплицируются при переходах; «Входящие» обновляется после триажа. |

---

## 4. Открытые (фикса нет — тестируем, ожидая, что ещё НЕ закрыто)

| ID | sev | Что ожидаем (всё ещё открыто) |
|---|---|---|
| S0-01 | high | нет принудительной смены временного пароля при первом входе. |
| S0-03 | med | расхождение текста правила пароля (статичная подсказка vs live-валидация). |
| S0-04 | low | нет тоста «Пароль изменён» после смены. |
| S0-05 | med | нет упрощённого онбординга для пустого аккаунта (~35 пунктов меню сразу). |
| S0-07 | low | boot-401 `/accounts/me` пишется в консоль (частично смягчено — не на каждой странице). |
| S2-02 | med | в диалоге «Пригласить» не пояснена роль Telegram-бота. |
| S3-02(b) | med | сосуществуют `/chat` и `/chat-v2` (баннер про новую версию). |
| S9-02 | med | расхождение наборов навигации мобайл vs десктоп. |
| PCD-F4 | med | нет DB-инварианта дедупа + backfill (физические дубли в БД). |

---

## 5. Как фиксируем результат (для двух финальных отчётов)

На каждый пункт: **🟢/🔴/🟡** + доказательство (статус запроса+url / текст консоли / снапшот / вывод diag). Затем:
- **Технический отчёт** — `plans/analysis/2026-06-06-retest-RESULTS-technical.md`: по кластерам, с url/статусами/diag-выводами и корнями для FAIL.
- **Отчёт простым языком** — `plans/analysis/2026-06-06-retest-RESULTS-plain.md`: что чинили, что реально заработало, что ещё нет — без жаргона.

> Полная пер-находочная детализация (repro/expectedAfter/fixRef по каждому ID) — в выгрузке workflow `retest-matrix-build` (7 кластеров). Этот файл — рабочая карта прогона.
