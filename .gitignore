node_modules/
.next/
dist/
build/
out/

# ENV — секреты никогда не коммитим, только *.example.
.env
.env.*
!.env.example

# Локальные креды тестового кабинета (скилл qa-tester, Playwright-приёмка). НЕ коммитить.
/.qa-cabinet.local.json

# Локальные креды прод-SSH (точечные прогоны скриптов на проде). НЕ коммитить.
/.prod-access.local.json

*.log
coverage/

# LiveKit prod-конфиги с секретами — в git только *.example.
# На сервере: cp livekit.yaml.example livekit.yaml && cp egress.yaml.example egress.yaml
infra/livekit/livekit.yaml
infra/livekit/egress.yaml

# Бинарные артефакты — никогда в git (LiveKit и пр. ставятся из Docker-образов).
*.exe
*.dll
*.zip
*.7z
*.tar
*.tar.gz

.DS_Store
.vscode/
.idea/
.cache/
.bun-cache/
.vexp
.playwright-mcp

# Локальные артефакты smoke-тестов и lint
backend/tmp/

# Claude Code harness кеш/state агентов
.agents/
.impeccable/
.qwen/
.claude/worktrees/

# Скриншоты/QA-снимки и аналитические артефакты сессий в корне (не для коммита — PII/мусор)
/*.png
# Артефакты прогонов eval/смоук и ошибочные вложенности — не коммитить
/backend/backend/
backend/scripts/_out/
backend/scripts/eval/_out/
backend/test/eval/**/runs/
/infra/document-conversion/smoke/results/

# Локальные тестовые медиа (звонки/записи) в корне — PII, НЕ коммитить (удаляются после теста).
/*.mp3
/*.wav
/*.m4a
/*.ogg
/*.opus
/*.aac
/*.flac
/*.amr
