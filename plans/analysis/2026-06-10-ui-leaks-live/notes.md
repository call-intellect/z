# Живой обход кабинета (korateam.ru) — найденные технические протечки

> Дата: 2026-06-10. Аккаунт: тестовый `mazursv39@gmail.com` (Org «ооо ромашка»).
> Метод: Playwright по проду (деплой БЕЗ моих локальных фиксов). Скан текста экранов.
> Дополняет код-аудит (workflow `w91hzyb4e`). Скриншоты — рядом в этой папке.

## Найдено (живьём)

### HIGH — «Подтверждения» (`/actions`), заголовки карточек
Список показывает сырой тип ресурса + внутренний cuid:
- `Конфликт карточек: idea_block` → должно: «Конфликт карточек знания».
- `Требует проверки: idea cmq4kioyn01qs01piqh8zqd0k` → сырой тип `idea` + cuid.
- `Требует проверки: idea cmq4kjfjc01si01pia3bapn7m`.
- `Требует проверки: knowledge_profile cmpzl0mee00065gnq9vm3ajv1` → сырой `knowledge_profile` + cuid.
- Категория: english-code + internal-id. Скрин: `ui-leak-actions-list.png`.
- Фикс: маппер resourceType→RU (`resourceTypeRu`, lowercase — уже поправлен в детали конфликта; здесь его НЕ применяют) + не показывать cuid (резолвить заголовок ресурса). Компонент списка actions.

### HIGH — Отчёт встречи (панель `/meetings?selected=…`), флагман
- Заголовки секций по-английски: `SUMMARY`, `ACTION ITEMS` → «Сводка», «Задачи/Поручения».
- Роль приклеена к имени: `Алексейhost` → «Алексей · ведущий» (роль `host` не локализована и не отделена).
- Категория: untranslated / english-code. Скрин: `ui-leak-meeting-report.png`.

### Чисто (протечек не видно)
- `/entities` — типы локализованы (Сотрудник, Метрика, Тема, Подразделение, Цель, Технология…).
- `/cards` — пусто (нет карточек), без протечек.

## Связь с детальным конфликтом
Деталь конфликта (`/curation/conflicts/[id]`) с сырым JSON «Доказательства» + cuid + `IDEA_BLOCK`/`contradicts`/`block-linker` — **уже исправлена локально** (ConflictDetailClient + resourceTypeRu + conflictRelationLabel), ждёт выката. Но СПИСОК `/actions` — отдельный компонент, его правка ещё нужна (см. выше).
